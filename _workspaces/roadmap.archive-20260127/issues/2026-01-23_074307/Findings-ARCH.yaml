findings:
  agent: ARCHITECTURE_AND_DESIGN_PATTERNS
  execution_timestamp: 2026-01-23_074307
  severity_distribution:
    CRITICAL: 1
    HIGH: 6
    MEDIUM: 9
  findings:
    srp_violations:
      count: 4
      severity: HIGH
      instances:
      - component: cortex/orchestrators/orchestrator.py
        issue_id: ARCH-SRP-001
        description: 850-line Orchestrator class handles 4 concerns
        concerns:
        - AC scheduling and queuing
        - AC execution orchestration
        - State lifecycle management
        - Persistence and logging
        impact: Testing difficulty, high coupling, difficult maintenance
        remediation: Split into Orchestrator, Scheduler, LifecycleManager, PersistenceManager
    dependency_inversion_violations:
      count: 12
      severity: HIGH
      pattern: Hard-coded dependencies instead of dependency injection
      components:
      - cortex/execution/executor.py (12 direct instantiations)
      - cortex/api/service_client.py (8 direct instantiations)
      remediation: Implement dependency injection container or factory pattern
    coupling_antipatterns:
      count: 8
      severity: MEDIUM
      types:
      - pattern: Feature envy - accessing multiple attributes of other objects
        components:
        - brain/core/rule_evaluator.py
        - orchestrators/coordinator.py
      - pattern: Circular dependencies between modules
        components:
        - knowledge ← → governance
        - core ← → orchestrators
    abstraction_failures:
      count: 5
      severity: MEDIUM
      issue: Missing abstractions lead to code duplication
      remediation: Extract common patterns to abstract base classes or mixins

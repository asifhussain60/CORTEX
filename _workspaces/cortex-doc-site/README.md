# CORTEX Visual Documentation Site (Offline)

## Open
1. Unzip
2. Open `index.html` directly (double-click). Works via `file://`.

## Update content
Replace `assets/data/cortex-doc-data.js` with a regenerated bundle.

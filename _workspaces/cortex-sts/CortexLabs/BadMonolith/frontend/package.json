{
  "name": "fintrack-frontend",
  "version": "0.0.1",
  "description": "CortexLabs FinTrack Frontend — intentionally bad for CORTEX STS demo",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "tsc && echo 'Open index.html in browser'"
  },
  "devDependencies": {
    "typescript": "~5.2.0"
  }
}

Execute a CORE-064 full-sweep `/audit fix` across CORTEX to achieve 100% production readiness with zero drift, zero brittleness, and zero regression. The scope is comprehensive: both prompt files (`cortex-architect.prompt.md`, `CORTEX.prompt.md`), all agent files in `.github/agents/` (specifically `CORTEX.md`, `cortex-architect.md`, `cortex-auditor.md`, `cortex-interactive.md`, `cortex-digest.md`, `cortex-environment-setup.md`, `cortex-holistic-validator.md`, `cortex-meta-auditor.md`, `cortex-sts-refactoring.md`, `cortex-universal-orchestration.md`, `STAGE-0-GOVERNANCE-AUDIT-SPEC.md`, `AGENT-INDEX.md`), all wiring YAMLs in `cortex-registry/core/specifications/`, and the source orchestrators + MCP tools themselves.

**PART 1 — MCP TOOL NAME DRIFT REMEDIATION (P0):**
For every prompt and agent file, apply these canonical corrections inline: replace `cortex_sample_tool` with `cortex_verify` (op: `mcp`) as the MCP health check verification tool; replace `cortex_validate_compliance` with `cortex_validate` (op: `compliance`); replace `cortex_load_core_rules` with `cortex_load` (op: `rules`); replace `cortex_query_governance` with `cortex_governance` (op: `query`); replace `cortex_check_dependency_drift` with `cortex_check` (op: `dependencies`); replace `cortex_capture_metrics` with `cortex_metrics` (op: `capture`); replace `cortex_onboard_repository_v3` with `cortex_onboard` (op: `full`); replace `cortex_audit_remediation_plan` with `cortex_governance` (op: `remediation_plan`). Correct ALL instances of "25 MCP tools" or "25 tools" to "24 MCP tools" or "24 tools" across all files. Correct ALL instances of "22 rules" when referring to CORE governance rules to "35 CORE rules" (37 total including 2 AC rules). Correct ALL instances of "10-Point" or "10 checks" audit references to "14-Point" and "12 checks" respectively to match the canonical `cortex-architect.prompt.md` which already has the correct 14-point audit and 12 meta-audit checks. Update `cortex-architect.md` audit check #8 from "All 25 tools functional" to "All 24 tools functional". Update `CORTEX.md` agent to replace its stale tool table with the operation-based tool names. Update `CORTEX.prompt.md` MCP verification from `cortex_sample_tool` to `cortex_verify` (op: `mcp`). Fix `AGENT-INDEX.md` line 220 which still says `cortex_lens_analyze` replaced by `cortex_onboard_repository_v3` — correct to `cortex_onboard` (op: `full`).

**PART 2 — DRIFT PREVENTION: META-AUDITOR EXPANSION (P0):**
Update `cortex-meta-auditor.md` to add permanent drift-prevention checks that close the feedback loop: Check #13 — MCP tool name alignment (all tool names referenced in agent/prompt files must match registered IDs from `cortex/mcp/mcp_registry.py`; detect operation-based consolidation drift where old tool names survive in docs after registry consolidation); Check #14 — Governance rule count alignment (agents referencing CORE rule counts must match `skull-rules.yaml` canonical count of 35 CORE + 2 AC); Check #15 — Knowledge YAML wiring (verify `cortex-registry/knowledge/` domain YAMLs including `architecture/`, `backend-python/`, `security/`, `testing-validation/`, `devops-infrastructure/`, `performance-optimization/` are referenced and loadable by KnowledgeSynthesisEngine at `cortex/intelligence/knowledge/knowledge_synthesis_engine.py`); Check #16 — LENS synthesis health (verify 8 LENS analyzers importable from `cortex/lens/` and golden tests in `tests/golden/` passing); Check #17 — Success/failure pattern learning (verify `.cortex-runtime/traces/orchestrator-traces.db` captures AC markers per orchestrator invocation and audit sessions are queryable for regression pattern detection — orphaned `AC_START` without `AC_COMPLETE` is a P0 violation). Also update the `cortex-auditor.md` 13-Point Audit to become a 17-Point Audit by adding: Check #14 — MCP tool name registry alignment (every prompt/agent tool reference must match `mcp_registry.py` registered IDs, detect consolidated-name drift); Check #15 — Governance YAML SSOT enforcement (only `skull-rules.yaml` in `cortex-registry/core/tier0-skull/` is canonical source of truth for rule count, `core-rules.yaml` in `cortex-registry/governance/` is secondary reference — detect count divergence as P1); Check #16 — Knowledge synthesis wiring (registry knowledge YAMLs in `cortex-registry/knowledge/` are loadable and have no dead references to deleted knowledge files); Check #17 — LENS pipeline health (8 analyzers importable from `cortex/lens/`, golden tests green in `tests/golden/test_lens_full_pipeline_truth.py`). Then update the stage count references: `cortex-architect.prompt.md` Stage 2 should say "17-Point Production Scan" and Stage 6 should say "17 checks"; update the same in `AGENT-INDEX.md`, `cortex-auditor.md`, `cortex-architect.md`, and `copilot-instructions.md`.

**PART 3 — LIVE CAPABILITIES YAML FOR MASTER ORCHESTRATOR (P1):**
Create `cortex-registry/core/capabilities-manifest.yaml` — a single live YAML that MasterOrchestrator references to know what capabilities exist and can self-diagnose drift. This YAML must enumerate: all 22 orchestrators with their module paths, health_check status, and wiring priority; all 24 MCP tools with their registered IDs and operation lists; all 35 CORE rules with their IDs and severity; all 8 LENS analyzers with their import paths; the knowledge domain registry pointing to `cortex-registry/knowledge/` subdirectories; the workflow template registry pointing to `cortex-registry/workflows/templates/`; and the response template SSOT at `.github/templates/cortex-response-templates.md`. MasterOrchestrator should be able to load this manifest on startup and compare live system state against it to detect capability drift (orchestrator not importable, MCP tool missing from registry, LENS analyzer broken, knowledge domain empty). Place this in `cortex-registry/core/` since it's a core architectural artifact, not a planning document.

**PART 4 — RESPONSE TEMPLATE RUNTIME COMPOSITION (P1):**
Ensure the response rendering system in `cortex-architect.prompt.md` and `CORTEX.prompt.md` enforces that response templates are pieced together at runtime from reusable blocks in `.github/templates/cortex-response-templates.md` — no duplication of header format, progress bar format, or section structure across prompts/agents. Every response must use simple business language with practical examples. The InteractionOrchestrator at `cortex/orchestrators/core/interaction_orchestrator.py` should surface blind spots and edge cases on every turn using LENS analysis, presenting them in the "Analysis" section with plain-language explanations. Add explicit instruction to `cortex-architect.prompt.md` under RESPONSE FORMAT stating: "Templates are composable blocks — assemble from SSOT at runtime, never duplicate inline. Use business language: explain 'CORE-008 violation' as 'You're trying to write code without tests first — CORTEX requires a failing test before any implementation.' Surface edge cases discovered by LENS in the Analysis section using the pattern: 'CORTEX noticed: {finding} — this matters because {impact} — suggested action: {step}'."

**PART 5 — GOVERNANCE ENFORCEMENT + WIRING INTEGRITY (P0):**
Verify GovernanceRegistry at `cortex/orchestrators/core/governance_registry.py` loads from canonical `cortex-registry/core/tier0-skull/skull-rules.yaml` (37 rules = 35 CORE + 2 AC), not from stale `cortex-registry/governance/core-rules.yaml`. Verify `core-rules.yaml` metadata `rule_count` matches its actual content (35 rules, not 54). Verify all 4 wiring spec YAMLs in `cortex-registry/core/specifications/` (`orchestration-master-wiring.yaml`, `core-orchestrator-wiring.yaml`, `domain-orchestrator-wiring.yaml`, `support-orchestrator-wiring.yaml`) have module paths that are importable — run L1 structural validation for each. Verify EnforcementOrchestrator pre-commit hook validates AC markers, type hints, docstrings, and file naming. Any `cortex_process_request` references in active source code should be updated to reference the consolidated tool pattern — this stale tool name was removed from the MCP registry but still appears in docstrings and examples in `cortex/mcp/tools/core.py`, `cortex/mcp/native_tool_gate.py`, `cortex/mcp/self_healing.py`, `cortex/opentelemetry_tracing.py`, and `cortex/intelligence/education/next_step_generator.py`.

**PART 6 — LENS SYNTHESIS + KNOWLEDGE WIRING (P1):**
Verify LENS orchestrator at `cortex/lens/lens_orchestrator.py` imports cleanly with all 8 analyzers. Verify KnowledgeSynthesisEngine at `cortex/intelligence/knowledge/knowledge_synthesis_engine.py` can discover and load knowledge YAMLs from `cortex-registry/knowledge/` (6 domains: architecture, backend-python, security, testing-validation, devops-infrastructure, performance-optimization). Verify the empty `cortex/knowledge/` directory (if it still exists) is removed — knowledge content lives in `cortex-registry/knowledge/`, not in the Python package. Verify golden tests pass for LENS (`test_lens_full_pipeline_truth.py`), governance (`test_governance_tier_precedence_truth.py`), MCP (`test_mcp_e2e_truth.py`), and work items (`test_work_item_provider_truth.py`).

**PART 7 — SUCCESS/FAILURE PATTERN LEARNING + TRACES (P1):**
Verify `.cortex-runtime/traces/orchestrator-traces.db` is the canonical persistence target for AC markers. Every orchestrator's `health_check()` and public methods must emit `AC_START`/`AC_COMPLETE` pairs. Add instruction to `cortex-architect.prompt.md` under CROSS-CUTTING INTELLIGENCE stating that audit sessions should be queryable from the traces DB — MasterOrchestrator can query previous audit sessions to detect recurring failure patterns (same P0 appearing across multiple audits = systemic issue requiring architectural fix, not point remediation). Add a note that the capabilities manifest YAML created in Part 3 should be version-stamped after each successful `/audit fix` run to track capability evolution over time.

**PART 8 — WORKFLOW TEMPLATES + ORCHESTRATOR INTEGRATION (P2):**
Verify workflow templates in `cortex-registry/workflows/templates/` are referenced by the appropriate orchestrators. The MasterOrchestrator's 4-stage pipeline (Interaction → Intent → Intelligence → Execution) should have a template entry in the capabilities manifest. Each execution mode (AUDIT, IMPLEMENT, FIX, REFACTOR, etc.) should have its workflow steps documented in the capabilities manifest under a `workflows` key so MasterOrchestrator knows the expected sequence for each mode and can detect if an orchestrator skips a required step.

**PART 9 — INFLIGHT UPGRADE DETECTION + INTELLIGENT MERGE (P0):**
Wire an inflight upgrade detection system so that every CORTEX session — triggered by `copilot-instructions.md`, `CORTEX.prompt.md`, `cortex-architect.prompt.md`, and any loaded agent — silently checks `origin/main` for new commits before routing the user's request. The mechanism works as follows: at Stage 0 (before governance pre-flight), MasterOrchestrator runs `git fetch origin main --dry-run` to check if `origin/main` has commits not present in the local `CORTEX` branch by comparing `git rev-list --count HEAD..origin/main`. If the count is zero, proceed silently with no user notification. If the count is greater than zero, emit a single inline notice — "CORTEX detected N new commit(s) on origin/main — upgrading before proceeding" — then execute the upgrade pipeline autonomously: (1) `git fetch origin main` to download the changes; (2) `git merge origin/main --no-ff -m "chore: inflight upgrade from origin/main — {timestamp}"` using the non-fast-forward strategy to preserve local branch history and allow clean rollback; (3) if the merge produces conflicts in `.github/prompts/`, `.github/agents/`, `cortex-registry/`, or `cortex/orchestrators/`, use a conflict resolution strategy that keeps the LOCAL version for all `cortex-registry/` content (user customizations), keeps the REMOTE version for `.github/prompts/` and `.github/agents/` content (framework updates), and for `cortex/orchestrators/` and `cortex/mcp/` applies a three-way merge preferring the remote if the local file has not been modified since the last successful upgrade (tracked via `.cortex-runtime/traces/upgrade-manifest.json`); (4) after merge, immediately run the full `/audit fix` 9-stage pipeline to verify wiring integrity — any new orchestrators introduced in the merge must be importable (L1 check), any new MCP tools must be registered in `mcp_registry.py`, and any new governance rules in `skull-rules.yaml` must load cleanly through GovernanceRegistry; (5) if all checks pass, write a version-stamped entry to `.cortex-runtime/traces/upgrade-manifest.json` with fields `{timestamp, origin_sha, local_sha, commits_merged, conflicts_resolved, audit_result, tests_passed}`; (6) if any check fails, execute `git merge --abort` to restore the pre-merge state, write a failure entry to `upgrade-manifest.json`, and surface the conflict inline to the user with specific file paths and resolution instructions.

Add the following instruction block to both `CORTEX.prompt.md` (under MCP section) and `cortex-architect.prompt.md` (under STAGE 0 section): "**Inflight Upgrade Protocol (runs before Stage 0):** At session start, check `git rev-list --count HEAD..origin/main`. If > 0: fetch → merge origin/main → run `/audit fix` → continue. If merge fails: abort → surface conflicts inline → request user guidance. Upgrade manifest: `.cortex-runtime/traces/upgrade-manifest.json`. Silent unless upgrade detected (CORE-049)." Add `/upgrade` as a quick command to `CORTEX.prompt.md` and `cortex-architect.prompt.md` quick command tables: "| `/upgrade` | Check origin/main, merge if ahead, run audit fix | Inflight upgrade |". Wire the upgrade check into `UpgradeOrchestrator` at `cortex/orchestrators/support/upgrade_orchestrator.py` by adding a `check_upstream_and_merge()` method that encapsulates the git fetch/compare/merge/audit cycle, callable from MasterOrchestrator's Stage 0. Add the upgrade trigger to `AGENT-INDEX.md` intent mapping: "| **UPGRADE** | `cortex-environment-setup.md` + `cortex-auditor.md` | ~5,500 |". Update `cortex-meta-auditor.md` Check #18 — Upgrade manifest health (verify `.cortex-runtime/traces/upgrade-manifest.json` exists and last entry has `audit_result: pass`; if missing or last result is `fail`, surface as P1 warning that inflight upgrade left the system in an unverified state). Ensure the upgrade detection check is guarded by a `CORTEX_AUTO_UPGRADE=true` environment variable (defaulting to `true`) so users can disable it via `.vscode/settings.json` or shell export without modifying any code.

**PART 10 — INTENT REFLECTION: PARAGRAPH FORMAT ENFORCEMENT (P0):**
Fix the DoR (Definition of Ready) display in `cortex-architect.md` — the agent currently renders intent reflection as a flow diagram with `→` arrows and `[DESIGN/IMPLEMENT]` labels (lines 75–85) which exposes internal architecture names and uses table-like structure. Replace this with the paragraph pattern already canonical in `CORTEX.prompt.md` §DoR Display: "**Here's what CORTEX heard:** You've asked CORTEX to {one-line summary}: 1. **{Action}** — {plain description}..." with CORTEX confidence indicator and `proceed` gate. Apply the same correction to any agent or prompt file that currently renders intent reflection using markdown tables (`| Field | Value |` rows), code fences, or bullet-point lists of internal field names (Handler, Scope, Rules, AC markers). Specifically audit and fix: `cortex-architect.md` lines 75–110 (replace flow diagram with paragraph DoR), `cortex-universal-orchestration.md` (verify DoR format is paragraph not table), and `STAGE-0-GOVERNANCE-AUDIT-SPEC.md` (verify Stage 0 output is single paragraph with inline violations, no separate table for each check). Add to `cortex-meta-auditor.md` Check #19 — DoR paragraph enforcement (scan all agent files for `| Field |` or `| Handler |` patterns inside DoR/intent reflection sections; any such table in a DoR context is a P1 violation — DoR must always be a numbered paragraph list with confidence indicator and proceed gate). This check runs automatically during every `/audit fix` Stage 6 meta-audit.

**VERIFICATION:**
After all edits, run `grep -rn "cortex_sample_tool\|cortex_validate_compliance\|cortex_load_core_rules\|cortex_query_governance\|cortex_check_dependency_drift\|cortex_onboard_repository_v3\|cortex_audit_remediation_plan\|cortex_capture_metrics\|25 tools\|25 MCP" .github/` to verify zero stale references remain in prompts and agents. Run `grep -rn "22 rules" .github/agents/` to verify governance rule count drift is eliminated. Run `python3 -c "from cortex.orchestrators.support.upgrade_orchestrator import UpgradeOrchestrator; o = UpgradeOrchestrator(); print(hasattr(o, 'check_upstream_and_merge'))"` to verify the upgrade method is wired. Run `python3 scripts/run_tests.py golden` and `python3 scripts/run_tests.py smoke` to confirm zero regressions. Verify MCP server starts with exactly 24 tools via `python3 -m cortex.mcp`. Verify `upgrade-manifest.json` structure is writable at `.cortex-runtime/traces/`. All output inline per CORE-002 — no report files created.

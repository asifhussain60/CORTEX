# CORTEX SELF-IMPROVEMENT PLAN - SDLC GOVERNANCE & ORCHESTRATION GAPS
# Version: 1.0 | Created: 2026-02-04 | Scope: Complete SDLC Review
# Authority: cortex-architect.prompt.md v13.0 | Status: PLANNED
# Source: sesh01-tdd-break.md digest analysis

---
metadata:
  phase: "SDLC-001"
  title: "CORTEX Self-Improvement - SDLC Governance & Orchestration Enhancement"
  version: "1.0"
  status: "PLANNED"
  author: "Asif Hussain"
  created: "2026-02-04"
  priority: "P0 Critical"
  estimated_effort: "3-4 weeks"
  source: "Chat session digest: sesh01-tdd-break.md (Phase 21 failures analysis)"
  
  executive_summary: |
    Phase 21 implementation revealed systematic SDLC gaps where CORTEX's own
    governance was bypassed, leading to 4+ hours of debugging and schema
    misalignments. This plan addresses the root causes by enhancing the
    PlanningOrchestrator and InteractionOrchestrator with code-level intelligence.
    
    Key Insight: CORTEX can analyze repositories but cannot properly plan its
    own feature implementations. The orchestrators lack:
    1. Code-level planning (stops at high-level phases)
    2. Cross-layer coherence validation (Python ↔ JavaScript alignment)
    3. Post-phase review with automated verification
    4. Integration planning at design time (not discovery time)
  
  governance:
    - "CORE-008: TDD-first (tests BEFORE code)"
    - "CORE-027: Audit trail (AC_START → AC_COMPLETE)"
    - "CORE-029: Response header mandatory"
    - "CORE-030: Implementation Truth"
    - "CORE-035: Single canonical implementation"
    - "MCP-FIRST: All functionality via MCP tools"

---
# ============================================================================
# ROOT CAUSE ANALYSIS (FROM DIGEST)
# ============================================================================

root_causes:
  rc1_no_planning_phase:
    title: "No Planning Phase in CORTEX Workflow"
    evidence:
      - "Phase 21 jumped from request to implementation without plan generation"
      - "PlanningOrchestrator never invoked (0 calls in 3846-line session)"
      - "MasterOrchestrator routes IMPLEMENT directly to TDDOrchestrator"
    impact: "HIGH - Features implemented without coherent cross-layer design"
    resolution: "Add mandatory planning phase between intent classification and execution"
    
  rc2_shallow_planning_depth:
    title: "Planning Stops at Phase Level, Not Code Level"
    evidence:
      - "EnhancedPlanningOrchestrator creates phases like 'Implement Data Layer'"
      - "No breakdown to: which files, which functions, which interfaces"
      - "No cross-layer contract specification (Python enums ↔ JavaScript)"
    impact: "HIGH - Schema misalignments discovered at runtime, not design time"
    resolution: "Add code-level planning that generates file/function/interface specs"
    
  rc3_no_cross_layer_validation:
    title: "No Cross-Layer Coherence Checking"
    evidence:
      - "Python SeverityLevel vs JavaScript Severity mismatch"
      - "Python 'type' vs JavaScript 'category' field names"
      - "No contract tests until manually added post-failure"
    impact: "HIGH - 60% of bugs were layer alignment issues"
    resolution: "Add cross-layer contract validation to planning phase"
    
  rc4_no_post_phase_review:
    title: "No Post-Phase Review or Verification"
    evidence:
      - "Phases marked 'complete' without verification"
      - "No smoke tests between phases"
      - "Integration bugs discovered 4+ hours into implementation"
    impact: "MEDIUM - Cascading failures from early phases to late phases"
    resolution: "Add automated post-phase verification with checkpoint gates"
    
  rc5_mcp_gateway_bypass:
    title: "Direct Chat Bypassed MCP Gateway"
    evidence:
      - "User typing 'implement X' went to TDDOrchestrator directly"
      - "MCP-GATE rule not enforced by Copilot"
      - "No audit trail for implementation decisions"
    impact: "CRITICAL - All CORTEX governance was bypassed"
    resolution: "4-layer defense (pre-commit + MCP gate + CI/CD + contract tests)"
    status: "✅ IMPLEMENTED in TDD enforcement commit"

  rc6_no_intelligent_orchestrator_switching:
    title: "MasterOrchestrator Uses Static Routing"
    evidence:
      - "IMPLEMENT intent → TDDOrchestrator (always, no complexity check)"
      - "PlanningOrchestrator never auto-invoked"
      - "No capability-based orchestrator selection"
    impact: "HIGH - Planning bypassed even when needed"
    resolution: "Event-driven orchestrator mesh with capability discovery"
    
  rc7_no_final_holistic_review:
    title: "No Final Git-Based Implementation Verification"
    evidence:
      - "Completion reported after last phase, no holistic check"
      - "Git commits not analyzed for coherence"
      - "No verification that plan was correctly implemented"
    impact: "MEDIUM - Drift between plan and implementation undetected"
    resolution: "Mandatory CORTEX Review Phase using git lens + git history"

---
# ============================================================================
# ARCHITECTURAL ENHANCEMENT: EVENT-DRIVEN ORCHESTRATOR MESH
# ============================================================================

event_driven_architecture:
  overview: |
    Replace direct orchestrator coupling with an event-driven mesh where:
    1. Orchestrators advertise capabilities via event subscriptions
    2. InteractionOrchestrator emits classified events
    3. Relevant orchestrators react based on subscriptions
    4. MasterOrchestrator observes all events for audit + coordination
    
  benefits:
    - "Decoupled: Orchestrators don't import each other"
    - "Extensible: Add new orchestrators via subscriptions, no code changes"
    - "Testable: Each orchestrator testable in isolation"
    - "Async-ready: Events can be processed in parallel"
    - "Observable: Full event stream for debugging"
    
  event_bus:
    location: "cortex/infrastructure/orchestrator_event_bus.py"
    interface: |
      class OrchestratorEventBus:
          def publish(self, event: OrchestratorEvent) -> None
          def subscribe(self, event_type: str, handler: Callable) -> None
          def unsubscribe(self, event_type: str, handler: Callable) -> None
          def get_event_history(self, since: datetime) -> List[OrchestratorEvent]
          
  event_types:
    request_events:
      - name: "REQUEST_RECEIVED"
        emitter: "InteractionOrchestrator"
        data: "{user_request, session_id, timestamp}"
        subscribers: ["MasterOrchestrator (observe)"]
        
      - name: "INTENT_CLASSIFIED"
        emitter: "IntentRouter"
        data: "{intent, confidence, complexity, target_modules}"
        subscribers: ["MasterOrchestrator (observe)", "PlanningOrchestrator (conditional)"]
        
      - name: "PLANNING_REQUIRED"
        emitter: "InteractionOrchestrator"
        data: "{task_spec, complexity_level, layers_involved}"
        subscribers: ["PlanningOrchestrator"]
        trigger_condition: "complexity >= SIMPLE AND intent == IMPLEMENT"
        
    planning_events:
      - name: "PLAN_GENERATED"
        emitter: "PlanningOrchestrator"
        data: "{code_level_plan, coherence_report, estimated_effort}"
        subscribers: ["InteractionOrchestrator", "TDDOrchestrator"]
        
      - name: "PLAN_APPROVED"
        emitter: "InteractionOrchestrator"
        data: "{plan_id, user_approval_timestamp}"
        subscribers: ["TDDOrchestrator", "MasterOrchestrator (observe)"]
        
    execution_events:
      - name: "PHASE_STARTED"
        emitter: "TDDOrchestrator"
        data: "{phase_id, phase_spec, plan_id}"
        subscribers: ["MasterOrchestrator (observe)", "ReviewOrchestrator"]
        
      - name: "PHASE_COMPLETE"
        emitter: "TDDOrchestrator"
        data: "{phase_id, artifacts_created, tests_passed}"
        subscribers: ["ReviewOrchestrator", "PlanningOrchestrator"]
        
      - name: "PHASE_REVIEW_COMPLETE"
        emitter: "ReviewOrchestrator"
        data: "{phase_id, review_result, issues_found}"
        subscribers: ["TDDOrchestrator (gate next phase)", "MasterOrchestrator (observe)"]
        
    completion_events:
      - name: "IMPLEMENTATION_COMPLETE"
        emitter: "TDDOrchestrator"
        data: "{task_id, all_phases_complete, total_artifacts}"
        subscribers: ["ReviewOrchestrator (trigger final review)"]
        
      - name: "FINAL_REVIEW_STARTED"
        emitter: "ReviewOrchestrator"
        data: "{task_id, review_scope}"
        subscribers: ["MasterOrchestrator (observe)"]
        
      - name: "FINAL_REVIEW_COMPLETE"
        emitter: "ReviewOrchestrator"
        data: "{task_id, holistic_review_result, plan_implementation_score}"
        subscribers: ["InteractionOrchestrator (report to user)"]
        
  complexity_gating:
    purpose: "Automatically determine when planning is needed"
    location: "cortex/orchestrators/core/complexity_classifier.py"
    levels:
      TRIVIAL:
        criteria: "LOC impact < 5, single file, no new functions"
        planning_required: false
        examples: ["typo fix", "comment update", "log message change"]
        
      SIMPLE:
        criteria: "LOC impact 5-50, single module, ≤3 new functions"
        planning_required: true
        planning_depth: "lightweight (file specs only)"
        examples: ["add helper function", "extend existing class"]
        
      MODERATE:
        criteria: "LOC impact 50-200, single layer, new class/module"
        planning_required: true
        planning_depth: "standard (file + function specs)"
        examples: ["new orchestrator", "new MCP tool"]
        
      COMPLEX:
        criteria: "LOC impact > 200, multiple layers, cross-module"
        planning_required: true
        planning_depth: "full (file + function + interface contracts)"
        examples: ["new feature system", "architecture change"]
        
      CRITICAL:
        criteria: "Breaking changes, security-sensitive, governance-affecting"
        planning_required: true
        planning_depth: "full + mandatory challenge + extended review"
        examples: ["auth system changes", "data model migrations"]
        
    classification_method: |
      def classify_complexity(task: Dict, lens_context: LENSContext) -> ComplexityLevel:
          # Analyze task description
          loc_estimate = estimate_loc_impact(task, lens_context)
          layers_affected = identify_affected_layers(task, lens_context)
          modules_touched = count_modules(task, lens_context)
          security_sensitive = check_security_sensitivity(task)
          
          if security_sensitive or is_governance_affecting(task):
              return ComplexityLevel.CRITICAL
          elif len(layers_affected) > 1 or loc_estimate > 200:
              return ComplexityLevel.COMPLEX
          elif modules_touched > 1 or loc_estimate > 50:
              return ComplexityLevel.MODERATE
          elif loc_estimate > 5:
              return ComplexityLevel.SIMPLE
          else:
              return ComplexityLevel.TRIVIAL

---
# ============================================================================
# MANDATORY CORTEX REVIEW PHASE
# ============================================================================

cortex_review_phase:
  overview: |
    Every implementation MUST have a final CORTEX Review Phase that:
    1. Uses git lens to analyze all commits holistically
    2. Compares implementation against the original plan
    3. Verifies cross-layer coherence was maintained
    4. Generates implementation fidelity score
    5. Only reports success after review passes
    
  review_orchestrator:
    name: "ReviewOrchestrator"
    location: "cortex/orchestrators/core/review_orchestrator.py"
    purpose: "Holistic implementation verification using LENS + Git"
    
    capabilities:
      git_based_review:
        title: "Git History Analysis"
        methods:
          - name: "analyze_implementation_commits"
            signature: "(task_id: str, plan: CodeLevelPlan) -> CommitAnalysis"
            description: "Analyze all commits related to task implementation"
            data_sources:
              - "GitHistoryAnalyzer - commit messages, authors, timestamps"
              - "ASTAnalyzer - code structure changes per commit"
              - "DiffAnalyzer - line-level changes"
            analysis_points:
              - "Commit message quality (descriptive, references task)"
              - "Commit atomicity (one logical change per commit)"
              - "Commit sequence (matches plan phases)"
              - "No unrelated changes sneaked in"
              
          - name: "verify_plan_implementation"
            signature: "(plan: CodeLevelPlan, commits: CommitAnalysis) -> PlanFidelityReport"
            description: "Compare implemented code against plan specifications"
            checks:
              - "All FileSpecs were created/modified as planned"
              - "All FunctionSpecs have matching implementations"
              - "All InterfaceContracts are satisfied"
              - "No extra files created outside plan"
              - "No missing files from plan"
            output: |
              PlanFidelityReport:
                score: 0-100 (100 = perfect match)
                missing_specs: List[Spec] - Planned but not implemented
                extra_artifacts: List[str] - Implemented but not planned
                deviations: List[Deviation] - Implemented differently than planned
                
          - name: "verify_cross_layer_coherence_final"
            signature: "(task_id: str) -> CoherenceReport"
            description: "Final verification that all layers work together"
            verification_steps:
              - "Re-run all contract tests"
              - "Check enum alignments via AST"
              - "Verify field name mappings"
              - "Run integration smoke tests"
              
      holistic_review:
        title: "Holistic Implementation Review"
        methods:
          - name: "execute_final_review"
            signature: "(task_id: str, plan: CodeLevelPlan) -> FinalReviewResult"
            description: "Complete holistic review before reporting success"
            steps:
              1: "Collect all commits for task (git log --grep=task_id)"
              2: "Analyze commit sequence against plan phases"
              3: "Run plan fidelity check"
              4: "Run cross-layer coherence verification"
              5: "Run LENS analysis on final codebase"
              6: "Generate review report with recommendations"
              7: "Determine PASS/FAIL/CONDITIONAL_PASS"
            
          - name: "generate_implementation_report"
            signature: "(review: FinalReviewResult) -> ImplementationReport"
            description: "Generate human-readable implementation report"
            sections:
              - "Executive Summary (pass/fail, score)"
              - "Plan Adherence (what was planned vs implemented)"
              - "Quality Metrics (test coverage, complexity)"
              - "Security Status (vulnerabilities introduced?)"
              - "Recommendations (follow-up actions)"
              
    review_criteria:
      must_pass:
        - "All planned artifacts exist"
        - "All tests pass (unit + integration + contract)"
        - "No HIGH/CRITICAL security vulnerabilities introduced"
        - "Plan fidelity score >= 80%"
        
      should_pass:
        - "Commit messages reference task/phase"
        - "No extra unplanned files"
        - "LENS complexity within acceptable range"
        
      nice_to_have:
        - "Documentation updated"
        - "Changelog entry added"
        
    review_outcomes:
      PASS:
        criteria: "All must_pass + majority of should_pass"
        action: "Report success to user"
        
      CONDITIONAL_PASS:
        criteria: "All must_pass, some should_pass failed"
        action: "Report success with recommendations"
        
      FAIL:
        criteria: "Any must_pass failed"
        action: "Block completion, report issues, require fixes"
        
    async_behavior:
      description: |
        Review runs asynchronously to not block user:
        1. Report "Implementation complete, review in progress..."
        2. Run full review in background
        3. Emit FINAL_REVIEW_COMPLETE event
        4. Update user with final status (badge/notification)
      timeout: "60 seconds max"
      fallback: "If timeout, report CONDITIONAL_PASS with warning"
      
  integration_with_interaction:
    flow: |
      IMPLEMENTATION_COMPLETE event
          ↓
      ReviewOrchestrator.execute_final_review()
          ↓
      FINAL_REVIEW_COMPLETE event
          ↓
      InteractionOrchestrator receives result
          ↓
      Report to user:
        - PASS: "✅ Implementation complete and verified"
        - CONDITIONAL_PASS: "✅ Implementation complete (see recommendations)"
        - FAIL: "❌ Implementation has issues (see report)"

---
# ============================================================================
# ORCHESTRATOR ENHANCEMENT SPECIFICATIONS
# ============================================================================

orchestrator_enhancements:
  # =========================================================================
  # PLANNING ORCHESTRATOR ENHANCEMENT
  # =========================================================================
  enhanced_planning_orchestrator:
    current_state:
      file: "cortex/orchestrators/domain/enhanced_planning_orchestrator.py"
      version: "3.0.0"
      capabilities:
        - "YAML-driven phase templates (AC-DOMAIN-PLAN-001)"
        - "Topological phase sorting (AC-DOMAIN-PLAN-003)"
        - "ML-based effort estimation (AC-DOMAIN-PLAN-009)"
        - "Resource constraint modeling (AC-DOMAIN-PLAN-011)"
        - "Risk assessment matrix (AC-DOMAIN-PLAN-012)"
      gaps:
        - "❌ No code-level planning (stops at phase level)"
        - "❌ No cross-layer contract specification"
        - "❌ No file/function/interface breakdown"
        - "❌ No post-phase verification hooks"
        - "❌ No integration coherence checking"
        
    target_state:
      version: "4.0.0"
      new_capabilities:
        code_level_planning:
          title: "Code-Level Planning Intelligence"
          description: |
            Plan down to the code level WITHOUT generating code:
            - File specifications (what files to create/modify)
            - Function signatures (inputs, outputs, types)
            - Interface contracts (Python ↔ JavaScript alignment)
            - Data model schemas (with cross-layer validation)
            - Test requirements (what tests to write first)
          methods:
            - name: "generate_code_level_plan"
              signature: "(task: Dict, lens_context: LENSContext) -> CodeLevelPlan"
              description: "Generate detailed code-level plan from high-level task"
              returns:
                file_specs: "List[FileSpec] - Files to create/modify"
                function_specs: "List[FunctionSpec] - Function signatures"
                interface_contracts: "List[InterfaceContract] - Cross-layer contracts"
                test_specs: "List[TestSpec] - Required tests (TDD-first)"
                
            - name: "validate_cross_layer_coherence"
              signature: "(plan: CodeLevelPlan) -> CoherenceReport"
              description: "Validate that all layers will work together"
              checks:
                - "Enum value alignment (Python ↔ JavaScript)"
                - "Field name consistency (snake_case ↔ camelCase mapping)"
                - "Type compatibility (Pydantic ↔ JSON Schema)"
                - "Import path correctness (relative imports work)"
                
            - name: "generate_integration_contracts"
              signature: "(plan: CodeLevelPlan) -> List[IntegrationContract]"
              description: "Generate testable integration contracts"
              output: "Contract tests that can be run to verify alignment"
              
        post_phase_review:
          title: "Automated Post-Phase Review"
          description: |
            After each phase completion, automatically:
            1. Run phase-specific verification tests
            2. Check cross-layer contracts still pass
            3. Generate phase completion report
            4. Gate next phase on verification pass
          methods:
            - name: "execute_post_phase_review"
              signature: "(phase_id: str) -> PhaseReviewResult"
              description: "Run automated verification after phase completion"
              steps:
                - "Run unit tests for phase artifacts"
                - "Run integration contracts"
                - "Check LENS analysis for new issues"
                - "Generate review summary"
                
            - name: "gate_next_phase"
              signature: "(current_phase: str, review: PhaseReviewResult) -> GateDecision"
              description: "Decide if next phase can start based on review"
              conditions:
                - "All phase tests pass"
                - "No new HIGH/CRITICAL issues from LENS"
                - "Cross-layer contracts still valid"
                
        comprehensive_logging:
          title: "Enhanced Audit Logging"
          description: |
            Log all planning decisions with full context:
            - LENS context used for decisions
            - Alternative approaches considered
            - Risk assessments at each level
            - Traceability from requirement → code spec
          log_points:
            - "PLAN_START: Task decomposition begins"
            - "CODE_SPEC_GENERATED: Each file/function spec"
            - "COHERENCE_CHECK: Cross-layer validation"
            - "PHASE_GATE: Transition decisions"
            - "PLAN_COMPLETE: Final plan summary"
            
    data_models:
      FileSpec:
        path: "str - Relative file path"
        action: "Enum[CREATE, MODIFY, DELETE]"
        purpose: "str - What this file does"
        dependencies: "List[str] - Files this depends on"
        estimated_loc: "int - Estimated lines of code"
        language: "str - Programming language"
        
      FunctionSpec:
        file_path: "str - Which file contains this"
        name: "str - Function/method name"
        signature: "str - Full signature with types"
        purpose: "str - What this function does"
        inputs: "List[ParameterSpec] - Input parameters"
        outputs: "ReturnSpec - Return type and meaning"
        test_cases: "List[TestCase] - Example test cases"
        
      InterfaceContract:
        contract_id: "str - Unique identifier"
        python_side: "PythonSpec - Python implementation"
        javascript_side: "JavaScriptSpec - JavaScript implementation"
        validation_tests: "List[str] - Tests to verify alignment"
        field_mappings: "Dict[str, str] - Python name → JS name"
        
      CodeLevelPlan:
        task_id: "str - Parent task"
        file_specs: "List[FileSpec]"
        function_specs: "List[FunctionSpec]"
        interface_contracts: "List[InterfaceContract]"
        test_specs: "List[TestSpec]"
        execution_order: "List[str] - Topologically sorted"
        estimated_effort: "EffortEstimate"
        risk_assessment: "RiskMatrix"
        
    mcp_tools:
      - name: "cortex_generate_code_plan"
        description: "Generate code-level implementation plan (no code generation)"
        parameters:
          task_description: "str - What to implement"
          target_modules: "List[str] - Where to implement"
          lens_context: "Optional[str] - Pre-gathered LENS context"
        returns: "CodeLevelPlan - Detailed implementation plan"
        
      - name: "cortex_validate_plan_coherence"
        description: "Validate cross-layer coherence of a plan"
        parameters:
          plan: "CodeLevelPlan - Plan to validate"
        returns: "CoherenceReport - Validation results"
        
      - name: "cortex_execute_phase_review"
        description: "Run post-phase verification and gating"
        parameters:
          phase_id: "str - Phase to review"
          artifacts: "List[str] - Files created in phase"
        returns: "PhaseReviewResult - Pass/fail with details"

  # =========================================================================
  # INTERACTION ORCHESTRATOR ENHANCEMENT
  # =========================================================================
  enhanced_interaction_orchestrator:
    current_state:
      file: "cortex/orchestrators/core/interaction_orchestrator.py"
      version: "Current"
      capabilities:
        - "Challenge system integration (AC-CHALLENGE-SYSTEM-002)"
        - "LENS context building on every turn"
        - "Security threat assessment (Phase 8.3)"
        - "Communication pattern enforcement"
      gaps:
        - "❌ No planning phase invocation for IMPLEMENT intents"
        - "❌ Challenge system doesn't check for cross-layer alignment"
        - "❌ No cohesive design coordination across layers"
        - "❌ Single-layer focus (doesn't orchestrate multi-layer)"
        
    target_state:
      version: "Enhanced with Planning Integration"
      new_capabilities:
        planning_integration:
          title: "Mandatory Planning for IMPLEMENT Intents"
          description: |
            Before any IMPLEMENT intent proceeds to execution:
            1. Invoke PlanningOrchestrator for code-level plan
            2. Present plan to user for approval (DoR includes plan)
            3. Validate cross-layer coherence
            4. Only then proceed to TDDOrchestrator
          flow: |
            User Request (IMPLEMENT)
                ↓
            LENS Context Build
                ↓
            Challenge Generation (if disagreement)
                ↓
            Planning Phase (NEW) ← PlanningOrchestrator
                ├─ Generate CodeLevelPlan
                ├─ Validate Coherence
                └─ Present Plan in DoR
                ↓
            User Approval (DoR with Plan)
                ↓
            TDD Execution (per plan)
                ↓
            Post-Phase Review (after each phase)
                
        multi_layer_coordination:
          title: "Cross-Layer Design Coordination"
          description: |
            When a feature touches multiple layers:
            - Backend (Python): API, data models, orchestrators
            - Frontend (JavaScript): UI components, data binding
            - Data (JSON/SQL): Schema, migrations, contracts
            
            Ensure all layers are designed together:
            - Shared interface contracts
            - Field name mapping
            - Type alignment
            - Test coverage per layer
          methods:
            - name: "coordinate_multi_layer_design"
              signature: "(task: Dict, layers: List[str]) -> MultiLayerDesign"
              description: "Coordinate design across identified layers"
              
            - name: "validate_layer_contracts"
              signature: "(design: MultiLayerDesign) -> ContractValidation"
              description: "Validate all layers can work together"
              
        enhanced_challenge:
          title: "Enhanced Challenge with Coherence Checks"
          description: |
            Extend challenge generation to include:
            - Cross-layer alignment challenges
            - Integration risk challenges
            - Schema migration challenges
          new_challenge_types:
            - "LAYER_MISALIGNMENT: Detected potential cross-layer issues"
            - "INTEGRATION_RISK: This change affects multiple systems"
            - "SCHEMA_DRIFT: Schema changes require migration"
            
    mcp_tools:
      - name: "cortex_coordinate_layers"
        description: "Coordinate multi-layer feature design"
        parameters:
          task: "Dict - Task specification"
          layers: "List[str] - Layers involved (backend, frontend, data)"
        returns: "MultiLayerDesign - Coordinated design"
        
      - name: "cortex_validate_integration"
        description: "Validate that all layers will integrate correctly"
        parameters:
          design: "MultiLayerDesign - Design to validate"
        returns: "IntegrationValidation - Pass/fail with details"

---
# ============================================================================
# IMPLEMENTATION PHASES (Updated with Event-Driven Architecture + Review)
# ============================================================================

implementation_phases:
  phase_0_foundation:
    title: "Foundation - Data Models & Interfaces"
    duration: "2 days"
    immediate_fixes:
      - file: "cortex/orchestrators/domain/enhanced_planning_orchestrator.py"
        change: "Re-export RiskLevel from canonical_enums for backward compatibility"
        status: "✅ FIXED"
      - file: "tests/contracts/"
        change: "Create directory structure for contract tests"
        priority: "P0"
    deliverables:
      - "cortex/models/planning_models.py - CodeLevelPlan, FileSpec, FunctionSpec, InterfaceContract"
      - "cortex/models/coherence_models.py - CoherenceReport, ContractValidation"
      - "cortex/models/review_models.py - PhaseReviewResult, GateDecision, FinalReviewResult"
      - "cortex/models/event_models.py - OrchestratorEvent, EventType, EventPayload (NEW)"
    tests:
      - "tests/unit/models/test_planning_models.py"
      - "tests/unit/models/test_coherence_models.py"
      - "tests/unit/models/test_event_models.py"
    tdd_approach: "Define all data classes with Pydantic, full test coverage"
    
  phase_1_event_bus:
    title: "Orchestrator Event Bus Infrastructure (EXTEND)"
    duration: "3 days"
    dependencies: ["phase_0_foundation"]
    approach: |
      EXTEND existing cortex/core/event_bus.py rather than creating entirely new infrastructure.
      This reduces risk and preserves existing feature integrations.
      Existing EventBus has basic publish/subscribe - add orchestrator-specific features.
    existing_foundation:
      file: "cortex/core/event_bus.py"
      current_capabilities: ["publish", "subscribe", "feature_registry_notifications"]
      gaps_to_fill: ["event_history", "correlation_ids", "dead_letter_queue", "async_handlers"]
    deliverables:
      - "cortex/infrastructure/orchestrator_event_bus.py (extends cortex/core/event_bus.py)"
        description: "Event-driven communication backbone for orchestrators"
        key_methods:
          - "publish(event: OrchestratorEvent) - Emit event to subscribers"
          - "subscribe(event_type: str, handler: Callable) - Register handler"
          - "unsubscribe(event_type: str, handler: Callable) - Remove handler"
          - "get_event_history(since: datetime) - Retrieve past events"
          - "replay_events(from_event_id: str) - Replay for recovery"
        features:
          - "Async event handling (non-blocking)"
          - "Event persistence for audit trail"
          - "Dead letter queue for failed handlers"
          - "Event correlation via task_id"
    tests:
      - "tests/unit/infrastructure/test_orchestrator_event_bus.py (20+ tests)"
    event_types_implemented:
      - "REQUEST_RECEIVED, INTENT_CLASSIFIED, PLANNING_REQUIRED"
      - "PLAN_GENERATED, PLAN_APPROVED"
      - "PHASE_STARTED, PHASE_COMPLETE, PHASE_REVIEW_COMPLETE"
      - "IMPLEMENTATION_COMPLETE, FINAL_REVIEW_STARTED, FINAL_REVIEW_COMPLETE"
      
  phase_2_complexity_classifier:
    title: "Intelligent Complexity Classification (EXTEND)"
    duration: "2 days"
    dependencies: ["phase_1_event_bus"]
    approach: |
      EXTEND existing RequestComplexityClassifier in conversation_protocol.py.
      Current implementation is keyword-based (SIMPLE/MEDIUM/COMPLEX/CRITICAL).
      Add LOC impact estimation and layer detection while preserving backward compatibility.
    existing_foundation:
      file: "cortex/brain/core/orchestrator/conversation_protocol.py"
      class: "RequestComplexityClassifier"
      current_capabilities: ["keyword_analysis", "complexity_levels"]
      gaps_to_fill: ["loc_impact_estimation", "layer_detection", "security_sensitivity"]
    deliverables:
      - "cortex/orchestrators/core/complexity_classifier.py (extends RequestComplexityClassifier)"
        description: "Automatically determine planning depth based on task complexity"
        key_methods:
          - "classify_complexity(task, lens_context) -> ComplexityLevel"
          - "estimate_loc_impact(task, lens_context) -> int"
          - "identify_affected_layers(task, lens_context) -> List[str]"
          - "check_security_sensitivity(task) -> bool"
        complexity_levels:
          - "TRIVIAL: Skip planning"
          - "SIMPLE: Lightweight planning (file specs only)"
          - "MODERATE: Standard planning (file + function specs)"
          - "COMPLEX: Full planning (contracts + review)"
          - "CRITICAL: Full + extended challenge + review"
    tests:
      - "tests/unit/orchestrators/core/test_complexity_classifier.py (25+ tests)"
    integration: "Subscribed to INTENT_CLASSIFIED event"
      
  phase_3_code_level_planning:
    title: "Code-Level Planning Intelligence"
    duration: "4 days"
    dependencies: ["phase_2_complexity_classifier"]
    deliverables:
      - "cortex/orchestrators/domain/code_level_planner.py"
        description: "Intelligence for generating file/function/interface specs"
        key_methods:
          - "analyze_task_scope() - Determine what files/functions needed"
          - "generate_file_specs() - Create FileSpec for each artifact"
          - "generate_function_specs() - Create FunctionSpec with signatures"
          - "generate_interface_contracts() - Cross-layer contracts"
    tests:
      - "tests/unit/orchestrators/domain/test_code_level_planner.py (25+ tests)"
      - "tests/integration/test_code_level_planning_e2e.py"
    lens_integration: "Use AST analyzer to understand existing code structure"
    event_subscriptions:
      - "PLANNING_REQUIRED → generate_code_level_plan()"
    event_emissions:
      - "PLAN_GENERATED"
    
  phase_4_coherence_validation:
    title: "Cross-Layer Coherence Validation"
    duration: "3 days"
    dependencies: ["phase_3_code_level_planning"]
    deliverables:
      - "cortex/orchestrators/domain/coherence_validator.py"
        description: "Validates cross-layer alignment"
        key_methods:
          - "validate_enum_alignment() - Python enums ↔ JavaScript values"
          - "validate_field_naming() - snake_case ↔ camelCase mapping"
          - "validate_type_compatibility() - Pydantic ↔ JSON Schema"
          - "generate_contract_tests() - Testable integration contracts"
    tests:
      - "tests/unit/orchestrators/domain/test_coherence_validator.py (20+ tests)"
      - "tests/integration/test_cross_layer_contracts.py"
    example_output: |
      CoherenceReport:
        status: FAIL
        issues:
          - type: ENUM_MISMATCH
            python: "SeverityLevel.HIGH"
            javascript: "Severity.high"
            recommendation: "Add mapping"
            
  phase_5_review_orchestrator:
    title: "Review Orchestrator (NEW)"
    duration: "4 days"
    dependencies: ["phase_4_coherence_validation"]
    deliverables:
      - "cortex/orchestrators/core/review_orchestrator.py"
        description: "Holistic implementation verification using LENS + Git"
        key_methods:
          - "analyze_implementation_commits(task_id, plan) -> CommitAnalysis"
          - "verify_plan_implementation(plan, commits) -> PlanFidelityReport"
          - "verify_cross_layer_coherence_final(task_id) -> CoherenceReport"
          - "execute_final_review(task_id, plan) -> FinalReviewResult"
          - "execute_post_phase_review(phase_id) -> PhaseReviewResult"
          - "gate_next_phase(phase_id, review) -> GateDecision"
        features:
          - "Git-based commit analysis (messages, sequence, atomicity)"
          - "Plan fidelity scoring (0-100)"
          - "Async execution (non-blocking)"
          - "Unified review: post-phase + final in one orchestrator"
    tests:
      - "tests/unit/orchestrators/core/test_review_orchestrator.py (30+ tests)"
      - "tests/integration/test_review_workflow_e2e.py"
    event_subscriptions:
      - "PHASE_COMPLETE → execute_post_phase_review()"
      - "IMPLEMENTATION_COMPLETE → execute_final_review()"
    event_emissions:
      - "PHASE_REVIEW_COMPLETE"
      - "FINAL_REVIEW_COMPLETE"
    review_criteria:
      must_pass: ["All planned artifacts exist", "All tests pass", "No HIGH security issues", "Plan fidelity >= 80%"]
      
  phase_6_interaction_enhancement:
    title: "Interaction Orchestrator Enhancement"
    duration: "3 days"
    dependencies: ["phase_5_review_orchestrator"]
    deliverables:
      - "Update cortex/orchestrators/core/interaction_orchestrator.py"
        changes:
          - "Subscribe to event bus (PLAN_GENERATED, FINAL_REVIEW_COMPLETE)"
          - "Emit PLANNING_REQUIRED event (instead of direct PlanningOrchestrator call)"
          - "Integrate with ComplexityClassifier for planning depth"
          - "Enhance DoR to include CodeLevelPlan summary"
          - "Add multi_layer_coordination capability"
          - "Extend challenge types for coherence issues"
          - "Report final review result to user"
    tests:
      - "tests/unit/orchestrators/core/test_interaction_orchestrator_enhanced.py (20+ tests)"
      - "tests/integration/test_planning_workflow_e2e.py"
    event_emissions:
      - "REQUEST_RECEIVED"
      - "PLANNING_REQUIRED (when complexity >= SIMPLE)"
    event_subscriptions:
      - "PLAN_GENERATED → include plan in DoR"
      - "FINAL_REVIEW_COMPLETE → report to user"
      
  phase_7_mcp_tools:
    title: "MCP Tool Exposure"
    duration: "2 days"
    dependencies: ["phase_6_interaction_enhancement"]
    deliverables:
      - "cortex/mcp/tools/planning_tools.py"
        tools:
          - "cortex_generate_code_plan"
          - "cortex_validate_plan_coherence"
          - "cortex_execute_phase_review"
          - "cortex_coordinate_layers"
          - "cortex_validate_integration"
          - "cortex_classify_complexity (NEW)"
          - "cortex_execute_final_review (NEW)"
    tests:
      - "tests/unit/mcp/tools/test_planning_tools.py (25+ tests)"
      - "tests/integration/test_mcp_planning_e2e.py"
      
  phase_8_prompt_agent_updates:
    title: "Prompt & Agent Updates"
    duration: "2 days"
    dependencies: ["phase_7_mcp_tools"]
    deliverables:
      - "Update .github/prompts/cortex-architect.prompt.md"
        changes:
          - "Add Complexity Classification documentation"
          - "Add Planning Phase documentation"
          - "Include CodeLevelPlan in DoR template"
          - "Add coherence validation step"
          - "Update DESIGN mode flow"
          - "Add MANDATORY CORTEX Review Phase"
      - "Update .github/agents/core/cortex-architect.md"
        changes:
          - "Add PlanningOrchestrator routing via events"
          - "Include post-phase review in workflow"
          - "Document complexity gating"
      - "Update .github/agents/core/cortex-designer.md"
        changes:
          - "Add code-level planning step"
          - "Include coherence validation"
          - "Add review phase"
    verification: "Run prompt coherence check to ensure no conflicts"
            python: "SeverityLevel.HIGH"
            javascript: "Severity.high"
            recommendation: "Add mapping: SeverityLevel.HIGH → 'high'"
          - type: FIELD_NAME_MISMATCH
            python: "type"
            javascript: "category"
            recommendation: "Rename JavaScript field to 'type' or add alias"
            
  phase_3_post_phase_review:
    title: "Automated Post-Phase Review"
    duration: "3 days"
    dependencies: ["phase_2_coherence_validation"]
    deliverables:
      - "cortex/orchestrators/domain/phase_reviewer.py"
        description: "Automated verification after each phase"
        key_methods:
          - "collect_phase_artifacts() - Gather files created in phase"
          - "run_phase_tests() - Execute phase-specific tests"
          - "check_lens_issues() - Run LENS analysis for new issues"
          - "validate_contracts() - Re-run cross-layer contracts"
          - "generate_review_report() - Structured report"
          - "gate_next_phase() - Allow/block next phase"
    tests:
      - "tests/unit/orchestrators/domain/test_phase_reviewer.py"
    workflow_integration: "Hooks into TDDOrchestrator completion events"
    
  phase_4_interaction_enhancement:
    title: "Interaction Orchestrator Enhancement"
    duration: "3 days"
    dependencies: ["phase_3_post_phase_review"]
    deliverables:
      - "Update cortex/orchestrators/core/interaction_orchestrator.py"
        changes:
          - "Add planning_phase_enabled flag (default True)"
          - "Integrate with PlanningOrchestrator for IMPLEMENT intents"
          - "Enhance DoR to include CodeLevelPlan summary"
          - "Add multi_layer_coordination capability"
          - "Extend challenge types for coherence issues"
    tests:
      - "tests/unit/orchestrators/core/test_interaction_orchestrator_enhanced.py"
      - "tests/integration/test_planning_workflow_e2e.py"
      
  phase_5_mcp_tools:
    title: "MCP Tool Exposure"
    duration: "2 days"
    dependencies: ["phase_4_interaction_enhancement"]
    deliverables:
      - "cortex/mcp/tools/planning_tools.py"
        tools:
          - "cortex_generate_code_plan"
          - "cortex_validate_plan_coherence"
          - "cortex_execute_phase_review"
          - "cortex_coordinate_layers"
          - "cortex_validate_integration"
    tests:
      - "tests/unit/mcp/tools/test_planning_tools.py"
      - "tests/integration/test_mcp_planning_e2e.py"
      
  phase_6_prompt_agent_updates:
    title: "Prompt & Agent Updates"
    duration: "2 days"
    dependencies: ["phase_5_mcp_tools"]
    deliverables:
      - "Update .github/prompts/cortex-architect.prompt.md"
        changes:
          - "Add Planning Phase documentation"
          - "Include CodeLevelPlan in DoR template"
          - "Add coherence validation step"
          - "Update DESIGN mode flow"
      - "Update .github/agents/core/cortex-architect.md"
        changes:
          - "Add PlanningOrchestrator routing"
          - "Include post-phase review in workflow"
      - "Update .github/agents/core/cortex-designer.md"
        changes:
          - "Add code-level planning step"
          - "Include coherence validation"
    verification: "Run prompt coherence check to ensure no conflicts"

---
# ============================================================================
# TEST STRATEGY (Updated for Event-Driven Architecture)
# ============================================================================

test_strategy:
  unit_tests:
    target_coverage: "85%"
    test_files:
      - "tests/unit/models/test_planning_models.py (20+ tests)"
      - "tests/unit/models/test_coherence_models.py (15+ tests)"
      - "tests/unit/models/test_event_models.py (15+ tests) - NEW"
      - "tests/unit/infrastructure/test_orchestrator_event_bus.py (20+ tests) - NEW"
      - "tests/unit/orchestrators/core/test_complexity_classifier.py (25+ tests) - NEW"
      - "tests/unit/orchestrators/domain/test_code_level_planner.py (25+ tests)"
      - "tests/unit/orchestrators/domain/test_coherence_validator.py (20+ tests)"
      - "tests/unit/orchestrators/core/test_review_orchestrator.py (30+ tests) - NEW"
      - "tests/unit/orchestrators/core/test_interaction_orchestrator_enhanced.py (20+ tests)"
      - "tests/unit/mcp/tools/test_planning_tools.py (25+ tests)"
    total_estimated: "215+ unit tests"
    
  integration_tests:
    test_files:
      - "tests/integration/test_event_bus_orchestrator_integration.py - NEW"
      - "tests/integration/test_code_level_planning_e2e.py"
      - "tests/integration/test_cross_layer_contracts.py"
      - "tests/integration/test_planning_workflow_e2e.py"
      - "tests/integration/test_review_workflow_e2e.py - NEW"
      - "tests/integration/test_mcp_planning_e2e.py"
    total_estimated: "35+ integration tests"
    
  contract_tests:
    purpose: "Validate cross-layer alignment"
    test_files:
      - "tests/contracts/test_python_js_enums.py"
      - "tests/contracts/test_python_js_fields.py"
      - "tests/contracts/test_pydantic_json_schema.py"
    total_estimated: "20+ contract tests"
    
  event_tests:
    purpose: "Validate event-driven flow"
    test_files:
      - "tests/events/test_planning_event_flow.py - NEW"
      - "tests/events/test_review_event_flow.py - NEW"
      - "tests/events/test_event_correlation.py - NEW"
    total_estimated: "30+ event tests"

---
# ============================================================================
# SUCCESS METRICS (Updated)
# ============================================================================

success_metrics:
  implementation_quality:
    - metric: "Test Coverage"
      target: ">= 85%"
      measurement: "pytest --cov"
      
    - metric: "No Schema Misalignments"
      target: "0 cross-layer issues in production"
      measurement: "Contract test suite passes"
      
    - metric: "Planning Phase Adoption"
      target: "100% of IMPLEMENT intents >= SIMPLE complexity use planning"
      measurement: "Audit trail shows PlanningOrchestrator invoked"
      
    - metric: "Final Review Execution"
      target: "100% of implementations have final CORTEX review"
      measurement: "FINAL_REVIEW_COMPLETE event for each task"
      
  developer_experience:
    - metric: "Time to First Working Feature"
      target: "< 2 hours (vs 4+ hours in Phase 21)"
      measurement: "From request to passing E2E tests"
      
    - metric: "Debugging Time Reduction"
      target: "50% reduction in layer alignment debugging"
      measurement: "Compare similar feature implementations"
      
    - metric: "Plan Quality"
      target: "Plans require < 2 iterations to approve"
      measurement: "Track DoR approval rate"
      
    - metric: "Review Latency"
      target: "Final review completes in < 30 seconds"
      measurement: "Time from IMPLEMENTATION_COMPLETE to FINAL_REVIEW_COMPLETE"
      
  governance_compliance:
    - metric: "MCP Gateway Usage"
      target: "100% of IMPLEMENT intents via MCP"
      measurement: "Audit trail analysis"
      
    - metric: "Post-Phase Review Execution"
      target: "100% of phases have review"
      measurement: "PHASE_REVIEW_COMPLETE event for each phase"
      
    - metric: "Coherence Validation"
      target: "All plans pass coherence check"
      measurement: "CoherenceReport.status = PASS"
      
    - metric: "Event Bus Coverage"
      target: "100% of orchestrator communications via events"
      measurement: "No direct orchestrator imports between core orchestrators"

---
# ============================================================================
# ROLLOUT TIMELINE (Updated for 9 Phases)
# ============================================================================

rollout_timeline:
  week_1:
    days_1_2: "Phase 0: Foundation data models (including event models)"
    days_3_5: "Phase 1: Event bus infrastructure"
    
  week_2:
    days_1_2: "Phase 2: Complexity classifier"
    days_3_5: "Phase 3: Code-level planning (start)"
    
  week_3:
    days_1_2: "Phase 3: Code-level planning (complete)"
    days_3_5: "Phase 4: Coherence validation"
    
  week_4:
    days_1_4: "Phase 5: Review orchestrator"
    day_5: "Phase 5: Integration testing"
    
  week_5:
    days_1_3: "Phase 6: Interaction orchestrator enhancement"
    days_4_5: "Phase 7: MCP tools"
    
  week_6:
    days_1_3: "Phase 8: Prompt & agent updates"
    days_4_5: "Final integration testing & documentation"

---
# ============================================================================
# RISK MITIGATION (Updated for Event-Driven Architecture)
# ============================================================================

risks:
  r1_over_engineering:
    description: "Code-level planning may become too detailed"
    probability: 0.3
    impact: 0.5
    mitigation: "Complexity gating (TRIVIAL skips planning), allow user override"
    
  r2_performance_overhead:
    description: "Planning phase adds latency before execution"
    probability: 0.4
    impact: 0.3
    mitigation: "Complexity gating, async event processing, plan caching"
    
  r3_false_positives:
    description: "Coherence validation may flag non-issues"
    probability: 0.5
    impact: 0.4
    mitigation: "Configurable strictness, user override capability, learning from false positives"
    
  r4_adoption_resistance:
    description: "Users may skip planning phase for speed"
    probability: 0.3
    impact: 0.6
    mitigation: "TRIVIAL tasks skip planning, show value through faster completion"
    
  r5_event_bus_complexity:
    description: "Event-driven architecture adds indirection, harder to debug"
    probability: 0.4
    impact: 0.4
    mitigation: "Event history persistence, correlation IDs, event visualization tools"
    
  r6_review_latency:
    description: "Final review adds 30-60s before reporting success"
    probability: 0.6
    impact: 0.3
    mitigation: "Async review (report 'functionally complete', update with review badge)"
    
  r7_event_ordering:
    description: "Events may arrive out of order causing incorrect state"
    probability: 0.3
    impact: 0.5
    mitigation: "Event sequencing via timestamps, idempotent handlers, state machines"

---
# ============================================================================
# DEPENDENCIES (Updated)
# ============================================================================

dependencies:
  internal:
    - "LENS analyzers (ASTAnalyzer, GitHistoryAnalyzer) - for code structure + git review"
    - "TDDOrchestrator - subscribes to PLAN_APPROVED, executes per plan"
    - "IntentRouter - emits INTENT_CLASSIFIED event"
    - "MasterOrchestrator - observes all events for coordination"
    - "OrchestratorEventBus - NEW: communication backbone"
    - "ComplexityClassifier - NEW: determines planning depth"
    - "ReviewOrchestrator - NEW: holistic verification"
    
  external:
    - "Pydantic v2 - for data model validation"
    - "pytest - for test execution"
    - "GitHub Copilot Chat - for user interaction"
    - "asyncio - for async event handling"

---
# ============================================================================
# APPENDIX: EXAMPLE CODE-LEVEL PLAN OUTPUT
# ============================================================================

example_output:
  task: "Add SQLite adapter for dashboard data"
  
  code_level_plan:
    file_specs:
      - path: "cortex/visualization/adapters/sqlite_adapter.py"
        action: "CREATE"
        purpose: "SQLite implementation of DashboardDataAdapter"
        estimated_loc: 150
        language: "Python"
        dependencies: ["cortex/visualization/dashboard_data_adapter.py"]
        
      - path: "company/dashboards/spa/js/data/SQLiteDataLayer.js"
        action: "CREATE"
        purpose: "JavaScript SQLite client using sql.js"
        estimated_loc: 100
        language: "JavaScript"
        dependencies: ["sql.js"]
        
    function_specs:
      - file_path: "cortex/visualization/adapters/sqlite_adapter.py"
        name: "load"
        signature: "def load(self, repo_slug: str) -> DashboardData"
        purpose: "Load dashboard data from SQLite database"
        inputs:
          - name: "repo_slug"
            type: "str"
            description: "Repository identifier"
        outputs:
          type: "DashboardData"
          description: "Parsed dashboard data"
        test_cases:
          - "test_load_existing_repo"
          - "test_load_missing_repo_returns_error"
          
    interface_contracts:
      - contract_id: "severity_enum_alignment"
        python_side:
          file: "cortex/models/dashboard_schema_v3.py"
          type: "SeverityLevel"
          values: ["CRITICAL", "HIGH", "MEDIUM", "LOW", "INFO"]
        javascript_side:
          file: "company/dashboards/spa/js/data/SQLiteDataLayer.js"
          type: "Severity"
          values: ["critical", "high", "medium", "low", "info"]
        validation_tests:
          - "test_severity_enum_alignment"
        field_mappings:
          "CRITICAL": "critical"
          "HIGH": "high"
          "MEDIUM": "medium"
          "LOW": "low"
          "INFO": "info"
          
    test_specs:
      - file_path: "tests/unit/visualization/adapters/test_sqlite_adapter.py"
        test_count: 15
        coverage_target: "90%"
        priority: "Write FIRST (TDD)"
        
      - file_path: "tests/contracts/test_dashboard_layer_alignment.py"
        test_count: 10
        purpose: "Validate Python-JavaScript alignment"
        priority: "Write FIRST (TDD)"
        
    execution_order:
      1: "Create test files (TDD-first)"
      2: "Create Python data models"
      3: "Create Python adapter"
      4: "Create JavaScript data layer"
      5: "Run contract tests"
      6: "Integration testing"
      
    estimated_effort:
      hours: 16
      confidence: 0.8
      breakdown:
        tests: 4
        python: 6
        javascript: 4
        integration: 2
        
    risk_assessment:
      overall_risk: "MEDIUM"
      risks:
        - risk: "sql.js WASM initialization latency"
          probability: 0.6
          impact: 0.4
          mitigation: "Lazy loading, preload on page load"
        - risk: "Enum value case sensitivity"
          probability: 0.4
          impact: 0.3
          mitigation: "Case-insensitive comparison, lowercase normalization"

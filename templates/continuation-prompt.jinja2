# 🔄 Session Continuation Prompt

**Generated:** {{ timestamp }}  
**Plan:** {{ plan_name }}  
**Progress:** {{ completed_phases|length }}/{{ total_phases }} phases ({{ progress_percentage }}%)

---

## 📋 Quick Context

You are resuming execution of the **{{ plan_name }}** plan.

**Completed Phases:**
{% for phase in completed_phases_list %}
- ✅ Phase {{ phase.number }}: {{ phase.name }} ({{ phase.duration }})
{% endfor %}

**Current Phase:** Phase {{ current_phase.number }}: {{ current_phase.name }}

**Next Phase:** Phase {{ next_phase.number }}: {{ next_phase.name }}

---

## 🚀 Continuation Instructions

**Copy and paste this prompt into a new Copilot Chat window:**

```
Follow instructions in .github/prompts/CORTEX.prompt.md.

Continue executing plan: {{ plan_name }}

Plan location: cortex-brain/documents/planning/active/{{ plan_name }}/

Current status: {{ completed_phases|length }}/{{ total_phases }} phases complete

Last completed: Phase {{ current_phase.number }} - {{ current_phase.name }}

Next action: Begin Phase {{ next_phase.number }} - {{ next_phase.name }}

Verify state with:
```python
from src.operations.mcp_tools.planning_mcp import PlanningStateMCP
mcp = PlanningStateMCP()
status = mcp.get_plan_status(plan_id="{{ plan_id }}")
print(status)
```
```

---

## 📊 State Summary

### Git Checkpoints
{% for checkpoint in checkpoints %}
- `{{ checkpoint.hash }}`: {{ checkpoint.message }} ({{ checkpoint.date }})
{% endfor %}

### Database State
- **Plan ID:** `{{ plan_id }}`
- **Status:** {{ plan_status }}
- **Active Phase:** {{ current_phase.number }}
- **Artifacts Generated:** {{ artifact_count }}

### Key Files
- Plan: `cortex-brain/documents/planning/active/{{ plan_name }}/00-master-plan.md`
- Progress Tracker: `cortex-brain/documents/planning/active/{{ plan_name }}/tracking/progress-tracker.json`
- Database: `cortex-brain/database/planning_state.db`
- This File: `cortex-brain/documents/planning/active/{{ plan_name }}/tracking/CONTINUATION-PROMPT.md`

---

## ⚠️ Important Notes

1. **State Recovery:** Always verify database state matches file system before continuing
2. **Git History:** Review recent commits to understand what was completed
3. **Phase Dependencies:** Ensure all prerequisites for next phase are met
4. **Testing:** Run tests for completed phases to verify integrity
5. **Documentation:** Update any in-progress documentation from previous session

---

**Resume execution with confidence. All state has been preserved.**

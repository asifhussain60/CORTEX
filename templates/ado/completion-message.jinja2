{#
  ADO Completion Message Template
  
  Used in Completion Phase to display success message after work items created.
  
  Context variables:
  - feature_name: str - feature name
  - test_mode: bool - whether test mode was enabled
  - items_created: int - number of work items created
  - work_item_links: List[Dict] - work item URLs with details
  - execution_time_seconds: float - total execution time
  - story_points: int - story points assigned
  - total_effort_hours: int - total effort in hours
#}

# 🎉 ADO Work Item Creation {{ 'Simulated' if test_mode else 'Complete' }}!

## Feature: {{ feature_name }}

{% if test_mode %}
### ⚠️ TEST MODE - No Real Work Items Created

This was a simulation. To create real work items in Azure DevOps:
- Set `test_mode=False` in execution parameters
- Ensure ADO_PAT environment variable is configured
- Verify ADO_ORGANIZATION_URL and ADO_PROJECT are set

{% else %}
### ✅ Work Items Successfully Created

{% endif %}

---

## Work Items {{ 'Planned' if test_mode else 'Created' }} ({{ items_created }})

{% if work_item_links %}
| Type | Title | ID | URL |
|------|-------|------|-----|
{% for item in work_item_links %}
| {{ item.type }} | {{ item.title }} | #{{ item.id }} | [View]( {{item.url}}) |
{% endfor %}
{% else %}
_No work item links available (test mode or creation failed)_
{% endif %}

---

## Metrics

- **Execution Time:** {{ "%.2f"|format(execution_time_seconds) }}s
- **Work Items {{ 'Planned' if test_mode else 'Created' }}:** {{ items_created }}
- **Story Points:** {{ story_points }}
- **Total Effort:** {{ total_effort_hours }}h

---

## Next Steps

{% if test_mode %}
1. **Review the work item preview above**
2. **Run again with `test_mode=False`** to create real work items
3. **Verify ADO configuration** (PAT token, organization, project)
{% else %}
1. **Review work items in Azure DevOps** using the links above
2. **Assign tasks** to team members
3. **Update work item states** as work progresses
4. **Track progress** in ADO boards and backlogs
{% endif %}

---

_Generated by ADO Orchestrator v2 • Execution ID: {{ execution_id | default('N/A') }}_

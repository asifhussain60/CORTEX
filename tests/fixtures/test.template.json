{
  "name": "{{PROJECT_NAME}}",
  "version": "{{VERSION:1.0.0}}",
  "environment": "{{ENV}}",
  "paths": {
    "root": "{{PROJECT_ROOT}}",
    "logs": "{{PROJECT_ROOT}}/logs",
    "config": "{{PROJECT_ROOT}}/config"
  },
  "metadata": {
    "branch": "{{GIT_BRANCH:main}}",
    "generated": "{{TIMESTAMP}}"
  }
}

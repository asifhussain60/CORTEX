"""
Universal Orchestrator Wiring Test Suite

AC-ID: UNIVERSAL-WIRING-TEST-001
Purpose: Validate that the registry intelligence system handles ALL orchestrator types
         Not just deployment, but refactoring, testing, analysis, onboarding, etc.

Author: Asif Hussain
Date: 2026-02-10
Compliance: CORE-008 (TDD), CORE-011 (type hints), CORE-012 (docstrings)
"""

import unittest
from unittest.mock import Mock, patch, MagicMock
from pathlib import Path
from typing import Dict, Any, List

from cortex.learning.registry_intelligence_agent import (
    RegistryIntelligenceAgent,
    OrchestratorDiscovery,
    RegistryGap,
)


class TestUniversalOrchestratorWiring(unittest.TestCase):
    """Test universal orchestrator wiring for ALL orchestrator types."""
    
    def setUp(self):
        """Set up test fixtures."""
        self.workspace_root = Path(__file__).parent.parent
        self.agent = RegistryIntelligenceAgent(
            workspace_root=self.workspace_root,
            enable_learning=False
        )
    
    def test_all_orchestrator_types_discovered(self):
        \"\"\"Test that ALL orchestrator types are discovered, not just deployment.\"\"\"\n        discoveries = self.agent.scan_for_orchestrators(force_rescan=True)\n        \n        # Expected orchestrator types that should be discovered\n        expected_types = {\n            \"DeploymentOrchestrator\": \"deploy\",\n            \"TDDOrchestrator\": \"test\", \n            \"RefactoringOrchestrator\": \"refactor\",\n            \"LENSSynthesis\": \"analyze\",\n            \"OnboardingOrchestrator\": \"onboard\",\n            \"PlanOrchestrator\": \"plan\",\n            \"MasterOrchestrator\": \"orchestrate\",\n            \"IntentRouter\": \"route\",\n            \"EnforcementOrchestrator\": \"audit\",\n            \"DocumentationOrchestrator\": \"document\"\n        }\n        \n        discovered_names = {d.name for d in discoveries}\n        \n        # Check that we discovered multiple orchestrator types\n        self.assertGreater(\n            len(discovered_names),\n            5,\n            f\"Should discover multiple orchestrator types, found: {discovered_names}\"\n        )\n        \n        # Check for specific high-value orchestrators\n        critical_orchestrators = [\"DeploymentOrchestrator\", \"TDDOrchestrator\", \"MasterOrchestrator\"]\n        \n        for critical in critical_orchestrators:\n            matching = [d for d in discoveries if critical in d.name]\n            self.assertTrue(\n                matching,\n                f\"Critical orchestrator type '{critical}' not discovered. Found: {discovered_names}\"\n            )\n    \n    def test_universal_intent_coverage(self):\n        \"\"\"Test that all intent types have corresponding orchestrators.\"\"\"\n        discoveries = self.agent.scan_for_orchestrators(force_rescan=True)\n        \n        # All intent types that should be covered\n        all_intents = [\n            \"deploy\", \"test\", \"refactor\", \"analyze\", \"onboard\", \n            \"plan\", \"debug\", \"audit\", \"document\", \"security\",\n            \"performance\", \"integration\", \"migration\", \"monitoring\"\n        ]\n        \n        # Check coverage for each intent\n        coverage_report = {}\n        for intent in all_intents:\n            matching_orchestrators = [\n                d.name for d in discoveries\n                if intent in d.keywords\n            ]\n            coverage_report[intent] = {\n                \"orchestrators\": matching_orchestrators,\n                \"covered\": len(matching_orchestrators) > 0\n            }\n        \n        # Calculate coverage percentage\n        covered_intents = sum(1 for info in coverage_report.values() if info[\"covered\"])\n        coverage_percentage = (covered_intents / len(all_intents)) * 100\n        \n        self.assertGreater(\n            coverage_percentage,\n            60.0,  # At least 60% of intents should have orchestrator coverage\n            f\"Intent coverage too low: {coverage_percentage}%. Coverage: {coverage_report}\"\n        )\n        \n        # Critical intents that MUST have coverage\n        critical_intents = [\"deploy\", \"test\", \"analyze\", \"audit\"]\n        for intent in critical_intents:\n            self.assertTrue(\n                coverage_report[intent][\"covered\"],\n                f\"Critical intent '{intent}' has no orchestrator coverage\"\n            )\n    \n    def test_orchestrator_inheritance_detection(self):\n        \"\"\"Test detection of complex orchestrator inheritance patterns.\"\"\"\n        discoveries = self.agent.scan_for_orchestrators(force_rescan=True)\n        \n        # Look for orchestrators with different inheritance patterns\n        inheritance_patterns = {\n            \"direct_orchestrator\": [],  # Classes ending in \"Orchestrator\"\n            \"base_protocol\": [],       # Inheriting from OrchestratorBaseProtocol\n            \"learning_mixin\": [],      # Using OrchestratorLearningMixin\n            \"interface_impl\": [],      # Implementing IOrchestrator\n            \"handler_pattern\": []      # Classes ending in \"Handler\"\n        }\n        \n        for discovery in discoveries:\n            name = discovery.name\n            if name.endswith(\"Orchestrator\"):\n                inheritance_patterns[\"direct_orchestrator\"].append(name)\n            elif name.endswith(\"Handler\"):\n                inheritance_patterns[\"handler_pattern\"].append(name)\n            \n            # Check docstring/file content for inheritance patterns\n            try:\n                content = discovery.file_path.read_text(encoding='utf-8')\n                if \"OrchestratorBaseProtocol\" in content:\n                    inheritance_patterns[\"base_protocol\"].append(name)\n                if \"OrchestratorLearningMixin\" in content:\n                    inheritance_patterns[\"learning_mixin\"].append(name)\n                if \"IOrchestrator\" in content:\n                    inheritance_patterns[\"interface_impl\"].append(name)\n            except Exception:\n                pass\n        \n        # Should detect multiple inheritance patterns\n        detected_patterns = sum(1 for pattern_list in inheritance_patterns.values() \n                              if pattern_list)\n        \n        self.assertGreater(\n            detected_patterns,\n            2,\n            f\"Should detect multiple inheritance patterns. Found: {inheritance_patterns}\"\n        )\n    \n    def test_dependency_analysis(self):\n        \"\"\"Test comprehensive dependency analysis across orchestrators.\"\"\"\n        discoveries = self.agent.scan_for_orchestrators(force_rescan=True)\n        dependencies = self.agent.analyze_orchestrator_dependencies(discoveries)\n        \n        # Should find dependencies between orchestrators\n        orchestrators_with_deps = [name for name, deps in dependencies.items() if deps]\n        \n        self.assertGreater(\n            len(orchestrators_with_deps),\n            3,\n            f\"Should find orchestrators with dependencies. Found: {orchestrators_with_deps}\"\n        )\n        \n        # Check for realistic dependency patterns\n        # MasterOrchestrator should depend on other orchestrators\n        if \"MasterOrchestrator\" in dependencies:\n            master_deps = dependencies[\"MasterOrchestrator\"]\n            self.assertGreater(\n                len(master_deps),\n                0,\n                \"MasterOrchestrator should have dependencies on other orchestrators\"\n            )\n    \n    def test_universal_validation_report(self):\n        \"\"\"Test comprehensive universal validation across all orchestrator types.\"\"\"\n        validation_report = self.agent.validate_universal_wiring()\n        \n        # Validation report should be comprehensive\n        required_fields = [\n            \"total_orchestrators\", \"registered_count\", \"unregistered_count\",\n            \"coverage_by_intent\", \"dependency_issues\", \"mcp_exposure_gaps\",\n            \"health_score\", \"overall_status\"\n        ]\n        \n        for field in required_fields:\n            self.assertIn(\n                field,\n                validation_report,\n                f\"Validation report missing required field: {field}\"\n            )\n        \n        # Should find significant number of orchestrators\n        self.assertGreater(\n            validation_report[\"total_orchestrators\"],\n            10,\n            \"Should discover significant number of orchestrators\"\n        )\n        \n        # Health score should be calculated\n        self.assertIsInstance(validation_report[\"health_score\"], (int, float))\n        self.assertGreaterEqual(validation_report[\"health_score\"], 0)\n        self.assertLessEqual(validation_report[\"health_score\"], 100)\n    \n    def test_universal_auto_fix_capabilities(self):\n        \"\"\"Test that auto-fix handles all types of orchestrator issues.\"\"\"\n        # First get validation report\n        validation_report = self.agent.validate_universal_wiring()\n        \n        # Test universal auto-fix (dry run)\n        fix_results = self.agent.universal_auto_fix(\n            validation_report=validation_report,\n            dry_run=True\n        )\n        \n        # Should identify fixable issues\n        self.assertIn(\"total_fixes_attempted\", fix_results)\n        self.assertIn(\"fixes_by_type\", fix_results)\n        \n        # Should cover different types of fixes\n        fix_types = fix_results[\"fixes_by_type\"]\n        expected_fix_types = [\n            \"orchestrator_registration\", \"keyword_mapping\", \n            \"mcp_exposure\", \"dependency_resolution\"\n        ]\n        \n        for fix_type in expected_fix_types:\n            self.assertIn(\n                fix_type,\n                fix_types,\n                f\"Auto-fix should handle {fix_type}\"\n            )\n    \n    def test_all_mcp_tool_categories_covered(self):\n        \"\"\"Test that MCP tools cover all orchestrator categories.\"\"\"\n        from cortex.mcp.tools import MCP_TOOLS\n        \n        # Get all MCP tool categories\n        categories = set()\n        for tool_info in MCP_TOOLS.values():\n            if \"category\" in tool_info:\n                categories.add(tool_info[\"category\"])\n        \n        # Should have diverse categories covering different orchestrator types\n        expected_categories = {\n            \"deployment\", \"analysis\", \"testing\", \"governance\", \n            \"onboarding\", \"maintenance\", \"discovery\"\n        }\n        \n        overlap = categories & expected_categories\n        coverage_percentage = (len(overlap) / len(expected_categories)) * 100\n        \n        self.assertGreater(\n            coverage_percentage,\n            50.0,  # At least 50% category coverage\n            f\"MCP tool category coverage too low: {coverage_percentage}%. \"\n            f\"Found categories: {categories}, Expected: {expected_categories}\"\n        )\n    \n    def test_end_to_end_universal_workflow(self):\n        \"\"\"Test complete end-to-end universal orchestrator wiring workflow.\"\"\"\n        # Step 1: Universal discovery\n        discoveries = self.agent.scan_for_orchestrators(force_rescan=True)\n        self.assertGreater(len(discoveries), 5, \"Should discover multiple orchestrators\")\n        \n        # Step 2: Universal validation\n        validation_report = self.agent.validate_universal_wiring(discoveries)\n        self.assertIn(\"overall_status\", validation_report)\n        \n        # Step 3: Universal auto-fix (dry run)\n        fix_results = self.agent.universal_auto_fix(validation_report, dry_run=True)\n        self.assertIn(\"overall_status\", fix_results)\n        \n        # Step 4: Learning capture (if enabled)\n        if self.agent.learning_loop:\n            # Should capture patterns from the workflow\n            pass  # Learning integration tested separately\n        \n        # Workflow should complete without critical errors\n        self.assertNotEqual(validation_report[\"overall_status\"], \"error\")\n        self.assertNotEqual(fix_results[\"overall_status\"], \"error\")\n    \n    def test_specific_orchestrator_scenarios(self):\n        \"\"\"Test specific orchestrator wiring scenarios beyond deployment.\"\"\"\n        discoveries = self.agent.scan_for_orchestrators(force_rescan=True)\n        \n        # Scenario 1: TDD Orchestrator should have test-related keywords\n        tdd_orchestrators = [d for d in discoveries if \"TDD\" in d.name]\n        if tdd_orchestrators:\n            tdd_orch = tdd_orchestrators[0]\n            test_keywords = {\"test\", \"tdd\", \"spec\", \"verify\"}\n            found_test_keywords = tdd_orch.keywords & test_keywords\n            \n            self.assertTrue(\n                found_test_keywords,\n                f\"TDDOrchestrator should have test keywords. Found: {tdd_orch.keywords}\"\n            )\n        \n        # Scenario 2: Refactoring Orchestrator should have refactor keywords\n        refactor_orchestrators = [d for d in discoveries if \"Refactor\" in d.name]\n        if refactor_orchestrators:\n            refactor_orch = refactor_orchestrators[0]\n            refactor_keywords = {\"refactor\", \"clean\", \"improve\", \"optimize\"}\n            found_refactor_keywords = refactor_orch.keywords & refactor_keywords\n            \n            self.assertTrue(\n                found_refactor_keywords,\n                f\"RefactoringOrchestrator should have refactor keywords. Found: {refactor_orch.keywords}\"\n            )\n        \n        # Scenario 3: Analysis orchestrators should have analyze keywords\n        analysis_orchestrators = [d for d in discoveries if \"LENS\" in d.name or \"Analy\" in d.name]\n        if analysis_orchestrators:\n            analysis_orch = analysis_orchestrators[0]\n            analysis_keywords = {\"analyze\", \"lens\", \"inspect\", \"examine\"}\n            found_analysis_keywords = analysis_orch.keywords & analysis_keywords\n            \n            self.assertTrue(\n                found_analysis_keywords,\n                f\"Analysis orchestrator should have analysis keywords. Found: {analysis_orch.keywords}\"\n            )\n\n\nif __name__ == \"__main__\":\n    # Run comprehensive universal wiring tests\n    suite = unittest.TestLoader().loadTestsFromTestCase(TestUniversalOrchestratorWiring)\n    runner = unittest.TextTestRunner(verbosity=2)\n    result = runner.run(suite)\n    \n    # Print summary\n    print(f\"\\n{'='*60}\")\n    print(\"UNIVERSAL ORCHESTRATOR WIRING TEST SUMMARY\")\n    print(f\"{'='*60}\")\n    print(f\"Tests Run: {result.testsRun}\")\n    print(f\"Failures: {len(result.failures)}\")\n    print(f\"Errors: {len(result.errors)}\")\n    print(f\"Success Rate: {((result.testsRun - len(result.failures) - len(result.errors)) / result.testsRun * 100):.1f}%\")\n    \n    if result.failures:\n        print(\"\\nFAILURES:\")\n        for test, traceback in result.failures:\n            print(f\"- {test}: {traceback.split('AssertionError: ')[-1].split('\\n')[0]}\")\n    \n    if result.errors:\n        print(\"\\nERRORS:\")\n        for test, traceback in result.errors:\n            print(f\"- {test}: {traceback.split('\\n')[-2]}\")
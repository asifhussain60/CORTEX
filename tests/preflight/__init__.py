"""CORTEX Preflight Tests — T0 tier.

~40 surgical tests that validate CORTEX wiring, imports, and key
operational components. Target: < 10s parallel on 8 workers.

These are the ONLY tests run during /audit fix Stage 9.
For full suite, use /healthcheck (runs batch mode).
"""

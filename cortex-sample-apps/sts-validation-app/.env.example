# Example configuration for local development
# DO NOT use these values in production!
# This file updated during Capability 1 (Code Sanitization) validation

# JWT Configuration
JWT_SECRET=dev_secret_key_for_testing_only_change_in_production

# SMTP Configuration (using Gmail for development)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dev@example.com
SMTP_PASSWORD=dev_password_change_me

# Database Configuration
DATABASE_PATH=./data/ecommerce_dev.db

# Application Configuration
DEBUG=True

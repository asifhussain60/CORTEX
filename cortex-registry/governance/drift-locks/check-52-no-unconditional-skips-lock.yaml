# Drift Lock: Check #52 — No Unconditional Test Skips (CORE-075)
# Authority: CORE-075 (Zero Test Bypass Tolerance)
# Created: 2026-03-17

id: "check-52-no-unconditional-skips-lock"
check_number: 52
title: "No Unconditional Test Skips"
test_file: "tests/preflight/test_no_unconditional_skips.py"
rule: "CORE-075"
status: "ACTIVE"
enforcement_tier: "P0"
ci_gate: true
detect_command: "python -m pytest tests/preflight/test_no_unconditional_skips.py -q --no-header"
description: |
  Enforces a hard ceiling on module-level unconditional pytest.mark.skip
  markers. The ceiling can only decrease (ratchet-down protocol).

locked_artifacts:
  - path: "tests/preflight/test_no_unconditional_skips.py"
    contract: "UNCONDITIONAL_SKIP_CEILING must only decrease"
  - path: "cortex-registry/governance/skip-debt-registry.yaml"
    contract: "All unconditional skips must be registered with remediation_action"

ceiling_history:
  - date: "2026-03-17"
    ceiling: 22
    event: "Initial baseline — CORE-075 introduced"

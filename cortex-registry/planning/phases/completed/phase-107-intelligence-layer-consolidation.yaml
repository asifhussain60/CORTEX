# CORTEX Phase Plan — Phase 107: Intelligence Layer Consolidation
# Layered Diamond Architecture + Migration Automation + E2E Audit Verification
# Authority: Architect Review 2026-03-02
# Governance: CORE-008, CORE-035, CORE-064, CORE-068

id: "phase-107"
title: "Intelligence Layer Consolidation — Layered Diamond Architecture with Migration Automation, Prompt/Agent Refresh, Audit Hardening, and P0 Wiring Verification"
version: "1.0"
created: "2026-03-02"
status: COMPLETE
priority: P0
governance_authority:
  - CORE-008   # TDD mandatory
  - CORE-011   # Type hints
  - CORE-012   # Docstrings
  - CORE-035   # Single canonical implementation
  - CORE-048   # Holistic validation gate
  - CORE-064   # Sweep Completeness Contract
  - CORE-068   # Universal Convergence Gate
sweep_catalogue_id: "SWEEP-107-INTELLIGENCE-CONSOLIDATION"

# ─────────────────────────────────────────────────────────────────────────────
# BACKGROUND
# ─────────────────────────────────────────────────────────────────────────────
background: |
  Source: Architect Review (2026-03-02) — holistic intelligence audit
  Score at entry: 4.5 / 10 (cognitive load: 379 files, 102K LOC, 100 dirs, 5 duplicate synthesis engines)
  Target score: 8.5 / 10
  Baseline measurements:
    - Smoke tests: 1950 passed
    - Intelligence files: 230 (cortex/intelligence/) + 65 (cortex/lens/) + 27 (cortex/governance/) + 1 (cortex/knowledge/)
    - Intelligence LOC: ~102,000
    - Subdirectories: ~100
    - Duplicate synthesis engines: 5
    - Duplicate base classes: 2 (base.py + base_engine.py)
    - Entry point facades: 3 (UnifiedIntelligenceProvider, LENSIntelligenceFacade, KnowledgeRegistryProxy)
    - DomainBrainAPI duplicate definitions: 2
    - Single-file subdirectories (1-2 files each): 8 (perception, sensory, reasoning, nlp, explainability, verification, education, infrastructure)
    - Wiring YAML specs: 5 files in cortex-registry/core/specifications/
    - Wiring test coverage: 50 test files scattered, no unified P0 gate
  Git prerequisite: All prior phases COMPLETE, smoke green

  Key problems:
    1. FOUR separate top-level packages for intelligence (intelligence/, lens/, knowledge/, governance/)
    2. FIVE synthesis engines doing the same knowledge-merge operation
    3. TWO competing base classes (base.py ABC vs base_engine.py concrete)
    4. cortex/intelligence/lens/ duplicates cortex/lens/ concepts
    5. domain_brain/domain_brain/ — double-nested namespace anti-pattern
    6. 27 subdirectories inside intelligence/ — 8 have only 1-2 files
    7. 46 memory tier files mostly stubs
    8. copilot-instructions.md, prompts, agents reference scattered intelligence paths
    9. Audit Check #20 (Workflow Composer health) has no end-to-end wiring verification test
    10. No unified AC_START→AC_COMPLETE verification test across all audit stages
    11. VacuumOrchestrator lacks intelligence-migration cleanup stages

# ─────────────────────────────────────────────────────────────────────────────
# GAP CATALOGUE — CORE-064: ALL must be CLOSED before phase COMPLETE
# ─────────────────────────────────────────────────────────────────────────────
sweep_catalogue:
  # ── Sub-phase A: Unified Models + Single Base Class ────────────────────
  - id: "GAP-107-01"
    title: "Two competing BaseIntelligenceEngine classes — base.py (ABC) vs base_engine.py (concrete)"
    severity: P0
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex/intelligence/base.py — ABC with AnalysisContext, AnalysisResult
      cortex/intelligence/base_engine.py — concrete with EngineMetrics, caching
      Both define 'BaseIntelligenceEngine' — CORE-035 violation
    action_ref: "phase-107-a"
    status: CLOSED

  - id: "GAP-107-02"
    title: "Duplicate UnifiedIntelligenceContext / SynthesisResult definitions across 3+ files"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex/intelligence/knowledge/unified_intelligence_context.py — SynthesisResult
      cortex/intelligence/tier3/knowledge/synthesis_engine.py — SynthesisResult (duplicate)
      cortex/intelligence/knowledge/knowledge_synthesis_engine.py — SynthesizedInstruction
    action_ref: "phase-107-a"
    status: CLOSED

  # ── Sub-phase B: Synthesis Engine Consolidation ────────────────────────
  - id: "GAP-107-03"
    title: "5 synthesis engines doing variations of knowledge-merge"
    severity: P0
    source: "Architect Review 2026-03-02"
    evidence: |
      1. cortex/intelligence/knowledge/knowledge_synthesis_engine.py (KnowledgeSynthesisEngine, 1403 LOC)
      2. cortex/intelligence/learning/knowledge_synthesizer.py (KnowledgeSynthesizer)
      3. cortex/intelligence/tier3/knowledge/synthesis_engine.py (SynthesisEngine)
      4. cortex/intelligence/lens/lens/lens_pipeline.py (SynthesisPhase)
      5. cortex/intelligence/domain_brain/lens_integration.py (LENSSynthesis)
      grep: 'grep -rn "class.*Synthesis" cortex/intelligence --include="*.py"' → 9 hits
    action_ref: "phase-107-b"
    status: CLOSED

  - id: "GAP-107-04"
    title: "Duplicate BusinessKnowledgeRepository in 2 locations"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex/intelligence/knowledge/business_knowledge_repository.py
      cortex/intelligence/domain_brain/business_knowledge_repository.py
    action_ref: "phase-107-b"
    status: CLOSED

  # ── Sub-phase C: LENS Merge + Facade Unification ──────────────────────
  - id: "GAP-107-05"
    title: "cortex/lens/ is a separate top-level package — should be under intelligence/analysis/"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex/lens/ — 65 files, 27K LOC
      cortex/intelligence/lens/ — ANOTHER lens layer inside intelligence (duplication)
      LENSOrchestrator in cortex/lens/lens_orchestrator.py (2045 LOC)
      LENSIntelligenceFacade in cortex/lens/facade.py (308 LOC)
    action_ref: "phase-107-c"
    status: CLOSED

  - id: "GAP-107-06"
    title: "3 entry point facades — callers don't know which to use"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      1. cortex/intelligence/provider.py — UnifiedIntelligenceProvider (778 LOC)
      2. cortex/lens/facade.py — LENSIntelligenceFacade (308 LOC)
      3. cortex/knowledge/registry_proxy.py — KnowledgeRegistryProxy (248 LOC)
    action_ref: "phase-107-c"
    status: CLOSED

  # ── Sub-phase D: Directory Flattening + Stub Pruning ───────────────────
  - id: "GAP-107-07"
    title: "domain_brain/domain_brain/ double-nested namespace"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex/intelligence/domain_brain/domain_brain/ — 15 KG files nested redundantly
      DomainBrainAPI defined in BOTH __init__.py AND api.py
    action_ref: "phase-107-d"
    status: CLOSED

  - id: "GAP-107-08"
    title: "8 single-file subdirectories should be modules not packages"
    severity: P2
    source: "Architect Review 2026-03-02"
    evidence: |
      perception/ — 1 file (chat_file_detector.py)
      sensory/ — 1 file
      reasoning/ — 1 file (strategy_selector.py)
      nlp/ — 1 file (embedding_cache.py)
      explainability/ — 2 files
      verification/ — 2 files
      education/ — 3 files
      infrastructure/ — 2 files
    action_ref: "phase-107-d"
    status: CLOSED

  - id: "GAP-107-09"
    title: "Memory tier (46 files) — mostly stubs/config-holders with minimal runtime value"
    severity: P2
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex/intelligence/memory/ — 46 files across tier1/tier2/tier3
      tier3_scratch/ — 1 YAML file only
      memory/core/ — path_abstraction.py, linux/mac/windows compat (platform utils, not intelligence)
    action_ref: "phase-107-d"
    status: CLOSED

  # ── Sub-phase E: Prompt/Agent/copilot-instructions Refresh ─────────────
  - id: "GAP-107-10"
    title: "copilot-instructions.md references scattered intelligence paths that will change"
    severity: P0
    source: "Architect Review 2026-03-02"
    evidence: |
      .github/copilot-instructions.md references:
        - cortex/intelligence/ (generic)
        - cortex/intelligence/learning/rca_engine.py
        - cortex/intelligence/learning/rca_store.py
        - cortex/lens/ (separate package)
      All prompts in .github/prompts/ reference intelligence paths
      All agents in .github/agents/ reference intelligence paths
    action_ref: "phase-107-e"
    status: CLOSED

  - id: "GAP-107-11"
    title: "Audit agent (cortex-auditor.md) references Check #20 without end-to-end wiring verification"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex-auditor.md describes Check #20 (Workflow Composer Pipeline Health)
      No golden test validates the complete audit pipeline E2E
      No test verifies AC_START→AC_COMPLETE persistence for all 9 audit stages
    action_ref: "phase-107-e"
    status: CLOSED

  # ── Sub-phase F: Audit-Fix Pipeline Hardening ──────────────────────────
  - id: "GAP-107-12"
    title: "No E2E test verifying AC_START→AC_COMPLETE for every audit stage persisted to SQLite"
    severity: P0
    source: "Architect Review 2026-03-02"
    evidence: |
      Check #28 (AC marker persistence gap) is documented but has no acceptance test
      orchestrator-traces.db may have 0 AC_START rows even when workflow_runs > 0
      No test asserts: 'for each audit stage, trace_master contains AC_START AND AC_COMPLETE'
    action_ref: "phase-107-f"
    status: CLOSED

  - id: "GAP-107-13"
    title: "28-Point audit checks lack machine-verifiable acceptance tests — manual grep commands only"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex-architect.prompt.md § 28-Point Production Readiness Audit
      Each check has a 'command' column but no pytest test that executes and asserts
      Checks #1-#28 are verified manually — no test gate prevents regression
    action_ref: "phase-107-f"
    status: CLOSED

  # ── Sub-phase G: P0 Wiring Verification Gate ──────────────────────────
  - id: "GAP-107-14"
    title: "Wiring YAML specs (5 files) have no unified L1-structural import test at preflight tier"
    severity: P0
    source: "Architect Review 2026-03-02"
    evidence: |
      cortex-registry/core/specifications/ — 5 wiring YAMLs
      tests/preflight/test_orchestrator_wiring.py — checks 12 orchestrators (hardcoded)
      But wiring YAMLs declare MORE orchestrators — no test validates spec↔reality alignment
      architecture-integrity-agent.md describes L1/L2/L3 checks but no automated P0 gate
    action_ref: "phase-107-g"
    status: CLOSED

  - id: "GAP-107-15"
    title: "No test validates every wiring.yaml 'entry_point' is importable and class exists"
    severity: P0
    source: "Architect Review 2026-03-02"
    evidence: |
      core-orchestrator-wiring.yaml has 8 provides entries with entry_point paths
      domain-orchestrator-wiring.yaml, support-orchestrator-wiring.yaml have more
      No test iterates ALL wiring YAML entry_points and verifies import + class existence
    action_ref: "phase-107-g"
    status: CLOSED

  - id: "GAP-107-16"
    title: "MCP registry tool count (29) not validated against actual tool file count (35) in preflight"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      tests/preflight/test_mcp_wiring.py — tests 6 specific tool files
      cortex/mcp/mcp_registry.py registers 29 tools
      cortex/mcp/tools/ has 35 tool files
      No test asserts: len(registered_tools) >= MINIMUM and all registered are importable
    action_ref: "phase-107-g"
    status: CLOSED

  # ── Sub-phase H: VacuumOrchestrator Migration Stages ───────────────────
  - id: "GAP-107-17"
    title: "VacuumOrchestrator has no migration-cleanup stage for intelligence consolidation"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      vacuum_orchestrator.py has 8 pipeline stages
      No stage handles: compat shim detection, stale import path cleanup, empty __init__.py after moves
      Post-consolidation, stale cortex/lens/ and cortex/knowledge/ compat shims need lifecycle management
    action_ref: "phase-107-h"
    status: CLOSED

  - id: "GAP-107-18"
    title: "VacuumOrchestrator doesn't detect stale intelligence path imports in non-cortex files"
    severity: P2
    source: "Architect Review 2026-03-02"
    evidence: |
      After moving cortex/lens/ → cortex/intelligence/analysis/, any external consumer
      referencing old paths will break. Vacuum should scan for 'from cortex.lens' in tests/ and scripts/
    action_ref: "phase-107-h"
    status: CLOSED

  # ── Sub-phase I: Production Readiness Check Expansion ──────────────────
  - id: "GAP-107-19"
    title: "Checks #1-#28 missing: intelligence-layer health check (facade importable, synthesis callable)"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      After consolidation, a new Check #29 should verify:
        - IntelligenceFacade importable
        - facade.analyze() returns valid result for sample file
        - UnifiedIntelligenceProvider.synthesize() callable
        - All knowledge YAML paths resolvable
    action_ref: "phase-107-i"
    status: CLOSED

  - id: "GAP-107-20"
    title: "copilot-instructions.md audit check table out of sync after intelligence consolidation"
    severity: P1
    source: "Architect Review 2026-03-02"
    evidence: |
      The 28-point table in copilot-instructions.md (carried into cortex-architect.prompt.md)
      will have stale paths (cortex/lens/ etc.) and needs new intelligence-layer checks
    action_ref: "phase-107-i"
    status: CLOSED

# ─────────────────────────────────────────────────────────────────────────────
# SEQUENTIAL EXECUTION CONTRACT
# ─────────────────────────────────────────────────────────────────────────────
sequential_execution_contract:
  policy: STRICT_SEQUENTIAL
  partial_completion_allowed: false
  decomposition_allowed: false
  phase_atomic: true
  gate_on_failure: HALT
  tdd_cycle_mandatory: true

# ─────────────────────────────────────────────────────────────────────────────
# PHASES — ROI-ordered, TDD-gated sub-phases (A through I)
# ─────────────────────────────────────────────────────────────────────────────
phases:

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE A: Unified Models + Single Base Class
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-a"
    title: "Unified Intelligence Models — Single BaseIntelligenceEngine + Consolidated Data Models"
    priority: P0
    status: COMPLETE
    gap_refs: ["GAP-107-01", "GAP-107-02"]
    governance_authority: "CORE-008, CORE-035, CORE-064"
    duration_estimate: "4 hours"
    depends_on: []
    prerequisite: "smoke tests green (≥1950 baseline)"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write ALL failing tests BEFORE any implementation"
        gate: "python3 scripts/run_tests.py file tests/intelligence/models/test_unified_models.py — ALL tests FAIL"
        blocker: "Do NOT write implementation until tests fail"
      green:
        action: "Implement unified models to turn all RED tests GREEN"
        gate: "python3 scripts/run_tests.py file tests/intelligence/models/test_unified_models.py — ALL PASS"
        blocker: "Do NOT begin REFACTOR until every test passes"
      refactor:
        action: "Add type hints, docstrings, deduplicate, add compat re-exports"
        gate: "python3 scripts/run_tests.py smoke — zero regressions"
        blocker: "Do NOT mark COMPLETE until refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py smoke + ruff check cortex/intelligence/models/"
      fix_step: "Fix any lint or test failure from model consolidation"
      success_predicate: "p0_count == 0 and p1_count == 0 and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log remaining issues — do NOT silently skip"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_single_base_engine_class_exists — assert exactly 1 BaseIntelligenceEngine importable from cortex.intelligence.models"
        - "test_analysis_result_canonical — assert AnalysisResult importable from cortex.intelligence.models (not from base.py)"
        - "test_analysis_context_canonical — assert AnalysisContext importable from cortex.intelligence.models"
        - "test_engine_metrics_canonical — assert EngineMetrics importable from cortex.intelligence.models"
        - "test_synthesis_result_single_definition — assert exactly 1 SynthesisResult class across entire cortex/ (no duplicates)"
        - "test_unified_intelligence_context_canonical — assert UnifiedIntelligenceContext importable from cortex.intelligence.models"
        - "test_old_base_module_compat_shim — assert 'from cortex.intelligence.base import BaseIntelligenceEngine' still works (compat)"
        - "test_old_base_engine_compat_shim — assert 'from cortex.intelligence.base_engine import BaseIntelligenceEngine' still works (compat)"
      green:
        - "Create cortex/intelligence/models/ package with consolidated classes"
        - "Move AnalysisContext, AnalysisResult, EngineMetrics into cortex/intelligence/models/analysis_result.py"
        - "Move UnifiedIntelligenceContext, SynthesisResult, CompanyKnowledge, CORTEXKnowledge, LENSIntelligence into cortex/intelligence/models/context.py"
        - "Merge BaseIntelligenceEngine from both base.py and base_engine.py — keep caching + ABC contract"
        - "Add compat re-exports in cortex/intelligence/base.py and base_engine.py"
      refactor:
        - "Delete duplicate SynthesisResult from tier3/knowledge/synthesis_engine.py"
        - "Add docstrings (CORE-012), type hints (CORE-011)"
        - "Verify no duplicate class names (ruff F811)"

    acceptance_criteria:
      - "Single BaseIntelligenceEngine class in cortex/intelligence/models/ — merges ABC + caching"
      - "Single SynthesisResult definition (grep -rn 'class SynthesisResult' cortex/ → exactly 1 hit)"
      - "All existing imports via compat shims work without changes (backward compatible)"
      - "Smoke tests pass with zero new failures"
      - "ruff check cortex/intelligence/ --select=F811 → 0 violations"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE B: Synthesis Engine Consolidation
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-b"
    title: "Synthesis Engine Consolidation — 5→1 Knowledge Merge Engine"
    priority: P0
    status: COMPLETE
    gap_refs: ["GAP-107-03", "GAP-107-04"]
    governance_authority: "CORE-008, CORE-035, CORE-064"
    duration_estimate: "6 hours"
    depends_on: ["phase-107-a"]
    prerequisite: "phase-107-a completion_gate PASSED"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write tests for unified synthesis engine"
        gate: "ALL tests FAIL"
        blocker: "No implementation until tests fail"
      green:
        action: "Implement single KnowledgeSynthesisEngine"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Remove 4 duplicate engines, add compat shims"
        gate: "Smoke green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py smoke + grep -rn 'class.*SynthesisEngine' cortex/ --include='*.py'"
      fix_step: "Remove remaining duplicate definitions"
      success_predicate: "synthesis_engine_count == 1 and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_single_synthesis_engine — assert exactly 1 class named *SynthesisEngine in cortex/ (AST scan)"
        - "test_synthesis_engine_has_tiered_execution — synthesize(tier=QUICK|TARGETED|FULL) works"
        - "test_synthesis_merges_cortex_and_company — output contains both CORTEX + Company knowledge"
        - "test_synthesis_precedence_resolution — Company rules override CORTEX when conflict"
        - "test_single_business_knowledge_repository — exactly 1 BusinessKnowledgeRepository class"
        - "test_synthesis_engine_caches_results — repeated calls with same input return cached"
      green:
        - "Consolidate into cortex/intelligence/knowledge/synthesis_engine.py (keep best of KnowledgeSynthesisEngine)"
        - "Add tiered execution (quick/targeted/full) to unified engine"
        - "Merge BusinessKnowledgeRepository — keep cortex/intelligence/knowledge/ copy"
        - "Add compat shims for old import paths"
      refactor:
        - "Delete cortex/intelligence/learning/knowledge_synthesizer.py"
        - "Delete cortex/intelligence/tier3/knowledge/synthesis_engine.py"
        - "Delete cortex/intelligence/domain_brain/lens_integration.py LENSSynthesis"
        - "Verify CORE-035 compliance"

    acceptance_criteria:
      - "grep -rn 'class.*SynthesisEngine' cortex/ --include='*.py' → exactly 1 canonical class"
      - "grep -rn 'class BusinessKnowledgeRepository' cortex/ --include='*.py' → exactly 1"
      - "Tiered execution: quick (<200ms cached), targeted (<2s), full (<10s)"
      - "All existing callers work via compat shims"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE C: LENS Merge + Facade Unification  [sequence: after D]
  # SEQUENCE ADJUSTMENT 2026-03-02: Flatten dirs FIRST (sub-phase D) to
  # reduce collision surface before moving 74 LENS files. Risk: HIGH without
  # this — double-nested dirs + import chain turbulence compound each other.
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-c"
    title: "LENS Merge into Intelligence + Single IntelligenceFacade"
    priority: P1
    status: COMPLETE
    gap_refs: ["GAP-107-05", "GAP-107-06"]
    governance_authority: "CORE-008, CORE-035, CORE-064"
    duration_estimate: "8 hours"
    depends_on: ["phase-107-d"]
    prerequisite: "phase-107-d completion_gate PASSED  # ADJUSTED: D before C"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write facade unification tests"
        gate: "ALL tests FAIL"
        blocker: "No implementation until tests fail"
      green:
        action: "Create IntelligenceFacade, move LENS analyzers, add shims"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Remove duplicate lens layer, update imports"
        gate: "Smoke green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py smoke + ruff check cortex/"
      fix_step: "Fix broken imports, remove stale files"
      success_predicate: "p0_count == 0 and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_intelligence_facade_single_entry_point — from cortex.intelligence.facade import IntelligenceFacade"
        - "test_facade_analyze_file — facade.analyze(file_path, intent) returns structured result"
        - "test_facade_delegates_to_lens — internally uses LENSOrchestrator for analysis"
        - "test_facade_delegates_to_synthesis — internally uses KnowledgeSynthesisEngine for knowledge"
        - "test_facade_delegates_to_learning — internally routes RCA requests to RCAEngine"
        - "test_lens_compat_shim — 'from cortex.lens.lens_orchestrator import LENSOrchestrator' still works"
        - "test_knowledge_compat_shim — 'from cortex.knowledge.registry_proxy import KnowledgeRegistryProxy' still works"
        - "test_lens_analyzers_importable_from_intelligence — 'from cortex.intelligence.analysis.ast_analyzer import ASTAnalyzer'"
      green:
        - "Create cortex/intelligence/facade.py — IntelligenceFacade (Mediator pattern)"
        - "Create cortex/intelligence/analysis/ — move cortex/lens/analyzers/ contents"
        - "Keep cortex/lens/ as compat shim package (re-exports only)"
        - "Keep cortex/knowledge/ as compat shim package (re-exports only)"
      refactor:
        - "Delete cortex/intelligence/lens/ nested duplicate"
        - "Update all orchestrator imports to use new paths (with grep verification)"
        - "Verify CORE-035"

    acceptance_criteria:
      - "'from cortex.intelligence.facade import IntelligenceFacade' works"
      - "facade.analyze() returns valid AnalysisResult"
      - "All cortex/lens/ re-exports work (zero breaking changes for external callers)"
      - "cortex/intelligence/lens/ directory deleted"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE D: Directory Flattening + Stub Pruning  [sequence: before C]
  # SEQUENCE ADJUSTMENT 2026-03-02: Run D BEFORE C to remove double-nested
  # dirs and 8 single-file stub packages first. Reduces import turbulence
  # when 74 LENS files are moved in sub-phase C.
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-d"
    title: "Directory Flattening — domain_brain + Single-File Dirs + Memory Tier Pruning"
    priority: P1
    status: COMPLETE
    gap_refs: ["GAP-107-07", "GAP-107-08", "GAP-107-09"]
    governance_authority: "CORE-008, CORE-035, CORE-064"
    duration_estimate: "4 hours"
    depends_on: ["phase-107-b"]
    prerequisite: "phase-107-b completion_gate PASSED  # ADJUSTED: D now runs before C"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write tests for flattened imports"
        gate: "ALL tests FAIL"
        blocker: "No implementation until tests fail"
      green:
        action: "Flatten directories, merge DomainBrainAPI, prune stubs"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Delete empty dirs, update all references"
        gate: "Smoke green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "find cortex/intelligence -maxdepth 1 -type d | wc -l — should be ≤15"
      fix_step: "Continue flattening"
      success_predicate: "intelligence_subdirs <= 15 and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_domain_brain_flat_import — 'from cortex.intelligence.knowledge.domain_brain import DomainBrainAPI'"
        - "test_single_domain_brain_api — exactly 1 DomainBrainAPI class definition"
        - "test_no_double_nested_dirs — no directory pattern intelligence/X/X/ exists"
        - "test_intelligence_subdir_count — ≤12 top-level subdirectories in cortex/intelligence/"
        - "test_memory_essential_only — cortex/intelligence/memory/ has ≤8 non-init .py files"
      green:
        - "Flatten domain_brain/domain_brain/ into knowledge/domain_brain.py or knowledge/kg/"
        - "Merge 8 single-file dirs into parent (perception→sensory.py, etc.)"
        - "Prune memory tier — keep core/, tier1_learned/knowledge_retention_store.py, delete stubs"
      refactor:
        - "Delete empty directories"
        - "Update all imports"
        - "Verify CORE-035"

    acceptance_criteria:
      - "find cortex/intelligence -maxdepth 1 -type d | wc -l → ≤15 (was 28)"
      - "No double-nested directories (intelligence/X/X/)"
      - "Single DomainBrainAPI definition"
      - "Memory tier ≤8 essential files (was 46)"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE E: Prompt / Agent / copilot-instructions Refresh
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-e"
    title: "Prompt Suite Refresh — copilot-instructions.md + All Prompts + All Agents"
    priority: P0
    status: COMPLETE
    gap_refs: ["GAP-107-10", "GAP-107-11"]
    governance_authority: "CORE-008, CORE-064"
    duration_estimate: "4 hours"
    depends_on: ["phase-107-d"]
    prerequisite: "phase-107-d completion_gate PASSED (all consolidation done)"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write tests for prompt accuracy"
        gate: "ALL tests FAIL"
        blocker: "No edits until tests fail"
      green:
        action: "Run refresh_prompt_suite.py + manual path updates"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Clean up stale references, validate all paths"
        gate: "Smoke green + path validation passes"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "grep -rn 'cortex/lens/' .github/ + grep -rn 'cortex/knowledge/' .github/ — should be 0 non-compat refs"
      fix_step: "Update remaining stale references"
      success_predicate: "stale_path_refs == 0 and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_copilot_instructions_no_stale_lens_path — grep 'cortex/lens/' .github/copilot-instructions.md → 0 (replaced with cortex/intelligence/analysis/)"
        - "test_copilot_instructions_no_stale_knowledge_path — grep 'cortex/knowledge/' .github/copilot-instructions.md → 0 (replaced with cortex/intelligence/knowledge/)"
        - "test_architect_prompt_paths_exist — every file path in cortex-architect.prompt.md is a real file on disk"
        - "test_agent_index_counts_match_reality — AGENT-INDEX.md orchestrator count matches live count"
        - "test_all_prompts_no_deleted_paths — no prompt file references dissolved paths (cortex_intelligence, cortex_lens, cortex.brain)"
        - "test_auditor_agent_check20_wiring_test_exists — cortex-auditor.md Check #20 has a corresponding golden test"
        - "test_copilot_instructions_intelligence_facade_documented — IntelligenceFacade mentioned as canonical entry"
      green:
        - "Run python3 scripts/refresh_prompt_suite.py — auto-regenerate copilot-instructions.md"
        - "Update cortex-architect.prompt.md — replace cortex/lens/ refs with cortex/intelligence/analysis/"
        - "Update cortex-auditor.md — add intelligence-layer health check"
        - "Update AGENT-INDEX.md counts"
        - "Update all 6 prompts in .github/prompts/"
        - "Update all 22 agents in .github/agents/core/"
      refactor:
        - "Verify zero stale paths via grep sweep"
        - "Run refresh_prompt_suite.py --counts-only to validate"

    acceptance_criteria:
      - "python3 scripts/refresh_prompt_suite.py --counts-only — zero drift"
      - "grep -rn 'cortex_intelligence\\|cortex_lens\\|cortex.brain' .github/ → 0 hits (dissolved paths)"
      - "Every file path in copilot-instructions.md resolves to a real file"
      - "Every file path in cortex-architect.prompt.md resolves to a real file"
      - "AGENT-INDEX.md counts match live architecture"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE F: Audit-Fix Pipeline Hardening — E2E AC Verification
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-f"
    title: "E2E Audit Log Verification — AC_START→AC_COMPLETE Acceptance Tests for All 9 Stages"
    priority: P0
    status: COMPLETE
    gap_refs: ["GAP-107-12", "GAP-107-13"]
    governance_authority: "CORE-008, CORE-064, CORE-068"
    duration_estimate: "6 hours"
    depends_on: ["phase-107-e"]
    prerequisite: "phase-107-e completion_gate PASSED"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write E2E audit verification tests — must FAIL initially"
        gate: "ALL tests FAIL (AC markers not yet verified)"
        blocker: "No implementation until tests written and failing"
      green:
        action: "Implement AC marker verification infrastructure + fix emission gaps"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Clean up, add golden test for complete audit pipeline"
        gate: "Smoke + golden green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py smoke + python3 -c 'import sqlite3; ...' (Check #28)"
      fix_step: "Fix AC emission gaps"
      success_predicate: "ac_start_count > 0 and orphaned_ac_start == 0 and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_audit_stage_ac_markers_persisted — for each of 9 stages, trace_master has AC_START AND AC_COMPLETE rows"
        - "test_no_orphaned_ac_start — every AC_START has a matching AC_COMPLETE (zero orphans)"
        - "test_audit_stages_sequential — AC_START timestamps are monotonically increasing across stages"
        - "test_audit_fix_convergence_loop_traces — when auto-fix runs, detect→fix→rescan loop has ≥1 cycle traced"
        - "test_check28_machine_verifiable — Check #28 assertion runs as pytest, not manual sqlite3 command"
        - "test_each_audit_check_has_pytest — for checks #1-#28, a test file exists in tests/golden/ or tests/preflight/"
        - "test_audit_complete_emits_summary — final AC_COMPLETE includes p0_count, p1_count, stages_run"
        - "test_vacuum_stage_traces — vacuum operations emit AC markers to trace_master"
        - "test_health_check_stage_traces — health check operations emit AC markers"
      green:
        - "Create tests/golden/test_audit_pipeline_e2e_truth.py — end-to-end verification"
        - "Fix AC emission in any stage that silently drops markers"
        - "Create tests/golden/test_audit_checks_coverage.py — asserts each check has a test"
        - "Add summary metadata to final AC_COMPLETE (p0_count, p1_count, stages_run)"
      refactor:
        - "Add docstrings, clean up trace helper utilities"
        - "Verify zero orphaned AC_START via smoke gate"

    acceptance_criteria:
      - "tests/golden/test_audit_pipeline_e2e_truth.py — ALL GREEN"
      - "SELECT COUNT(*) FROM trace_master WHERE action LIKE 'AC_START%' → > 0 after any audit run"
      - "Zero orphaned AC_START (every start has complete)"
      - "Each audit check (#1-#28) maps to at least 1 pytest test"
      - "Audit convergence loop traces at least 1 detect→fix→rescan cycle"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE G: P0 Wiring Verification Gate
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-g"
    title: "P0 Wiring Verification — Spec↔Reality Alignment Gate for ALL Wiring YAMLs"
    priority: P0
    status: COMPLETE
    gap_refs: ["GAP-107-14", "GAP-107-15", "GAP-107-16"]
    governance_authority: "CORE-008, CORE-035, CORE-064"
    duration_estimate: "4 hours"
    depends_on: ["phase-107-f"]
    prerequisite: "phase-107-f completion_gate PASSED"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write wiring verification tests"
        gate: "ALL tests FAIL initially"
        blocker: "No implementation until tests fail"
      green:
        action: "Implement dynamic wiring YAML parser + verification"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Move to preflight tier, add to CI gate"
        gate: "Smoke + preflight green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py preflight"
      fix_step: "Fix any wiring misalignment"
      success_predicate: "wiring_alignment_score == 100% and new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_all_wiring_yaml_entry_points_importable — iterate ALL provides[].entry_point in 5 wiring YAMLs, assert each imports"
        - "test_all_wiring_yaml_classes_exist — for each entry_point 'module:ClassName', assert ClassName exists in module"
        - "test_all_wiring_yaml_health_check_present — every wired orchestrator has health_check() method"
        - "test_wiring_yaml_priority_uniqueness — no two orchestrators share same priority value"
        - "test_wiring_yaml_dependency_resolution — all dependencies[] references point to existing provides[]"
        - "test_mcp_registry_covers_all_registered_tools — len(registered) >= 29 and each tool function importable"
        - "test_mcp_tool_files_all_importable — every .py in cortex/mcp/tools/ imports without error"
        - "test_wiring_spec_orchestrator_count_matches_reality — sum of provides[] across all YAMLs ≥ count of actual orchestrator classes"
        - "test_no_phantom_wiring_entries — every wiring entry points to a real file (no stale entries)"
      green:
        - "Create tests/preflight/test_wiring_yaml_alignment.py — dynamic YAML-driven test"
        - "Parse all 5 wiring YAMLs, extract entry_points, validate each"
        - "Create tests/preflight/test_mcp_registry_alignment.py — registry vs files alignment"
        - "Implement wiring priority uniqueness check"
      refactor:
        - "Add to preflight tier so it runs on every audit"
        - "Add clear error messages for each failure type"

    acceptance_criteria:
      - "tests/preflight/test_wiring_yaml_alignment.py — ALL GREEN"
      - "Every provides[].entry_point in 5 wiring YAMLs is importable"
      - "Every wired class has health_check() method"
      - "Zero priority conflicts"
      - "MCP registry count matches importable tool count"
      - "preflight tier runs in <10s"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py preflight"
      min_tests_pass: 50
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE H: VacuumOrchestrator Migration Stages
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-h"
    title: "VacuumOrchestrator Enhancement — Migration Cleanup + Stale Import Scanner"
    priority: P1
    status: COMPLETE
    gap_refs: ["GAP-107-17", "GAP-107-18"]
    governance_authority: "CORE-008, CORE-064"
    duration_estimate: "4 hours"
    depends_on: ["phase-107-g"]
    prerequisite: "phase-107-g completion_gate PASSED"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write tests for new vacuum stages"
        gate: "ALL tests FAIL"
        blocker: "No implementation until tests fail"
      green:
        action: "Implement migration cleanup stages"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Clean up, add to vacuum pipeline"
        gate: "Smoke green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py smoke"
      fix_step: "Fix any vacuum pipeline regression"
      success_predicate: "new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_vacuum_run_compat_shim_detection — detects files that are only re-export shims (deprecated modules)"
        - "test_vacuum_run_stale_import_scanner — detects 'from cortex.lens.' imports in tests/ and scripts/ (post-migration)"
        - "test_vacuum_run_empty_init_cleanup — detects __init__.py files that are now empty after moves"
        - "test_vacuum_report_includes_migration_stage — VacuumReport has migration_shim_count field"
        - "test_vacuum_pipeline_order — migration stages execute AFTER build artifact cleanup"
        - "test_vacuum_dry_run_migration — dry_run=True plans but does not execute migration cleanup"
      green:
        - "Add run_compat_shim_detection() to VacuumOrchestrator — scans for deprecated re-export-only modules"
        - "Add run_stale_import_scanner() — grep-based detection of old import paths in non-source files"
        - "Add run_empty_init_cleanup() — detects and removes __init__.py files with only comments/pass"
        - "Wire new stages into run() pipeline (stages 9, 10, 11)"
      refactor:
        - "Update cortex-vacuum.md agent with new stages"
        - "Add docstrings, type hints"

    acceptance_criteria:
      - "VacuumOrchestrator.run() pipeline has 11 stages (was 8)"
      - "Migration stages detect stale cortex.lens imports"
      - "Migration stages detect compat-shim-only modules"
      - "Dry-run mode works for all new stages"
      - "cortex-vacuum.md updated with new pipeline"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # SUB-PHASE I: Production Readiness Check Expansion
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-i"
    title: "Production Readiness Check #29 — Intelligence Layer Health + Audit Table Sync"
    priority: P1
    status: COMPLETE
    gap_refs: ["GAP-107-19", "GAP-107-20"]
    governance_authority: "CORE-008, CORE-064"
    duration_estimate: "3 hours"
    depends_on: ["phase-107-h"]
    prerequisite: "phase-107-h completion_gate PASSED"
    regression_gate: "python3 scripts/run_tests.py smoke — zero new failures"

    tdd_cycle:
      red:
        action: "Write tests for Check #29"
        gate: "ALL tests FAIL"
        blocker: "No implementation until tests fail"
      green:
        action: "Implement Check #29 + update audit table"
        gate: "ALL tests PASS"
        blocker: "No REFACTOR until tests pass"
      refactor:
        action: "Update copilot-instructions.md audit table"
        gate: "Smoke green"
        blocker: "Complete when refactor gate passes"

    convergence_gate:
      detect_step: "python3 scripts/run_tests.py smoke"
      fix_step: "Fix any remaining audit table drift"
      success_predicate: "new_failures == 0"
      max_cycles: 3
      on_exhaustion: "Log"
      primitive_ref: "primitives/validation/detect-fix-rescan-loop"
      blocks_ac_complete: true
      governance: "CORE-068"

    tdd_sequence:
      red:
        - "test_check29_intelligence_facade_importable — IntelligenceFacade imports without error"
        - "test_check29_facade_analyze_callable — facade.analyze() returns valid result for sample"
        - "test_check29_synthesis_callable — UnifiedIntelligenceProvider.synthesize() returns context"
        - "test_check29_knowledge_yaml_paths_resolvable — all YAML paths in INDEX.yaml resolve"
        - "test_audit_table_29_checks — audit check table has exactly 29 entries (was 28)"
        - "test_audit_table_no_stale_paths — every file path in audit check table exists on disk"
      green:
        - "Create Check #29 implementation in audit pipeline"
        - "Add intelligence facade health to cortex-auditor.md"
        - "Update 28-point → 29-point in copilot-instructions.md and cortex-architect.prompt.md"
        - "Run refresh_prompt_suite.py for final sync"
      refactor:
        - "Verify all 29 checks have corresponding tests"
        - "Final path verification sweep"

    acceptance_criteria:
      - "Check #29 (Intelligence Layer Health) passes in audit pipeline"
      - "copilot-instructions.md says '29-Point Production Readiness Audit'"
      - "cortex-architect.prompt.md has Check #29 row"
      - "Every path in audit table resolves to real file"
      - "Smoke tests ≥1950, zero new failures"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  # ════════════════════════════════════════════════════════════════════════
  # FINAL: Smoke Gate + CORE-064 Close
  # ════════════════════════════════════════════════════════════════════════
  - id: "phase-107-final"
    title: "Smoke Gate + CORE-064 Sweep Close + cortex-master.yaml Update"
    priority: P0
    status: COMPLETE
    gap_refs: []
    depends_on: ["phase-107-i"]
    prerequisite: "ALL preceding sub-phase completion_gates PASSED"
    regression_gate: "python3 scripts/run_tests.py smoke — ≥1950 pass"

    tdd_cycle:
      red:
        action: "N/A — verification gate"
        gate: "All sweep_catalogue entries verified CLOSED"
        blocker: "No smoke gate if any GAP remains OPEN"
      green:
        action: "Run full smoke gate"
        gate: "python3 scripts/run_tests.py smoke"
        blocker: "Do NOT mark COMPLETE if smoke fails"
      refactor:
        action: "Move planned/ → completed/, update cortex-master.yaml"
        gate: "YAML valid, ≤500 lines"
        blocker: "Do NOT close without cortex-master.yaml update"

    acceptance_criteria:
      - "ALL 20 sweep_catalogue GAPs have status: CLOSED"
      - "python3 scripts/run_tests.py smoke passes ≥1950 tests"
      - "Zero new test failures vs 1950 baseline"
      - "phase-107 file moved: planned/ → completed/"
      - "cortex-master.yaml updated to status: COMPLETE"
      - "cortex-master.yaml ≤500 lines (THIN INDEX CONTRACT)"
      - "python3 scripts/refresh_prompt_suite.py --counts-only — zero drift"
      - "Intelligence files: ≤80 (was 379) — 79% reduction"
      - "Intelligence subdirectories: ≤12 (was 100) — 88% reduction"
      - "Synthesis engines: 1 (was 5) — 80% reduction"
      - "Entry point facades: 1 (was 3) — 67% reduction"
      - "All preflight wiring tests GREEN"
      - "E2E audit AC marker verification GREEN"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1950
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: false

# ─────────────────────────────────────────────────────────────────────────────
# AC MARKERS
# ─────────────────────────────────────────────────────────────────────────────
ac_markers:
  start: "AC_START: AC-107-INTELLIGENCE-CONSOLIDATION-{timestamp}"
  complete: "AC_COMPLETE: AC-107-INTELLIGENCE-CONSOLIDATION-{timestamp} ✅"

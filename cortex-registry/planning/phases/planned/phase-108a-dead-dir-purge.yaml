id: "phase-108a"
title: "Phase 108-A — cortex-registry Dead Dir Purge + Phase Hierarchy Unification"
version: "1.0"
created: "2026-03-03"
status: PLANNED
priority: P1
parent_phase: "phase-108"
governance_authority:
  - CORE-002  # No report files
  - CORE-035  # Single canonical location
  - CORE-064  # Sweep Completeness Contract
sweep_catalogue_id: "SWEEP-108A-DEAD-DIRS"

background: |
  Two immediate quick wins in cortex-registry/:
  
  1. EMPTY PLANS/ DIRECTORY:
     cortex-registry/plans/ contains only .gitkeep files (3). No actual content.
     Action: Delete cortex-registry/plans/ entirely.
  
  2. SPLIT PHASE HIERARCHY:
     Phase detail files live in TWO locations:
     a. cortex-registry/planning/phases/ (42 files — newer, Phase 100+ era)
     b. cortex-registry/_cortex-master/phases/ (56 files — older, Phase 1-89 era)
     
     Problem: When looking for a phase file, you have to check both locations.
     Action: Do NOT merge — the split is intentional (older phases stay in _cortex-master/).
     Instead: Add a README.md to cortex-registry/planning/ clarifying the two-tier structure.
  
  3. .YAML-READER RELOCATION:
     cortex-registry/.yaml-reader/ contains 25 bundled HTML/JS/CSS files (a YAML viewer app).
     It doesn't belong in the governance registry.
     Action: Move to scripts/.yaml-reader/ or cortex-docs/tools/yaml-reader/

scope:
  actions:
    - "Delete cortex-registry/plans/ (3 .gitkeep files)"
    - "Add planning/README.md with phase hierarchy documentation"
    - "Move .yaml-reader/ to cortex-docs/tools/yaml-reader/"
    - "Update any path references to .yaml-reader/"

estimated_effort: "1 hour"
risk: LOW

acceptance_criteria:
  - "cortex-registry/plans/ does not exist"
  - "cortex-registry/planning/README.md exists with phase hierarchy docs"
  - "cortex-registry/.yaml-reader/ does not exist"
  - "cortex-docs/tools/yaml-reader/ exists with moved content"
  - "All test suite paths resolve (no broken references)"
  - "Smoke tests: >= 2172 passed"

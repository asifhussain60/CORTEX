# ═══════════════════════════════════════════════════════════════════════════════
# Phase 73: Unified Brain + Toolkit — P0 Consolidated Sweep
# ═══════════════════════════════════════════════════════════════════════════════
# Authority:  cortex-master.yaml v20.0 (thin index) + CORE-064 (sweep completeness)
# Template:   cortex-registry/planning/phases/_template.yaml
# Created:    2026-02-25
# Status:     PLANNED
# Sweep ID:   SWEEP-73-UNIFIED-BRAIN-TOOLKIT
#
# CONSOLIDATES:
#   - phase-71-holistic-unified-brain.yaml   (33 GAPs, 10 sub-phases — SSOT)
#   - phase-toolkit-consolidation.yaml       (7 GAPs, 8 sub-phases — absorbed)
#   - phase-70-unified-brain-remediation.yaml (superseded by phase-71, archived)
#
# ABSORBED SWEEPS:
#   SWEEP-71-HOLISTIC-UNIFIED-BRAIN  — 24 open GAPs (GAP-71-B1..I1)
#   SWEEP-TOOLKIT-001                — 7 open GAPs  (GAP-001..GAP-007)
#   SWEEP-70-UNIFIED-BRAIN           — fully closed (Phase 70A); carried for CORE-064
#
# RATIONALE:
#   Toolkit GAPs (CORE-035 BatchProcessor dedup, HierarchicalScanner generic,
#   DomainAdapter protocol) are structural prerequisites for Phase-71 matrix wiring.
#   GAP-71-H (Audit Pipeline Integration) explicitly absorbs toolkit work.
#   Running them as separate phases introduces scheduling risk and duplicate test runs.
#   Consolidating into one sweep eliminates that risk and satisfies CORE-064.
# ═══════════════════════════════════════════════════════════════════════════════

id: phase-73
title: "Unified Brain + Toolkit — Intelligence Matrix Full Wiring, OPJ, Stack Toolkit"
priority: P0
status: PLANNED
sweep_id: SWEEP-73-UNIFIED-BRAIN-TOOLKIT
created: "2026-02-25"

# ─────────────────────────────────────────────────────────────────────────────
# SEQUENTIAL EXECUTION CONTRACT (CORE-008 + CORE-064 + WHOLE-PHASE-FIRST)
# ─────────────────────────────────────────────────────────────────────────────
sequential_execution_contract:
  policy: STRICT_SEQUENTIAL          # sub-phases A→B→C→D→E→F→G→H→I→J, each gate blocks next
  partial_completion_allowed: false  # phase-73 is NOT COMPLETE with open GAPs
  decomposition_allowed: false       # do NOT split across separate phases or sessions
  phase_atomic: true                 # run end-to-end for max ROI (31 GAPs closed in one sweep)
  gate_on_failure: HALT              # gate fails → stop, fix, rerun gate, then continue
  tdd_cycle_mandatory: true          # every implementation sub-phase runs RED→GREEN→REFACTOR
  resume_from: last_passed_gate      # if session pauses, resume from next sub-phase after last PASSED gate

absorbs:
  - id: SWEEP-71-HOLISTIC-UNIFIED-BRAIN
    source_file: "cortex-registry/planning/phases/planned/phase-71-holistic-unified-brain.yaml"
    gaps_absorbed: 24
    gaps_closed_in_source: 9
    note: "Phase-71 is the primary authority. GAP-71-A1..A5 already closed. GAP-71-I1 closed. 24 open GAPs imported as-is."
  - id: SWEEP-TOOLKIT-001
    source_file: "cortex-registry/planning/phases/planned/phase-toolkit-consolidation.yaml"
    gaps_absorbed: 7
    gaps_closed_in_source: 0
    note: "All 7 toolkit GAPs open. Renumbered GAP-74-T1..T7 for CORE-064 catalogue tracking."
  - id: SWEEP-70-UNIFIED-BRAIN
    source_file: "cortex-registry/planning/phases/planned/phase-70-unified-brain-remediation.yaml"
    gaps_absorbed: 0
    gaps_closed_in_source: 16
    note: "Fully closed in Phase 70A sessions. Archived to completed/. Referenced here for CORE-064 lineage."

# ─────────────────────────────────────────────────────────────────────────────
# LIVE MATRIX SNAPSHOT (source of truth — 2026-02-25)
# ─────────────────────────────────────────────────────────────────────────────
live_matrix_state:
  execution_date: "2026-02-25"
  dimensions: "15×15 → target 20×20"
  total_cells_current: 29
  wired: 2
  coverage_score: "6.9%"
  target_coverage: "≥80%"
  p0_critical_unwired: 7
  p1_high_unwired: 18
  gap_from_gate: "43.1 percentage points below 50% gate"

# ─────────────────────────────────────────────────────────────────────────────
# SWEEP CATALOGUE — CORE-064 Completeness Contract
# Total: 31 GAPs (24 from phase-71 + 7 from toolkit)
# ─────────────────────────────────────────────────────────────────────────────
sweep_catalogue:

  # ═══ TRACK A: CLOSED — Verified Phase 70A (do not re-execute) ═════════════
  - id: GAP-73-A1
    source: GAP-71-A1
    title: "IntentRouter identity collapse [CLOSED]"
    status: CLOSED
    closed_by: "Phase 70A"

  - id: GAP-73-A2
    source: GAP-71-A2
    title: "OPJ Writer workspace root path [CLOSED]"
    status: CLOSED
    closed_by: "Phase 70A"

  - id: GAP-73-A3
    source: GAP-71-A3
    title: "OPJ YAML serialization timeout [CLOSED]"
    status: CLOSED
    closed_by: "Phase 70A"

  - id: GAP-73-A4
    source: GAP-71-A4
    title: "Golden test failures — routing differentiation [CLOSED]"
    status: CLOSED
    closed_by: "Phase 70A"

  - id: GAP-73-A5
    source: GAP-71-A5
    title: "Golden test failures — base class convergence [CLOSED]"
    status: CLOSED
    closed_by: "Phase 70A"

  - id: GAP-73-A6
    source: GAP-71-I1
    title: "SQLite convergence signals wired into AuditIntelligenceFeedback [CLOSED]"
    status: CLOSED
    closed_by: "Phase 71 tracking (pre-closed)"

  # ═══ TRACK B: OPJ + ORCHESTRATOR WIRING (P0) ═════════════════════════════
  - id: GAP-73-B1
    source: GAP-71-B1
    title: "MasterOrchestrator is pattern-blind — OPJMixin not inherited"
    severity: P0
    status: OPEN
    fix: "Add OPJMixin to MasterOrchestrator; call _opj_consult() in coordinate_operation()"
    file: "cortex/orchestrators/core/master_orchestrator.py"
    test: "tests/golden/intelligence/test_unified_brain_opj_golden.py::test_master_orchestrator_opj_consult"

  - id: GAP-73-B2
    source: GAP-71-B2
    title: "IntentClassifier never records outcomes to OPJ — no feedback loop"
    severity: P1
    status: OPEN
    fix: "Wire IntentRouter._opj_record_success/failure() after each routing decision"
    file: "cortex/orchestrators/core/intent_router.py"
    test: "tests/golden/intelligence/test_unified_brain_opj_golden.py::test_intent_router_opj_record"

  - id: GAP-73-B3
    source: GAP-71-B3
    title: "GovernanceRegistry not consulted at routing time — violations ignored"
    severity: P1
    status: OPEN
    fix: "Wire GovernanceRegistry.get_active_violations() → IntentRouter complexity_score"
    file: "cortex/orchestrators/core/intent_router.py"

  # ═══ TRACK C: P0 MATRIX CELL WIRING (7 critical pairs) ═══════════════════
  - id: GAP-73-C1
    source: GAP-71-C1
    title: "IC-001 × CC-001: LENS Analysis / HierarchicalScanner unwired"
    severity: P0
    status: OPEN
    fix: "Wire HierarchicalScanner.scan() output → LENS.analyze(files) pipeline"

  - id: GAP-73-C2
    source: GAP-71-C2
    title: "IC-004 × CC-008: BrainTier-T1-Learned / MCPToolRegistry unwired"
    severity: P0
    status: OPEN
    fix: "Expose T1/T2/T3 memory query as cortex_brain_query MCP tool"

  - id: GAP-73-C3
    source: GAP-71-C3
    title: "IC-007 × CC-001: IntelligenceOrchestrator / HierarchicalScanner unwired"
    severity: P0
    status: OPEN
    fix: "Wire HierarchicalScanner.scan() into IntelligenceOrchestrator.analyze()"

  - id: GAP-73-C4
    source: GAP-71-C4
    title: "IC-008 × CC-008: ResponseTemplateGenerator / MCPToolRegistry unwired"
    severity: P0
    status: OPEN
    fix: "Wrap all MCP tool results through format_response() before returning"

  - id: GAP-73-C5
    source: GAP-71-C5
    title: "IC-010 × CC-004: KnowledgeIndexer / DocGenPlaybook unwired"
    severity: P0
    status: OPEN
    fix: "Wire KnowledgeIndexer.inventory() → DocGenPlaybook stage_1.knowledge_yaml_scan"

  - id: GAP-73-C6
    source: GAP-71-C6
    title: "IC-012 × CC-004: KnowledgeIndexerDocGenBridge / DocGenPlaybook unwired"
    severity: P0
    status: OPEN
    fix: "Wire KnowledgeIndexerDocGenBridge → DocGenPlaybook stage_1"

  - id: GAP-73-C7
    source: GAP-71-F3
    title: "IC-014 × CC-008: CortexBrainQuery / MCPToolRegistry — tool not registered"
    severity: P0
    status: OPEN
    fix: "Register cortex_brain_query MCP tool in MCPToolRegistry"

  # ═══ TRACK D: P1 MATRIX CELL WIRING (18 pairs, 4 clusters) ═══════════════
  - id: GAP-73-D1
    source: GAP-71-D1
    title: "P1 cluster 1 — LENS/BatchProcessor + SynthesisEngine/SweepCatalogue (4 pairs)"
    severity: P1
    status: OPEN
    pairs: ["IC-001×CC-002", "IC-002×CC-009", "IC-002×CC-011", "IC-003×CC-009"]

  - id: GAP-73-D2
    source: GAP-71-D2
    title: "P1 cluster 2 — DomainBrain + BrainTier-T2 wiring (4 pairs)"
    severity: P1
    status: OPEN
    pairs: ["IC-003×CC-011", "IC-004×CC-003", "IC-005×CC-002", "IC-005×CC-005"]

  - id: GAP-73-D3
    source: GAP-71-D3
    title: "P1 cluster 3 — BlindSpotDetector wiring (4 pairs)"
    severity: P1
    status: OPEN
    pairs: ["IC-009×CC-005", "IC-009×CC-010", "IC-009×CC-013", "IC-008×CC-002"]

  - id: GAP-73-D4
    source: GAP-71-D4
    title: "P1 cluster 4 — KnowledgeIndexer + FormatResponseHook (6 pairs)"
    severity: P1
    status: OPEN
    pairs: ["IC-010×CC-002", "IC-010×CC-005", "IC-010×CC-009", "IC-010×CC-011", "IC-015×CC-002", "IC-015×CC-005"]

  # ═══ TRACK E: 20×20 MATRIX EXTENSION (5 new IC + 5 new CC) ════════════════
  - id: GAP-73-E1
    source: GAP-71-E1
    title: "Matrix extension: IC-016..IC-020 (5 new intelligence capabilities)"
    severity: P1
    status: OPEN
    new_x: ["IC-016 CompanyKnowledgeProvider", "IC-017 InfrastructureObservability",
            "IC-018 GovernanceRuleIntelligence", "IC-019 MemoryTierMigrator",
            "IC-020 AuditIntelligenceFeedback"]

  - id: GAP-73-E2
    source: GAP-71-E2
    title: "Matrix extension: CC-016..CC-020 (5 new cross-cutting capabilities)"
    severity: P1
    status: OPEN
    new_y: ["CC-016 OnboardedRepoRegistry", "CC-017 HealthCheckPipeline",
            "CC-018 LiveRuleValidator", "CC-019 CrossTierKnowledgeFlow",
            "CC-020 AuditFixConvergenceSignals"]

  # ═══ TRACK F: EXPANDED SCOPE (company knowledge + memory tiers) ════════════
  - id: GAP-73-F1
    source: GAP-71-F1
    title: "CompanyKnowledgeProvider not feeding LENS context"
    severity: P1
    status: OPEN
    fix: "CompanyKnowledgeProvider.load() → LENS analyze() context enrichment"
    file: "cortex/intelligence/knowledge/company_domain_loader.py"

  - id: GAP-73-F2
    source: GAP-71-F2
    title: "OPJ patterns never auto-promote to T1 learned (memory silo)"
    severity: P1
    status: OPEN
    fix: "OPJReader high-confidence entries (>0.80) auto-promoted → knowledge_retention_store"
    file: "cortex/intelligence/memory/"

  # ═══ TRACK G: GOLDEN TESTS DELIVERY (TDD RED gate) ════════════════════════
  - id: GAP-73-G1
    source: GAP-71-G1
    title: "36 golden intelligence tests missing (TDD RED phase)"
    severity: P0
    status: OPEN
    files:
      - "tests/golden/intelligence/test_intelligence_matrix_golden.py (24 tests)"
      - "tests/golden/intelligence/test_unified_brain_opj_golden.py (12 tests)"
    note: "Must be written RED before any implementation. CORE-008."

  - id: GAP-73-G2
    source: GAP-71-G2
    title: "6 golden test extensions for expanded scope (IC-016..IC-020)"
    severity: P1
    status: OPEN
    file: "tests/golden/intelligence/test_intelligence_matrix_golden.py"

  # ═══ TRACK H: AUDIT + TOOLKIT INTEGRATION ════════════════════════════════
  - id: GAP-73-H1
    source: GAP-71-H1
    title: "Check #20 (Intelligence Matrix health) missing from /audit fix Stage 2"
    severity: P1
    status: OPEN
    fix: "Add Check #20 to cortex-auditor.md and AuditCoordinator Stage 2"

  - id: GAP-73-H2
    source: GAP-71-H2
    title: "5 pre-existing unit test failures unresolved"
    severity: P1
    status: OPEN
    file: "tests/unit/core/test_phase_68_core_flatten.py"

  # ═══ TRACK T: TOOLKIT CONSOLIDATION (absorbed from SWEEP-TOOLKIT-001) ═════
  - id: GAP-73-T1
    source: "GAP-001 (SWEEP-TOOLKIT-001)"
    title: "MediaScanner is media-specific — should be generic HierarchicalScanner"
    severity: P1
    status: OPEN
    file: "cortex/tools/media/media_scanner.py"
    fix: "Extract generic HierarchicalScanner into cortex/toolkit/filesystem/"

  - id: GAP-73-T2
    source: "GAP-002 (SWEEP-TOOLKIT-001)"
    title: "4 duplicate BatchProcessor implementations violate CORE-035"
    severity: P0
    status: OPEN
    files:
      - "tests/unit/phase4/test_brt027_performance_optimization.py"
      - "cortex/core/knowledge/bulk_ingestion.py"
      - "cortex/lens/lens_tiered_mcp_api.py"
      - "cortex/testing/framework/ (CortexXdistPlugin)"
    fix: "Canonical BatchProcessor in cortex/toolkit/batch/; all others import from it"

  - id: GAP-73-T3
    source: "GAP-003 (SWEEP-TOOLKIT-001)"
    title: "FilenameAnalyzer domain-locked to media"
    severity: P1
    status: OPEN
    file: "cortex/tools/media/filename_sanitizer.py"
    fix: "Extract generic FilenameIntelligence with pattern config injection"

  - id: GAP-73-T4
    source: "GAP-004 (SWEEP-TOOLKIT-001)"
    title: "SafeFileRenamer Python-only — no adapter protocol for TS/C#"
    severity: P2
    status: OPEN
    file: "cortex/tools/safe_file_rename.py"
    fix: "DomainAdapter ABC in cortex/toolkit/adapters/"

  - id: GAP-73-T5
    source: "GAP-005 (SWEEP-TOOLKIT-001)"
    title: "No generic DomainAdapter protocol for pluggable toolkit extensions"
    severity: P1
    status: OPEN
    fix: "cortex/toolkit/adapters/domain_adapter.py — ABC + 2 concrete adapters"

  - id: GAP-73-T6
    source: "GAP-006 (SWEEP-TOOLKIT-001)"
    title: "Workflow orchestration pattern duplicated across 7+ orchestrators"
    severity: P1
    status: OPEN
    files:
      - "cortex/orchestrators/support/plex_workflow_orchestrator.py"
      - "cortex/orchestrators/support/digest_session_orchestrator.py"
      - "cortex/orchestrators/core/audit_coordinator.py"
    fix: "Generic WorkflowOrchestrator base in cortex/toolkit/workflow/"

  - id: GAP-73-T7
    source: "GAP-007 (SWEEP-TOOLKIT-001)"
    title: "No MCP tools exposing toolkit operations"
    severity: P2
    status: OPEN
    fix: "cortex_toolkit MCP tool with ops: scan, batch, rename, workflow_status"

# ─────────────────────────────────────────────────────────────────────────────
# SEQUENTIAL EXECUTION CONTRACT (CORE-008 + CORE-064)
# Sub-phases execute STRICTLY SEQUENTIALLY. Each sub-phase must pass its
# completion_gate before the next sub-phase may begin. No sub-phase may be
# marked COMPLETE with open GAPs or without completing its full RED→GREEN→REFACTOR loop.
# ─────────────────────────────────────────────────────────────────────────────
sequential_execution_contract:
  enforced_by: "CORE-008 (TDD) + CORE-064 (Sweep Completeness)"
  rule: "Each sub-phase runs to COMPLETE before the next begins — no parallel sub-phase execution"
  tdd_loop_required: true
  completion_gate_required_on_every_sub_phase: true
  smoke_gate_on_final_sub_phase: true

# ─────────────────────────────────────────────────────────────────────────────
# SUB-PHASES — Ordered execution (CORE-008: RED before GREEN)
# ─────────────────────────────────────────────────────────────────────────────
sub_phases:

  - id: phase-73-A
    title: "Verify Closed Blockers + Smoke Baseline"
    status: COMPLETE
    gaps: [GAP-73-A1, GAP-73-A2, GAP-73-A3, GAP-73-A4, GAP-73-A5, GAP-73-A6]
    depends_on: []
    action: "python3 scripts/run_tests.py smoke — confirm ≥1420 passing"
    note: "All 6 closed in Phase 70A. Run smoke to set baseline before any new work."
    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1420
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-B
    title: "TDD RED — Write All 42 Golden Tests (CORE-008 gate)"
    priority: P0
    status: PLANNED
    gaps: [GAP-73-G1, GAP-73-G2]
    depends_on: [phase-73-A]
    prerequisite: "phase-73-A completion_gate PASSED (≥1420 smoke)"
    deliverables:
      - "tests/golden/intelligence/test_intelligence_matrix_golden.py (24 tests — all RED)"
      - "tests/golden/intelligence/test_unified_brain_opj_golden.py (12 tests — all RED)"
      - "6 extension tests in test_intelligence_matrix_golden.py (RED)"

    tdd_cycle:
      red:
        action: "Write all 42 golden tests as listed in tdd_sequence.red — NO implementation code"
        gate: "python3 scripts/run_tests.py golden — ALL 42 new tests must FAIL (import errors count as FAIL)"
        blocker: "Implementation of any intelligence/toolkit code is FORBIDDEN until this gate passes"
      green:
        action: "N/A — this sub-phase is RED-only; GREEN phase is phase-73-I"
        gate: "N/A"
        blocker: "N/A"
      refactor:
        action: "N/A — test files only; no implementation to refactor"
        gate: "N/A"
        blocker: "N/A"

    tdd_sequence:
      red:
        - "test_intelligence_matrix_golden.py — 24 tests: IC-001..IC-015 × CC-001..CC-015 P0 pairs"
        - "test_unified_brain_opj_golden.py — 12 tests: OPJ consult, record, promote, memory silo"
        - "test_intelligence_matrix_golden.py extensions — 6 tests: IC-016..IC-020 × CC-016..CC-020"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py golden"
      min_tests_pass: 0
      new_tests_written: 42
      all_new_tests_failing: true
      zero_new_failures_in_existing_suite: true
      all_gap_refs_closed: false
      note: "GAP-73-G1 and GAP-73-G2 close in phase-73-I; this gate confirms 42 RED tests exist"
      blocks_next_sub_phase: true

  - id: phase-73-C
    title: "Toolkit Foundation + BatchProcessor Dedup (P0 CORE-035 fix)"
    priority: P0
    status: PLANNED
    gaps: [GAP-73-T1, GAP-73-T2, GAP-73-T3, GAP-73-T5]
    depends_on: [phase-73-B]
    prerequisite: "phase-73-B completion_gate PASSED (42 RED tests confirmed)"
    deliverables:
      - "cortex/toolkit/ directory with 6 subdirs"
      - "cortex/toolkit/batch/batch_processor.py (canonical)"
      - "cortex/toolkit/filesystem/hierarchical_scanner.py"
      - "cortex/toolkit/adapters/domain_adapter.py (ABC)"

    tdd_cycle:
      red:
        action: "Write all tests in tdd_sequence.red before creating any toolkit module"
        gate: "python3 scripts/run_tests.py file tests/unit/toolkit/ — ALL tests FAIL"
        blocker: "No toolkit source files may be created until RED gate passes"
      green:
        action: "Create cortex/toolkit/ structure; implement canonical BatchProcessor, HierarchicalScanner, DomainAdapter ABC"
        gate: "python3 scripts/run_tests.py file tests/unit/toolkit/ — ALL tests PASS"
        blocker: "REFACTOR is forbidden until GREEN gate passes"
      refactor:
        action: "Add type hints (CORE-011), docstrings (CORE-012); verify all 4 former BatchProcessor callers import from canonical (CORE-035)"
        gate: "python3 scripts/run_tests.py dir tests/unit/ — zero regressions"
        blocker: "Sub-phase COMPLETE is forbidden until REFACTOR gate passes"

    tdd_sequence:
      red:
        - "test_canonical_batch_processor_importable — from cortex.toolkit.batch import BatchProcessor"
        - "test_batch_processor_replaces_bulk_ingestion_caller — cortex.core.knowledge.bulk_ingestion uses canonical"
        - "test_batch_processor_replaces_lens_tiered_caller — cortex.lens.lens_tiered_mcp_api uses canonical"
        - "test_hierarchical_scanner_generic_interface — scanner accepts any file type, not media-specific"
        - "test_hierarchical_scanner_replaces_media_scanner — old MediaScanner delegates or is removed"
        - "test_domain_adapter_abc_enforces_contract — subclass without adapt() raises TypeError"
        - "test_toolkit_init_exports_canonical_names — cortex.toolkit exports BatchProcessor, HierarchicalScanner, DomainAdapter"
      green:
        - "Create cortex/toolkit/__init__.py, batch/, filesystem/, adapters/ subdirs"
        - "Implement BatchProcessor in cortex/toolkit/batch/batch_processor.py"
        - "Implement HierarchicalScanner in cortex/toolkit/filesystem/hierarchical_scanner.py"
        - "Implement DomainAdapter ABC in cortex/toolkit/adapters/domain_adapter.py"
        - "Update 4 former BatchProcessor callers to import from cortex.toolkit.batch"
      refactor:
        - "Full type hints on all public methods (CORE-011)"
        - "Module docstrings on all toolkit modules (CORE-012)"
        - "Verify CORE-035: grep for remaining BatchProcessor definitions outside canonical"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py dir tests/unit/toolkit/"
      min_tests_pass: 7
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-D
    title: "OPJ Wiring — MasterOrchestrator + IntentRouter (apex P0 fix)"
    priority: P0
    status: PLANNED
    gaps: [GAP-73-B1, GAP-73-B2, GAP-73-B3]
    depends_on: [phase-73-B]
    prerequisite: "phase-73-B completion_gate PASSED (42 RED tests exist)"
    note: "Can start in parallel with phase-73-C (both depend only on 73-B). However phase-73-E requires BOTH 73-C and 73-D complete."
    deliverables:
      - "MasterOrchestrator inherits OPJMixin, calls _opj_consult()"
      - "IntentRouter records all routing decisions to OPJ"
      - "GovernanceRegistry.get_active_violations() → IntentRouter complexity_score"

    tdd_cycle:
      red:
        action: "Write OPJ wiring tests before touching MasterOrchestrator or IntentRouter"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_unified_brain_opj_golden.py — OPJ-related tests FAIL"
        blocker: "MasterOrchestrator and IntentRouter must NOT be modified until RED gate passes"
      green:
        action: "Add OPJMixin to MasterOrchestrator; wire _opj_consult() and _opj_record_success/failure() into IntentRouter"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_unified_brain_opj_golden.py — OPJ tests PASS"
        blocker: "REFACTOR is forbidden until GREEN gate passes"
      refactor:
        action: "Type hints on all new OPJ methods; validate no regression in existing orchestrator tests"
        gate: "python3 scripts/run_tests.py dir tests/orchestrators/core/ — zero regressions"
        blocker: "Sub-phase COMPLETE forbidden until REFACTOR gate passes"

    tdd_sequence:
      red:
        - "test_master_orchestrator_opj_consult — MasterOrchestrator._opj_consult() called in coordinate_operation()"
        - "test_intent_router_opj_record_success — OPJ entry written after successful routing"
        - "test_intent_router_opj_record_failure — OPJ entry written after failed routing"
        - "test_governance_violations_feed_complexity_score — get_active_violations() influences complexity_score"
      green:
        - "Import OPJMixin in master_orchestrator.py; add to class bases"
        - "Call self._opj_consult() at start of coordinate_operation()"
        - "Add _opj_record_success/failure() calls in intent_router.py route() method"
        - "Wire GovernanceRegistry.get_active_violations() into complexity_score calculation"
      refactor:
        - "Docstrings on all new OPJ integration methods (CORE-012)"
        - "Type hints on _opj_consult(), _opj_record_success(), _opj_record_failure() (CORE-011)"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py dir tests/orchestrators/core/"
      min_tests_pass: 4
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-E
    title: "P0 Matrix Cell Wiring — 7 Critical Pairs"
    priority: P0
    status: PLANNED
    gaps: [GAP-73-C1, GAP-73-C2, GAP-73-C3, GAP-73-C4, GAP-73-C5, GAP-73-C6, GAP-73-C7]
    depends_on: [phase-73-C, phase-73-D]
    prerequisite: "phase-73-C completion_gate PASSED AND phase-73-D completion_gate PASSED"
    note: "Coverage: 6.9% → ~31% after this sub-phase."

    tdd_cycle:
      red:
        action: "Write matrix cell wiring tests (IC×CC pairs) — from test_intelligence_matrix_golden.py RED suite written in phase-73-B"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_intelligence_matrix_golden.py — 7 P0 cell tests FAIL"
        blocker: "No wiring code written until gate confirms 7 P0 tests are RED"
      green:
        action: "Wire all 7 P0 matrix cell pairs (GAP-73-C1..C7) as specified in sweep_catalogue"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_intelligence_matrix_golden.py — 7 P0 cell tests PASS"
        blocker: "REFACTOR forbidden until all 7 P0 tests GREEN"
      refactor:
        action: "Type hints on all wiring methods; validate LENS pipeline still imports cleanly"
        gate: "python3 scripts/run_tests.py dir tests/intelligence/ — zero regressions"
        blocker: "Sub-phase COMPLETE forbidden until REFACTOR gate passes"

    tdd_sequence:
      red:
        - "test_lens_hierarchical_scanner_wired — HierarchicalScanner.scan() feeds LENS.analyze() (IC-001×CC-001)"
        - "test_brain_tier_t1_mcp_registry_wired — T1/T2/T3 query exposed as cortex_brain_query (IC-004×CC-008)"
        - "test_intelligence_orchestrator_hierarchical_scanner_wired — HierarchicalScanner in IntelligenceOrchestrator (IC-007×CC-001)"
        - "test_response_template_generator_mcp_registry_wired — all MCP results through format_response() (IC-008×CC-008)"
        - "test_knowledge_indexer_docgen_playbook_wired — KnowledgeIndexer.inventory() → DocGenPlaybook stage_1 (IC-010×CC-004)"
        - "test_knowledge_indexer_bridge_docgen_wired — KnowledgeIndexerDocGenBridge → DocGenPlaybook stage_1 (IC-012×CC-004)"
        - "test_cortex_brain_query_mcp_tool_registered — cortex_brain_query appears in MCP registry (IC-014×CC-008)"
      green:
        - "Wire HierarchicalScanner.scan() into LENS.analyze() pipeline (GAP-73-C1)"
        - "Expose T1/T2/T3 brain query as cortex_brain_query MCP tool (GAP-73-C2, GAP-73-C7)"
        - "Wire HierarchicalScanner into IntelligenceOrchestrator.analyze() (GAP-73-C3)"
        - "Wrap MCP tool results through format_response() (GAP-73-C4)"
        - "Wire KnowledgeIndexer.inventory() → DocGenPlaybook stage_1 (GAP-73-C5, GAP-73-C6)"
      refactor:
        - "Full type hints on all wiring integration points (CORE-011)"
        - "Verify python3 -c 'from cortex.lens import *' — no import errors"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py dir tests/intelligence/"
      min_tests_pass: 7
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-F
    title: "P1 Matrix Cell Wiring — 18 High-Priority Pairs (4 clusters)"
    priority: P1
    status: PLANNED
    gaps: [GAP-73-D1, GAP-73-D2, GAP-73-D3, GAP-73-D4]
    depends_on: [phase-73-E]
    prerequisite: "phase-73-E completion_gate PASSED (~31% matrix coverage confirmed)"
    note: "Coverage: ~31% → ~93% (27 of 29 cells wired). Gate: PASS ≥50%."

    tdd_cycle:
      red:
        action: "Confirm 18 P1 cluster tests are RED from test_intelligence_matrix_golden.py"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_intelligence_matrix_golden.py — P1 cluster tests FAIL"
        blocker: "No P1 wiring code written until RED confirmation"
      green:
        action: "Wire all 18 P1 matrix cell pairs across 4 clusters (GAP-73-D1..D4)"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_intelligence_matrix_golden.py — P1 cluster tests PASS; coverage ≥50%"
        blocker: "REFACTOR forbidden until ≥50% coverage gate passes"
      refactor:
        action: "Remove any intermediate scaffolding; consolidate wiring helpers; type hints on all new code"
        gate: "python3 scripts/run_tests.py dir tests/intelligence/ — zero regressions"
        blocker: "Sub-phase COMPLETE forbidden until REFACTOR gate passes"

    tdd_sequence:
      red:
        - "test_d1_cluster_1_lens_batch_synthesis — 4 P1 pairs: LENS/BatchProcessor + SynthesisEngine/SweepCatalogue"
        - "test_d2_cluster_2_domain_brain_t2_wiring — 4 P1 pairs: DomainBrain + BrainTier-T2"
        - "test_d3_cluster_3_blind_spot_detector — 4 P1 pairs: BlindSpotDetector wiring"
        - "test_d4_cluster_4_knowledge_indexer_format — 6 P1 pairs: KnowledgeIndexer + FormatResponseHook"
      green:
        - "Wire cluster 1: LENS/BatchProcessor + SynthesisEngine/SweepCatalogue (GAP-73-D1)"
        - "Wire cluster 2: DomainBrain + BrainTier-T2 (GAP-73-D2)"
        - "Wire cluster 3: BlindSpotDetector (GAP-73-D3)"
        - "Wire cluster 4: KnowledgeIndexer + FormatResponseHook (GAP-73-D4)"
      refactor:
        - "Verify matrix.coverage_score ≥ 50% — this is the hard numeric gate for the phase"
        - "Type hints + docstrings on all 18 new integration points"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py dir tests/intelligence/"
      min_tests_pass: 18
      matrix_coverage_minimum: "50%"
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-G
    title: "20×20 Matrix Extension + CompanyKnowledge + Memory Tier Promotion"
    priority: P1
    status: PLANNED
    gaps: [GAP-73-E1, GAP-73-E2, GAP-73-F1, GAP-73-F2]
    depends_on: [phase-73-E]
    prerequisite: "phase-73-E completion_gate PASSED (7 P0 cells wired)"
    note: "Extend matrix to 20×20. Wire OPJ→T1 promotion. CompanyKnowledge→LENS. Can run after 73-E (does not depend on 73-F)."

    tdd_cycle:
      red:
        action: "Write extension tests for IC-016..IC-020 and CC-016..CC-020 axes; OPJ promotion test; CompanyKnowledge test"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_intelligence_matrix_golden.py — extension tests FAIL"
        blocker: "No extension implementation until RED gate passes"
      green:
        action: "Register IC-016..IC-020 and CC-016..CC-020; wire CompanyKnowledgeProvider→LENS; wire OPJ→T1 auto-promotion"
        gate: "python3 scripts/run_tests.py file tests/golden/intelligence/test_intelligence_matrix_golden.py — extension tests PASS"
        blocker: "REFACTOR forbidden until GREEN gate passes"
      refactor:
        action: "Type hints on all new capability registrations; validate OPJ promotion threshold (>0.80 confidence)"
        gate: "python3 scripts/run_tests.py dir tests/intelligence/ — zero regressions"
        blocker: "Sub-phase COMPLETE forbidden until REFACTOR gate passes"

    tdd_sequence:
      red:
        - "test_matrix_extended_ic016_to_ic020_registered — 5 new IC axes present in matrix"
        - "test_matrix_extended_cc016_to_cc020_registered — 5 new CC axes present in matrix"
        - "test_company_knowledge_feeds_lens_context — CompanyKnowledgeProvider.load() enriches LENS context"
        - "test_opj_high_confidence_promotes_to_t1 — OPJ entry with confidence >0.80 auto-promotes to knowledge_retention_store"
      green:
        - "Register IC-016..IC-020 as new X-axis entries in intelligence matrix"
        - "Register CC-016..CC-020 as new Y-axis entries in intelligence matrix"
        - "Wire CompanyKnowledgeProvider.load() into LENS analyze() context enrichment"
        - "Wire OPJReader high-confidence entries → knowledge_retention_store in memory/"
      refactor:
        - "Validate matrix now has 400 possible pairs (20×20)"
        - "Ensure OPJ promotion is idempotent (running twice produces same result)"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py dir tests/intelligence/"
      min_tests_pass: 4
      matrix_dimensions: "20×20"
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-H
    title: "Audit Integration + Remaining Toolkit + Unit Test Fix"
    priority: P1
    status: PLANNED
    gaps: [GAP-73-H1, GAP-73-H2, GAP-73-T4, GAP-73-T6, GAP-73-T7]
    depends_on: [phase-73-F]
    prerequisite: "phase-73-F completion_gate PASSED (≥50% matrix coverage)"
    deliverables:
      - "Check #20 added to /audit fix Stage 2 (cortex-auditor.md)"
      - "cortex/toolkit/workflow/workflow_orchestrator.py (generic base)"
      - "cortex_toolkit MCP tool registered"
      - "5 pre-existing unit test failures fixed"

    tdd_cycle:
      red:
        action: "Write tests for: Check #20 audit integration; WorkflowOrchestrator base; cortex_toolkit MCP tool; 5 failing unit tests"
        gate: "python3 scripts/run_tests.py unit — 5 pre-existing failures still fail PLUS new tests fail"
        blocker: "No implementation until RED gate passes"
      green:
        action: "Add Check #20 to AuditCoordinator Stage 2; implement WorkflowOrchestrator base; register cortex_toolkit MCP tool; fix 5 unit test failures"
        gate: "python3 scripts/run_tests.py unit — ALL tests pass including 5 previously failing"
        blocker: "REFACTOR forbidden until GREEN gate passes"
      refactor:
        action: "Type hints and docstrings on WorkflowOrchestrator base; verify cortex_toolkit appears in cortex_tools_catalog"
        gate: "python3 scripts/run_tests.py dir tests/unit/ — zero regressions"
        blocker: "Sub-phase COMPLETE forbidden until REFACTOR gate passes"

    tdd_sequence:
      red:
        - "test_audit_check_20_intelligence_matrix_health — Check #20 present in AuditCoordinator Stage 2"
        - "test_workflow_orchestrator_base_contract — WorkflowOrchestrator base class enforces execute() abstract method"
        - "test_cortex_toolkit_mcp_tool_registered — cortex_toolkit appears in tools catalog"
        - "test_cortex_toolkit_scan_op — scan operation calls HierarchicalScanner"
        - "test_cortex_toolkit_batch_op — batch operation calls canonical BatchProcessor"
        - "5 × fix existing test failures in tests/unit/core/test_phase_68_core_flatten.py"
      green:
        - "Add matrix health check to AuditCoordinator (GAP-73-H1)"
        - "Implement cortex/toolkit/workflow/workflow_orchestrator.py (GAP-73-T6)"
        - "Implement cortex/mcp/tools/cortex_toolkit.py with 4 ops (GAP-73-T7)"
        - "Implement SafeFileRenamer DomainAdapter delegation (GAP-73-T4)"
        - "Fix 5 pre-existing failures in test_phase_68_core_flatten.py (GAP-73-H2)"
      refactor:
        - "Ensure SafeFileRenamer uses DomainAdapter ABC (not ad-hoc conditionals)"
        - "Validate cortex_toolkit follows ConsolidatedTool pattern"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py unit"
      min_tests_pass: 0
      zero_new_failures: true
      pre_existing_failures_fixed: 5
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-I
    title: "GREEN Phase — Turn All 42 Golden Tests Green"
    priority: P0
    status: PLANNED
    gaps: [GAP-73-G1, GAP-73-G2]
    depends_on: [phase-73-D, phase-73-E, phase-73-F, phase-73-G, phase-73-H]
    prerequisite: "ALL of phase-73-D, 73-E, 73-F, 73-G, 73-H completion_gates PASSED"
    note: "All 42 golden tests must pass. Matrix coverage ≥80%. CORE-064 catalogue review."

    tdd_cycle:
      red:
        action: "Confirm all 42 golden tests still in RED/FAIL state before any further green work (state verification)"
        gate: "python3 scripts/run_tests.py golden — exactly 42 tests in this file FAIL, no more"
        blocker: "If any of the 42 tests already pass from prior sub-phases, record them; remaining must be confirmed RED"
      green:
        action: "Implement any remaining wiring needed to turn all 42 golden tests GREEN"
        gate: "python3 scripts/run_tests.py golden — ALL 42 new golden tests PASS; previous golden suite ≥947 passing"
        blocker: "REFACTOR forbidden until ALL 42 golden tests GREEN with no regressions"
      refactor:
        action: "Review any shortcuts taken in green phase; add type hints and docstrings; validate matrix.coverage_score ≥80%"
        gate: "python3 scripts/run_tests.py dir tests/golden/ — zero regressions in entire golden suite"
        blocker: "Sub-phase COMPLETE forbidden until matrix.coverage_score ≥80% confirmed"

    tdd_sequence:
      red:
        - "State verification: python3 scripts/run_tests.py golden — count RED tests = 42"
      green:
        - "Wire any remaining IC×CC pairs needed to satisfy outstanding golden test assertions"
        - "Verify matrix.coverage_score ≥80% via matrix status MCP tool"
        - "Verify OPJ→T1 promotion pipeline functional (confidence ≥0.80)"
      refactor:
        - "Sweep all new code for missing type hints (CORE-011)"
        - "Sweep all new public APIs for missing docstrings (CORE-012)"
        - "Run CORE-035 check: no duplicate intelligence provider implementations"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py golden"
      min_tests_pass: 42
      golden_suite_no_regressions: true
      matrix_coverage_minimum: "80%"
      zero_new_failures: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: true

  - id: phase-73-J
    title: "Smoke Gate + CORE-064 Close + cortex-master.yaml update"
    priority: P0
    status: PLANNED
    gaps: []
    depends_on: [phase-73-I]
    prerequisite: "phase-73-I completion_gate PASSED (42 golden GREEN, matrix ≥80%)"

    tdd_cycle:
      red:
        action: "N/A — this is the sweep closure and smoke verification sub-phase"
        gate: "N/A"
        blocker: "N/A"
      green:
        action: "Run full smoke suite; verify all 31 sweep_catalogue GAPs are CLOSED; update cortex-master.yaml"
        gate: "python3 scripts/run_tests.py smoke — ≥1400 passing"
        blocker: "Phase 73 file must NOT be moved to completed/ until smoke gate passes"
      refactor:
        action: "Update cortex-master.yaml thin index entry: status→COMPLETE, file→completed/; move phase-73 detail file planned/→completed/"
        gate: "python3 -c \"import yaml; yaml.safe_load(open('cortex-registry/cortex-master.yaml')); print('YAML valid')\" AND wc -l cortex-registry/cortex-master.yaml (≤500)"
        blocker: "Phase is not COMPLETE until YAML valid and ≤500 lines"

    steps:
      - "python3 scripts/run_tests.py smoke — must pass ≥1400"
      - "Verify ALL 31 sweep_catalogue GAPs have status: CLOSED"
      - "Move planned/phase-73-unified-brain-and-toolkit.yaml → completed/"
      - "Update cortex-master.yaml: phase-73 status→COMPLETE, file→completed/ path"
      - "python3 -c \"import yaml; yaml.safe_load(open('cortex-registry/cortex-master.yaml'))\""
      - "wc -l cortex-registry/cortex-master.yaml — must be ≤500"

    completion_gate:
      test_runner_command: "python3 scripts/run_tests.py smoke"
      min_tests_pass: 1400
      zero_new_failures: true
      all_sweep_catalogue_gaps_closed: true
      cortex_master_yaml_valid: true
      cortex_master_yaml_line_count_max: 500
      phase_file_moved_to_completed: true
      all_gap_refs_closed: true
      blocks_next_sub_phase: false

# ─────────────────────────────────────────────────────────────────────────────
# EXIT CRITERIA — Phase 73 COMPLETE when ALL met (CORE-064)
# ─────────────────────────────────────────────────────────────────────────────
exit_criteria:
  matrix:
    - "All 7 original P0 cells wired (coverage gate: PASS)"
    - "All 18 P1 cells wired (coverage ~93%)"
    - "Matrix extended 15×15 → 20×20 (400 possible pairs scored)"
    - "matrix.coverage_score ≥ 0.80 (80%+ on 20×20)"
    - "Check #20 wired into /audit fix Stage 2"
  opj:
    - "MasterOrchestrator._opj_consult() called in coordinate_operation()"
    - "IntentRouter records all decisions to OPJ"
    - "OPJ→T1 promotion pipeline functional (confidence ≥0.80)"
  toolkit:
    - "cortex/toolkit/ exists with 6 canonical subdirs"
    - "Canonical BatchProcessor imported by all 4 former duplicates (CORE-035 CLOSED)"
    - "HierarchicalScanner generic and used by LENS pipeline"
    - "DomainAdapter ABC present with ≥2 concrete adapters"
    - "cortex_toolkit MCP tool registered and callable"
  golden_tests:
    - "42 new golden tests written RED then turned GREEN"
    - "Previous golden suite ≥947 passing (no regressions)"
  unit_tests:
    - "0 unit test failures (5 pre-existing failures fixed)"
  smoke:
    - "python3 scripts/run_tests.py smoke ≥1400 passing"
  governance:
    - "cortex-master.yaml ≤500 lines after update"
    - "CORE-064: ALL 31 sweep catalogue GAPs CLOSED"
    - "phase-73 detail file moved: planned/ → completed/"

# ─────────────────────────────────────────────────────────────────────────────
# METRICS
# ─────────────────────────────────────────────────────────────────────────────
metrics:
  before:
    matrix_coverage: "6.9%"
    matrix_dimensions: "15×15"
    p0_cells_unwired: 7
    p1_cells_unwired: 18
    master_orchestrator_opj: false
    intent_router_opj: false
    batch_processor_duplicates: 4
    hierarchical_scanner_generic: false
    toolkit_exists: false
    golden_tests_intelligence: 1
    unit_test_failures: 5
    smoke_passing: 1420
  projected_after:
    matrix_coverage: "≥80%"
    matrix_dimensions: "20×20"
    p0_cells_unwired: 0
    p1_cells_unwired: 0
    master_orchestrator_opj: true
    intent_router_opj: true
    batch_processor_duplicates: 0
    hierarchical_scanner_generic: true
    toolkit_exists: true
    golden_tests_intelligence: 43
    unit_test_failures: 0
    smoke_passing: "≥1420"

# ─────────────────────────────────────────────────────────────────────────────
# SUPERSEDED / ARCHIVED FILES
# ─────────────────────────────────────────────────────────────────────────────
archived_sources:
  - file: "cortex-registry/planning/phases/planned/phase-71-holistic-unified-brain.yaml"
    action: "ARCHIVE to planned/_archived/ — superseded by phase-73 (this file)"
    reason: "GAP catalogue fully migrated. Retained for historical reference only."
  - file: "cortex-registry/planning/phases/planned/phase-toolkit-consolidation.yaml"
    action: "ARCHIVE to planned/_archived/ — superseded by phase-73 (this file)"
    reason: "All 7 GAPs migrated as GAP-73-T1..T7."
  - file: "cortex-registry/planning/phases/planned/phase-70-unified-brain-remediation.yaml"
    action: "MOVE to completed/ — fully closed in Phase 70A, superseded by phase-71/73"
    reason: "All 16 GAPs closed. Lineage preserved in absorbs section above."

metadata:
  authority: "cortex-master.yaml v20.0 + CORE-064 + CORE-008"
  phase_template: "cortex-registry/planning/phases/_template.yaml"
  ac_id: "AC-73-UNIFIED-BRAIN-TOOLKIT"
  test_runner_guide:
    during_tdd_red_green: "python3 scripts/run_tests.py file <specific_test_file>"
    after_sub_phase_green: "python3 scripts/run_tests.py dir tests/<affected_dir>/"
    pre_commit_gate: "python3 scripts/run_tests.py smoke"
    golden_validation: "python3 scripts/run_tests.py golden"
    sub_phase_boundary: "python3 scripts/run_tests.py batch"

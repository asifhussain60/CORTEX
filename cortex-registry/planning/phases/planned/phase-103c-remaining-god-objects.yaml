id: "phase-103c"
title: "Phase 103-C — Remaining God Objects (8 files 1000-2000 lines)"
version: "1.0"
created: "2026-03-03"
status: PLANNED
priority: P2
parent_phase: "phase-103"
governance_authority:
  - CORE-008  # TDD mandatory
  - CORE-035  # Single canonical implementation
  - CORE-064  # Sweep Completeness Contract
sweep_catalogue_id: "SWEEP-103C-REMAINING-GOD-OBJECTS"
depends_on: ["phase-103b"]

background: |
  After phases 103a and 103b, 8 god-object files remain in the 1000-2000 line range.
  These are lower priority (1x rather than 2-3x the target) but should be addressed:

  Priority order (highest coupling first):
  1. cortex/lens/lens_orchestrator.py (2,044L) — 8 analyzers in one file; extract per-analyzer
  2. cortex/orchestrators/core/enforcement_orchestrator.py (1,865L) — extract rule-checkers
  3. cortex/intelligence/memory/tier2_adaptive/resilience.py (1,876L) — extract per-strategy
  4. cortex/intelligence/knowledge/knowledge_synthesis_engine.py (1,566L) — extract synthesizers
  5. cortex/orchestrators/core/conversation_protocol.py (1,538L) — extract per-protocol
  6. cortex/tools/orchestrator_scaffolder.py (1,455L) — extract template sections
  7. cortex/tools/tool_generator.py (1,426L) — extract per-tool-type generators
  8. cortex/mcp/tools/utilities.py (1,213L) — extract utility categories

target: "All files < 1000 lines after extraction"
estimated_effort: "6-8 hours"
risk: MEDIUM
depends_on: ["phase-103b"]

acceptance_criteria:
  - "No file in cortex/ exceeds 1000 lines (except legacy known exceptions)"
  - "All extractions use composition pattern (not inheritance)"
  - "All existing tests pass (2172+)"

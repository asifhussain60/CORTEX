---
# Phase 13: Zero-Stub Enforcement
# FileFactory-validated name: phase-13-stub-elimination.yaml (CORE-028 compliant)
# Authority: CORE-008 (TDD) · CORE-011 (type hints) · CORE-012 (docstrings) · CORE-035 (single impl)

id: phase-13
title: "Zero-Stub Enforcement — Replace 186 stub bodies with production-grade implementations"
status: planned
priority: P0
risk: medium
dependencies:
  - phase-12

source: "CORTEX Architect investigation 2026-02-20 — 186 stub instances across 18 files"

# ──────────────────────────────────────────────────────────────────
# STUB INVENTORY (generated by FileFactory-aware audit)
# ──────────────────────────────────────────────────────────────────
stub_inventory:
  total_instances: 186
  total_files: 18
  categories:
    functional_stubs:
      description: "Methods with empty or incorrect behaviour — P0 correctness risk"
      count: 3
      files:
        - path: cortex/mcp/tools/operations.py
          method: "CortexRefactor.execute (all 5 operations)"
          issue: "Returns changes=[] always; C#/TS ops silently succeed with zero edits"
          fix: "Implement Python organize via AST; gate C#/TS with explicit NotImplementedError('C# adapter pending: use csharp_refactor_adapter.py')"
        - path: cortex/mcp/tools/intelligence.py
          method: "__init__ exception handler"
          issue: "bare `pass` swallows IntelligenceOrchestrator init failure silently"
          fix: "Replace with structured logger.warning + graceful degradation flag"
        - path: cortex/tools/testing_framework.py
          methods: "AssertionBuilder.has_field, has_section, equals, is_not_empty, build (lines 204-373)"
          issue: "Fluent builder methods return None — assertions never constructed"
          fix: "Implement full fluent builder chain returning self; build() returns List[Assertion]"

    docstring_stubs:
      description: "TODO placeholder docstrings violating CORE-012 — P1 quality risk"
      count: 183
      breakdown:
        - file: cortex/mcp/tools/utilities.py
          count: 45
          pattern: "All @property and execute() methods across 9 tool classes"
        - file: cortex/mcp/tools/operations.py
          count: 25
          pattern: "All @property methods across CortexDebug, CortexRefactor, CortexPlan, CortexOnboard, CortexDashboard"
        - file: cortex/mcp/tools/governance.py
          count: 20
          pattern: "All @property methods across CortexGovernance, CortexValidate, CortexLoad"
        - file: cortex/mcp/tools/core.py
          count: 20
          pattern: "All @property methods across CortexProcessRequest, CortexChallenge, CortexClassify, CortexRequestLifecycle"
        - file: cortex/mcp/tools/intelligence.py
          count: 15
          pattern: "All @property methods across CortexLens, CortexKnowledge, CortexGit, CortexGenerateTests"
        - file: cortex/tools/testing_framework.py
          count: 13
          pattern: "AssertionBuilder fluent methods + TestRunner helpers"
        - file: cortex/secrets/audit_trail.py
          count: 11
          pattern: "AuditLogger.log, get_entries, sign, verify, replay + AuditEntry properties"
        - file: cortex/secrets/git_scanning.py
          count: 9
          pattern: "SecretsScanner.scan_text, scan_file + GitHistoryScanner methods"
        - file: cortex/secrets/migration.py
          count: 8
          pattern: "SecretsMigrator.migrate, rollback, verify + plan methods"
        - file: cortex/secrets/compliance.py
          count: 8
          pattern: "ComplianceChecker.check, report, remediate + policy methods"
        - file: cortex/lens/cache/lens_cache.py
          count: 4
          pattern: "LensCache.get, set, invalidate, stats"
        - file: cortex/tools/debug_orchestrator/__init__.py
          count: 2
          pattern: "DebugOrchestrator.inject, capture"
        - file: cortex/observability/observability/health_monitor.py
          count: 1
          pattern: "HealthMonitor.check"
        - file: cortex/lens/discovery/__init__.py
          count: 1
          pattern: "LensDiscovery.discover"
        - file: cortex/intelligence/memory/tier2_adaptive/resilience.py
          count: 1
          pattern: "ResilienceAdapter.adapt"
        - file: cortex/intelligence/memory/core/import_resolver.py
          count: 1
          pattern: "ImportResolver.resolve"
        - file: cortex/intelligence/memory/adaptive_intelligence/resilience.py
          count: 1
          pattern: "duplicate resilience adapter"
        - file: cortex/infrastructure/graceful_degradation.py
          count: 1
          pattern: "GracefulDegradation.degrade"

# ──────────────────────────────────────────────────────────────────
# NEW FILES (FileFactory-validated, CORE-028 compliant names)
# ──────────────────────────────────────────────────────────────────
new_files:
  implementations:
    - path: cortex/orchestrators/support/stub_remediation_enforcer.py
      purpose: "AuditCoordinator plugin — detects TODO docstrings + functional stubs at audit-time"
      mcp_exposure: "cortex_audit (check #2 enhancement)"
      orchestrator: AuditCoordinator

    - path: cortex/mcp/tools/adapters/language_adapter_base.py
      purpose: "Abstract base for language-aware refactoring adapters (Python/C#/TypeScript)"
      mcp_exposure: "cortex_refactor (language adapter registry)"
      orchestrator: RefactoringOrchestrator

    - path: cortex/mcp/tools/adapters/python_refactor_adapter.py
      purpose: "Python refactoring: organize imports via isort/autoflake, extract via AST rope"
      mcp_exposure: "cortex_refactor (operation=organize|extract for Python)"
      orchestrator: RefactoringOrchestrator

    - path: cortex/mcp/tools/adapters/csharp_refactor_adapter.py
      purpose: "C# scaffolding adapter: emits Roslyn-compatible scaffold + IDE instruction manifest"
      mcp_exposure: "cortex_refactor (operation=extract|rename for C#, scaffold mode)"
      orchestrator: RefactoringOrchestrator

    - path: cortex/mcp/tools/adapters/typescript_refactor_adapter.py
      purpose: "TypeScript service-extraction adapter: emits Angular service scaffold + barrel exports"
      mcp_exposure: "cortex_refactor (operation=extract|organize for TypeScript, scaffold mode)"
      orchestrator: RefactoringOrchestrator

  tests:
    - path: tests/unit/orchestrators/support/test_stub_remediation_enforcer.py
      tdd_phase: RED
      golden_candidate: true

    - path: tests/unit/mcp/tools/adapters/test_language_adapter_base.py
      tdd_phase: RED

    - path: tests/unit/mcp/tools/adapters/test_python_refactor_adapter.py
      tdd_phase: RED

    - path: tests/unit/mcp/tools/adapters/test_csharp_refactor_adapter.py
      tdd_phase: RED

    - path: tests/unit/mcp/tools/adapters/test_typescript_refactor_adapter.py
      tdd_phase: RED

  workflow_template:
    path: cortex-registry/workflows/templates/lifecycle/stub-remediation-phase-spec.yaml
    purpose: "Reusable workflow for audit-detect → TDD-fix → validate cycle on any stub pattern"

# ──────────────────────────────────────────────────────────────────
# DELIVERABLES
# ──────────────────────────────────────────────────────────────────
deliverables:
  - "D1: stub_remediation_enforcer.py — AuditCoordinator check#2 plugin detecting TODO+functional stubs"
  - "D2: language_adapter_base.py — abstract LanguageAdapter protocol (Python/C#/TS)"
  - "D3: python_refactor_adapter.py — real Python organize/extract via AST (cortex_refactor now writes files)"
  - "D4: csharp_refactor_adapter.py — C# scaffold emitter + Roslyn IDE manifest"
  - "D5: typescript_refactor_adapter.py — Angular service extraction scaffold"
  - "D6: stub-remediation-phase-spec.yaml — reusable workflow template"
  - "D7: All 183 TODO docstrings replaced with real docstrings in-place (batch RED→GREEN)"
  - "D8: 3 functional stubs fixed (cortex_refactor, intelligence __init__, AssertionBuilder)"
  - "D9: Audit check#2 expanded — detects TODO pattern + functional stub pattern"
  - "D10: 5 RED test files, all passing GREEN, coverage ≥ 95%"
  - "D11: cortex_validate_compliance confirms 0 CORE-012 violations post-fix"

# ──────────────────────────────────────────────────────────────────
# TDD EXECUTION ORDER (CORE-008)
# ──────────────────────────────────────────────────────────────────
execution_order:
  - step: "GAP-13-001 (XS) — intelligence.py bare pass → logger.warning (zero-risk)"
  - step: "GAP-13-002 (S)  — AssertionBuilder fluent chain implementation"
  - step: "GAP-13-003 (M)  — language_adapter_base.py + python_refactor_adapter.py (real file-write)"
  - step: "GAP-13-004 (M)  — cortex_refactor wired to adapter registry (language dispatch)"
  - step: "GAP-13-005 (M)  — csharp_refactor_adapter.py scaffold emitter"
  - step: "GAP-13-006 (M)  — typescript_refactor_adapter.py scaffold emitter"
  - step: "GAP-13-007 (L)  — stub_remediation_enforcer.py + Audit check#2 expansion"
  - step: "GAP-13-008 (XL) — Batch docstring sweep: 183 TODO → real docstrings (5 batches, test after each)"

tdd_mandate: "CORE-008 — RED test first for every step above, no exceptions"
regression_gate: "15,303 collected tests must remain passing after each step"

acceptance_criteria:
  - "AC-PHASE13-001: grep -rn 'TODO: Add docstring' cortex/ | wc -l == 0"
  - "AC-PHASE13-002: cortex_refactor operation=organize on .py file produces real file diff"
  - "AC-PHASE13-003: cortex_refactor C# operation returns scaffold manifest, NOT silent changes=[]"
  - "AC-PHASE13-004: Audit check#2 catches TODO docstring in test fixture and reports P1 violation"
  - "AC-PHASE13-005: 486 golden tests still passing after all 8 steps"
  - "AC-PHASE13-006: cortex_validate_compliance returns 0 CORE-012 violations"
  - "AC-PHASE13-007: AssertionBuilder.build() returns populated List[Assertion] (not empty)"

progress:
  started: null
  completed: null
  issues_resolved: 0
  lens_scan_completed: false

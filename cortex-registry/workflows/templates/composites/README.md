# Composed Workflow Templates

Auto-generated by `TemplateComposer` (Phase 55). These templates are assembled
from validated primitives in `primitives/` and persisted here for reuse.

**Do not hand-edit.** To add custom templates, use `cortex-registry/workflows/templates/`
category directories or `company/workflows/`.

**Source:** `cortex/orchestrators/workflow/template_composer.py`

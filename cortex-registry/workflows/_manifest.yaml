# CORTEX Workflow Template Library - Master Manifest
# Auto-generated: 2026-02-16
# Version: 5.0
# Authority: Phase 100 - Convergence-Gated Workflow Template Library

version: '5.0'
generated_at: '2026-02-16T00:00:00Z'
total_templates: 10
categories: 8

# ============================================================================
# TEMPLATE CATEGORIES
# ============================================================================
categories:
  tdd:
    name: "Test-Driven Development"
    description: "TDD workflows with RED→GREEN→REFACTOR cycles"
    templates: 3
    
  security:
    name: "Security & Compliance"
    description: "Security audits and compliance workflows"
    templates: 1
    
  quality:
    name: "Code Quality"
    description: "Code quality improvement workflows"
    templates: 1
    
  migration:
    name: "Legacy Modernization"
    description: "Legacy code modernization workflows"
    templates: 1
    
  cleanup:
    name: "Code Cleanup"
    description: "Deduplication and cleanup workflows"
    templates: 1
    
  onboarding:
    name: "Repository Onboarding"
    description: "Repository setup and onboarding workflows"
    templates: 1
    
  review:
    name: "Code Review"
    description: "Code review and analysis workflows (auto-injected)"
    templates: 1
    
  refactor:
    name: "Refactoring"
    description: "Refactoring workflows (auto-injected epilogues)"
    templates: 1

# ============================================================================
# TEMPLATE REGISTRY
# ============================================================================
templates:
  # --------------------------------------------------------------------------
  # TDD TEMPLATES
  # --------------------------------------------------------------------------
  - template_id: "tdd/feature-implementation"
    name: "Generic Feature TDD"
    version: "1.0"
    category: "tdd"
    status: "planned"
    priority: P0
    
    description: "Basic RED→GREEN→REFACTOR cycle for new features"
    
    knowledge_placeholders:
      - "{test_framework}"
      - "{component_patterns}"
    
    convergence_gated: false
    
    stages: 3
    estimated_duration_minutes: 30
    
    use_cases:
      - "Implement new API endpoint"
      - "Add new UI component"
      - "Create new business logic module"
    
    location: "cortex-registry/workflows/tdd/feature-implementation/"
    
  - template_id: "tdd/frontend-visual"
    name: "Frontend Visual TDD"
    version: "2.0"
    category: "tdd"
    status: "planned"
    priority: P1
    
    description: "Visual TDD with screenshot comparison (migrated from Phase 99)"
    
    knowledge_placeholders:
      - "{test_framework}"
      - "{component_library}"
      - "{visual_regression_tool}"
    
    convergence_gated: true
    convergence_gates:
      - step: "visual_regression_check"
        max_cycles: 5
        criteria: "visual_diff_score < threshold"
    
    stages: 4
    estimated_duration_minutes: 45
    
    use_cases:
      - "Implement UI component with visual tests"
      - "Add responsive design with screenshot validation"
      - "Create interactive widget with visual regression"
    
    location: "cortex-registry/workflows/tdd/frontend-visual/"
    
    notes: |
      Migrated from Phase 99 with improvements:
      - Knowledge parameterization added
      - Convergence gate for visual diff loop
      - CORE-028 compliant naming (no 'dogfooding')
    
  - template_id: "tdd/api-service"
    name: "API Service TDD"
    version: "1.0"
    category: "tdd"
    status: "planned"
    priority: P0
    
    description: "API service TDD with contract validation"
    
    knowledge_placeholders:
      - "{test_framework}"
      - "{api_style}"
      - "{auth_pattern}"
      - "{contract_validation_tool}"
    
    convergence_gated: true
    convergence_gates:
      - step: "contract_validation"
        max_cycles: 3
        criteria: "contract_violations == 0"
    
    stages: 5
    estimated_duration_minutes: 40
    
    use_cases:
      - "Implement REST API with OpenAPI contract"
      - "Create GraphQL service with schema validation"
      - "Build gRPC service with protobuf contract"
    
    location: "cortex-registry/workflows/tdd/api-service/"

  # --------------------------------------------------------------------------
  # SECURITY TEMPLATES
  # --------------------------------------------------------------------------
  - template_id: "security/compliance-audit"
    name: "Security Compliance Audit"
    version: "1.0"
    category: "security"
    status: "planned"
    priority: P0
    
    description: "Security compliance audit with zero-findings convergence gate"
    
    knowledge_placeholders:
      - "{compliance_standards}"
      - "{security_scanners}"
      - "{remediation_patterns}"
    
    convergence_gated: true
    convergence_gates:
      - step: "security_scan"
        max_cycles: 10
        criteria: "critical_findings == 0 and high_findings == 0"
    
    stages: 4
    estimated_duration_minutes: 60
    
    use_cases:
      - "PCI-DSS compliance audit"
      - "HIPAA compliance validation"
      - "SOX compliance check"
      - "OWASP Top 10 scan"
    
    location: "cortex-registry/workflows/security/compliance-audit/"

  # --------------------------------------------------------------------------
  # QUALITY TEMPLATES
  # --------------------------------------------------------------------------
  - template_id: "quality/code-uplift"
    name: "Code Quality Uplift"
    version: "1.0"
    category: "quality"
    status: "planned"
    priority: P1
    
    description: "Code quality uplift with metric improvement convergence gate"
    
    knowledge_placeholders:
      - "{quality_threshold}"
      - "{quality_metrics}"
      - "{refactoring_patterns}"
    
    convergence_gated: true
    convergence_gates:
      - step: "improve_quality"
        max_cycles: 10
        criteria: "quality_score >= threshold and all_tests_pass"
    
    stages: 3
    estimated_duration_minutes: 50
    
    use_cases:
      - "Improve code maintainability score"
      - "Reduce cyclomatic complexity"
      - "Increase test coverage"
      - "Eliminate code smells"
    
    location: "cortex-registry/workflows/quality/code-uplift/"

  # --------------------------------------------------------------------------
  # MIGRATION TEMPLATES
  # --------------------------------------------------------------------------
  - template_id: "migration/modernize"
    name: "Legacy Code Modernization"
    version: "1.0"
    category: "migration"
    status: "planned"
    priority: P1
    
    description: "Legacy modernization with incremental convergence gate"
    
    knowledge_placeholders:
      - "{source_patterns}"
      - "{target_patterns}"
      - "{test_framework}"
      - "{migration_strategy}"
    
    convergence_gated: true
    convergence_gates:
      - step: "modernize_code"
        max_cycles: 20
        criteria: "legacy_patterns_remaining == 0 and all_tests_pass"
    
    stages: 5
    estimated_duration_minutes: 120
    
    use_cases:
      - "Migrate from legacy framework to modern"
      - "Refactor monolith to microservices"
      - "Upgrade deprecated dependencies"
      - "Modernize build pipeline"
    
    location: "cortex-registry/workflows/migration/modernize/"

  # --------------------------------------------------------------------------
  # CLEANUP TEMPLATES
  # --------------------------------------------------------------------------
  - template_id: "cleanup/deduplication"
    name: "Code Deduplication"
    version: "1.0"
    category: "cleanup"
    status: "planned"
    priority: P0
    
    description: "LENS-based deduplication with convergence loop"
    
    knowledge_placeholders:
      - "{duplication_threshold}"
      - "{extraction_strategy}"
    
    convergence_gated: true
    convergence_gates:
      - step: "remove_duplicates"
        max_cycles: 5
        criteria: "new_duplicates_count == 0"
    
    stages: 2
    estimated_duration_minutes: 30
    
    use_cases:
      - "Remove duplicate code across modules"
      - "Extract shared utilities"
      - "Consolidate similar implementations"
    
    location: "cortex-registry/workflows/cleanup/deduplication/"
    
    notes: |
      Uses LENS duplicate detection (CORE-035 enforcement).
      Loop continues until all duplicates resolved or max_cycles exceeded.

  # --------------------------------------------------------------------------
  # ONBOARDING TEMPLATES
  # --------------------------------------------------------------------------
  - template_id: "onboarding/repo-setup"
    name: "Repository Onboarding"
    version: "1.0"
    category: "onboarding"
    status: "planned"
    priority: P1
    
    description: "Repository onboarding with profile completeness convergence loop"
    
    knowledge_placeholders:
      - "{tech_stack_detection}"
      - "{profile_schema}"
      - "{security_baseline}"
    
    convergence_gated: true
    convergence_gates:
      - step: "complete_profile"
        max_cycles: 3
        criteria: "profile_completeness >= 90"
    
    stages: 6
    estimated_duration_minutes: 45
    
    use_cases:
      - "Onboard new repository to CORTEX"
      - "Detect tech stack and capabilities"
      - "Generate enhanced profile"
      - "Run initial security scan"
    
    location: "cortex-registry/workflows/onboarding/repo-setup/"

  # --------------------------------------------------------------------------
  # REVIEW TEMPLATES (AUTO-INJECTED)
  # --------------------------------------------------------------------------
  - template_id: "review/post-phase-dedup"
    name: "Post-Phase Deduplication Review"
    version: "1.0"
    category: "review"
    status: "planned"
    priority: P0
    
    description: "Cross-phase deduplication check (auto-injected after each phase)"
    
    knowledge_placeholders: []
    
    convergence_gated: true
    convergence_gates:
      - step: "scan_new_duplicates"
        max_cycles: 3
        criteria: "new_duplicates_count == 0"
    
    stages: 1
    estimated_duration_minutes: 10
    
    auto_injected: true
    injection_trigger: "After each phase completion"
    
    use_cases:
      - "Detect duplication introduced by completed phase"
      - "Prevent CORE-035 violations"
      - "Maintain single canonical implementation"
    
    location: "cortex-registry/workflows/review/post-phase-dedup/"
    
    notes: |
      AUTO-INJECTED by MasterOrchestrator.
      User does not explicitly request this.
      Runs transparently after phase boundary.

  # --------------------------------------------------------------------------
  # REFACTOR TEMPLATES (AUTO-INJECTED EPILOGUES)
  # --------------------------------------------------------------------------
  - template_id: "refactor/holistic-sweep"
    name: "Holistic Refactoring Sweep"
    version: "1.0"
    category: "refactor"
    status: "planned"
    priority: P0
    
    description: "Multi-file coherence convergence loop (auto-injected after ALL phases)"
    
    knowledge_placeholders:
      - "{refactoring_patterns}"
      - "{lens_baseline}"
    
    convergence_gated: true
    convergence_gates:
      - step: "refactor_all_files"
        max_cycles: 5
        criteria: "lens_score >= baseline and all_tests_pass"
    
    stages: 1
    estimated_duration_minutes: 20
    
    auto_injected: true
    injection_trigger: "After ALL phases in workflow complete"
    
    use_cases:
      - "Ensure holistic coherence across workflow"
      - "Eliminate local optima from individual phases"
      - "Maintain architectural consistency"
    
    location: "cortex-registry/workflows/refactor/holistic-sweep/"
    
    notes: |
      AUTO-INJECTED by MasterOrchestrator as workflow epilogue.
      Runs RefactoringOrchestrator across ALL files modified during workflow.
      Loops until LENS score meets baseline or max_cycles exceeded.

# ============================================================================
# KNOWLEDGE SOURCES
# ============================================================================
knowledge_sources:
  architect_mode:
    - "cortex-registry/_cortex-master/knowledge/ (45+ YAMLs)"
    - "CORTEX CORE rules (30 rules)"
    - "CORTEX architecture patterns"
  
  production_mode:
    - "company/domains/*.yaml (6 files)"
    - "cortex_intelligence/onboarded_repos/{repo}_enhanced.json"
    - "CORTEX knowledge (fallback only)"

# ============================================================================
# CONVERGENCE STATISTICS
# ============================================================================
convergence_stats:
  total_templates: 10
  convergence_gated: 8
  convergence_percentage: 80
  
  average_max_cycles: 7.5
  
  gates_by_category:
    tdd: 2
    security: 1
    quality: 1
    migration: 1
    cleanup: 1
    onboarding: 1
    review: 1
    refactor: 1

# ============================================================================
# USAGE STATISTICS (PLANNED)
# ============================================================================
usage_stats:
  most_used_placeholders:
    - "{test_framework}"
    - "{auth_pattern}"
    - "{api_style}"
    - "{compliance_standards}"
  
  most_common_convergence_criteria:
    - "all_tests_pass"
    - "zero_findings"
    - "quality_score >= threshold"
    - "duplicates_count == 0"

# ============================================================================
# GOVERNANCE
# ============================================================================
governance:
  core_rules_enforced:
    - CORE-008  # TDD mandatory (all templates)
    - CORE-027  # Audit trail (all templates)
    - CORE-028  # File naming (Phase 99 migration)
    - CORE-035  # No duplication (cleanup/review templates)
    - CORE-049  # Silent autonomous execution (all templates)
  
  enforcement_agent: "EnforcementOrchestrator (7-agent pre-execution gate)"
  
  approval_required: true  # All templates require user approval
  auto_checkpoint: true    # All templates create git checkpoints

# ============================================================================
# MCP INTEGRATION
# ============================================================================
mcp_tools:
  - tool_id: "cortex_workflow"
    operations:
      - "execute"      # Execute template with knowledge injection
      - "monitor"      # Monitor workflow progress
      - "list"         # List available templates
      - "search"       # Semantic search across templates
      - "validate"     # Validate template definition
    
    routing: "MasterOrchestrator Stage 3 → KnowledgeSynthesisEngine"

# ============================================================================
# DEPENDENCIES
# ============================================================================
dependencies:
  required_phases:
    - phase-84: "WorkflowComposer (execution engine)"
    - phase-83: "ConvergenceNeuron (convergence detection)"
    - phase-48: "EnforcementOrchestrator (7-agent validation)"
    - phase-20.5: "KnowledgeSynthesisEngine (knowledge merge)"
  
  external_libraries:
    - package: "transitions"
      version: ">=0.9.0"
      license: "MIT"
      purpose: "Step state machine (FSM)"

# ============================================================================
# VERSION HISTORY
# ============================================================================
version_history:
  - version: "5.0"
    date: "2026-02-16"
    changes:
      - "Initial manifest for Phase 100"
      - "10 templates defined (3 TDD, 1 security, 1 quality, 1 migration, 1 cleanup, 1 onboarding, 1 review, 1 refactor)"
      - "Knowledge parameterization architecture"
      - "Convergence-gated execution (8/10 templates)"
      - "Auto-injected epilogues (2 templates)"
      - "Phase 99 migrated to frontend-visual/"

# ============================================================================
# NOTES
# ============================================================================
notes: |
  This manifest is AUTO-GENERATED by WorkflowTemplateRegistry.
  DO NOT EDIT MANUALLY.
  
  To regenerate:
    python -m cortex.orchestrators.workflow.template_registry generate-manifest
  
  Templates are auto-discovered from:
    cortex-registry/workflows/{category}/{template-name}/
  
  Each template MUST have:
    - metadata.yaml (governance + knowledge + convergence)
    - workflow.yaml (steps with placeholders)
    - README.md (documentation)

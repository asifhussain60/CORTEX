# Phase 14 — MCP Consolidation
# 38 → 23 MCP tools, strangler-fig migration for legacy dirs

phase:
  id: "phase-14"
  title: "MCP Consolidation + Stale Dir Strangler-Fig Migration"
  status: complete
  completed: "2026-02-21"
  priority: P0
  risk: medium

  description: |
    Consolidated 38 MCP tool Python files to 23 canonical tools. Created
    multi_repo_tools.py (6 classes) and deployment_tools.py (5 classes).
    Converted 11 subdir files to backward-compat shims (CORE-035). Confirmed
    all 5 legacy stale directories already removed in prior phases.

  deliverables:
    - "MCP tool files: 38 → 24 (subdirs retained as compat packages)"
    - "multi_repo_tools.py — 6 consolidated classes"
    - "deployment_tools.py — 5 consolidated classes"
    - "11 subdir files → 3-line backward-compat shims"
    - "562 golden tests passing (exceeded 486 baseline)"

  artifacts:
    tests_added: 125
    tests_passing: 125

  metrics:
    duration: "~3 hours"

  dependencies: [phase-13]

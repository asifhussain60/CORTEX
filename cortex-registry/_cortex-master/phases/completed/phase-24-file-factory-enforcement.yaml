# Phase 24 — FileFactory Strict Enforcement
# Internal terminology blocking + universal file creation gate

phase:
  id: "phase-24"
  title: "FileFactory Strict Enforcement — Terminology Blocking & Creation Gate"
  status: complete
  completed: "2026-02-22"
  priority: P0
  risk: low

  description: |
    Added strict enforcement to FileFactory: 10 CORTEX-internal terms blocked
    in prohibited_patterns (phase, sts, skull, tier0, ccl, crystallized, brain,
    hexa, cortex_internal, wiring_spec). Added sanitize_name() method and
    create_file() universal gate that validates CORE-028 before writing.
    Static analysis gate scans orchestrators/mcp/core for raw writes.
    Exemption registry for legitimate runtime I/O.

  deliverables:
    - "D1: 10 internal terms in prohibited_patterns"
    - "D2: sanitize_name() method on FileFactory"
    - "D3: create_file() universal gate (validates before writing)"
    - "D4: 46 golden enforcement tests"
    - "D5: Static analysis gate for raw write detection"
    - "D6: Exemption registry for legitimate I/O"

  artifacts:
    files_modified:
      - cortex/core/file_factory.py
      - cortex-registry/core/config/file-naming-rules.yaml
    tests_added: 46
    tests_passing: 46

  metrics:
    duration: "~2 hours"

  dependencies: [phase-01]

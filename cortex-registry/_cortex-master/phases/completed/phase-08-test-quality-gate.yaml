# Phase 08 — Test Quality Gate
# Canonical TestQualityGate service, MCP tool, pytest plugin

phase:
  id: "phase-08"
  title: "Test Quality Gate — Canonical Service + MCP + pytest Plugin"
  status: complete
  completed: "2026-02-20"
  priority: P0
  risk: low

  description: |
    Replaced 3 existing test scorers with single canonical TestQualityGate (CORE-035).
    Created YAML-driven scoring SSOT, pytest hook plugin for collect-time blocking,
    MCP tool (cortex_score_tests), TDDOrchestrator wiring, and conftest.py registration.
    Scoring formula: Impact + Likelihood + Detection + Efficiency − Maintenance = 0–9.
    Gate: ≥7 KEEP | 4–6 REVIEW | <4 DELETE.

  deliverables:
    - "D1: cortex-registry/core/test-quality-gate.yaml — scoring SSOT"
    - "D2: cortex/testing/quality_gate.py — canonical TestQualityGate"
    - "D3: cortex/testing/pytest_quality_plugin.py — pytest hook"
    - "D4: cortex/mcp/tools/test_quality_tool.py — cortex_score_tests MCP tool"
    - "D5: TDDOrchestrator wiring — blocks test generation scoring < 7"
    - "D6: conftest.py registration"

  artifacts:
    files_created:
      - cortex/testing/quality_gate.py
      - cortex/testing/pytest_quality_plugin.py
      - cortex/mcp/tools/test_quality_tool.py
    tests_added: 49
    tests_passing: 49

  metrics:
    duration: "2 hours"

  dependencies: [phase-07]

id: phase-68
title: "cortex/core Flatten — 27 Subdirs → 15 Canonical (Phase 60 Revival)"
priority: P1
status: COMPLETE
created: "2026-02-24"
completed: "2026-02-24"
sweep_id: SWEEP-68-CORE-FLATTEN
authority: [CORE-035, CORE-064, CORE-008]
phase_prerequisites:
  - phase-58  # core/core collapse COMPLETE (0 imports remain)
  - phase-59  # orchestrator surface partial reduction COMPLETE

# ─────────────────────────────────────────────────────────────────────────────
# BASELINE (measured 2026-02-24)
# ─────────────────────────────────────────────────────────────────────────────
baseline:
  cortex_py_files: 1332
  cortex_core_subdirs: 27
  orchestrator_classes: 115
  yaml_files: 376
  core_core_imports: 0   # Phase 58 COMPLETE

# ─────────────────────────────────────────────────────────────────────────────
# SUBDIR AUDIT (measured 2026-02-24)
# ─────────────────────────────────────────────────────────────────────────────
subdir_audit:
  zero_caller_zero_file:
    - dir: cortex/core/schemas/    # 0 files, 0 callers → DELETE immediately
  zero_caller_with_files:
    - dir: cortex/core/examples/   # 1 file (demo), 0 callers → ARCHIVE to cortex-sts/
    - dir: cortex/core/database/   # 2 files, 0 callers (compat shim in core/database.py) → MOVE to cortex/infrastructure/database/
  low_caller_single_file:
    - dir: cortex/core/bootstrap/  # 1 caller (cortex/__init__.py) → inline into cortex/__init__.py
    - dir: cortex/core/config/     # 1 caller (test only) → move to cortex/infrastructure/config/
    - dir: cortex/core/resilience/ # 1 caller (test only) → move to cortex/infrastructure/resilience/
    - dir: cortex/core/safety/     # 1 caller (test only) → move to cortex/core/common/
    - dir: cortex/core/errors/     # 1 caller (test only) → move to cortex/core/common/
    - dir: cortex/core/recovery/   # 1 caller (test only) → move to cortex/core/common/
    - dir: cortex/core/decorators/ # 1 caller (self-import __init__) → move to cortex/core/common/
    - dir: cortex/core/state/      # 3 callers (2 tests + master) → move to cortex/core/common/
  high_callers_keep:
    - dir: cortex/core/common/       # 18 callers — KEEP
    - dir: cortex/core/discovery/    # 30 callers — KEEP
    - dir: cortex/core/execution/    # 37 callers — KEEP
    - dir: cortex/core/governance/   # 8 callers — KEEP
    - dir: cortex/core/hallucination_prevention/  # 13 callers — KEEP
    - dir: cortex/core/intelligence/ # 100 callers — KEEP
    - dir: cortex/core/intent/       # 71 callers — KEEP
    - dir: cortex/core/interaction/  # 49 callers — KEEP
    - dir: cortex/core/interfaces/   # 41 callers — KEEP
    - dir: cortex/core/knowledge/    # 82 callers — KEEP
    - dir: cortex/core/models/       # 5 callers — KEEP
    - dir: cortex/core/orchestrator/ # 137 callers — KEEP
    - dir: cortex/core/registry/     # 113 callers — KEEP
    - dir: cortex/core/security/     # 34 callers — KEEP
    - dir: cortex/core/wiring/       # 55 callers — KEEP

# ─────────────────────────────────────────────────────────────────────────────
# SUB-PHASES
# ─────────────────────────────────────────────────────────────────────────────
sub_phases:
  - id: phase-68-A
    title: "Delete zero-content dirs + archive examples/"
    status: CLOSED
    actions:
      - "Delete cortex/core/schemas/ ✅ (already empty — pre-existing)"
      - "cortex/core/examples/ ✅ (pre-existing — absent at phase start)"
      - "cortex/core/database/ ✅ (pre-existing — absent at phase start)"
    regression_gate: "smoke tests green after each action"

  - id: phase-68-B
    title: "Consolidate low-caller dirs into cortex/core/common/ + delete stubs"
    status: CLOSED
    actions:
      - "Files already consolidated to cortex.core.common (pre-existing compat shims) ✅"
      - "Deleted cortex/core/safety/ ✅"
      - "Deleted cortex/core/errors/ ✅"
      - "Deleted cortex/core/recovery/ ✅"
      - "Deleted cortex/core/state/ ✅"
      - "Deleted cortex/core/decorators/ ✅"
      - "Updated 6 test files to import from cortex.core.common.* ✅"
    regression_gate: "133 affected tests passing ✅"

  - id: phase-68-C
    title: "Move infrastructure dirs out of core/ + fix bootstrap"
    status: CLOSED
    actions:
      - "Deleted cortex/core/config/ ✅"
      - "Deleted cortex/core/resilience/ ✅"
      - "Deleted cortex/core/bootstrap/ ✅"
      - "Fixed cortex/__init__.py → imports from cortex.bootstrap directly ✅"
    regression_gate: "smoke tests green ✅"

  - id: phase-68-D
    title: "Final verification — cortex/core/ at 15 canonical subdirs"
    status: CLOSED
    actions:
      - "find cortex/core -maxdepth 1 -mindepth 1 -type d | wc -l → 15 ✅"
      - "1420 smoke tests passed ✅"
      - "19/19 Phase 68 RED→GREEN tests passing ✅"

# ─────────────────────────────────────────────────────────────────────────────
# EXIT CRITERIA
# ─────────────────────────────────────────────────────────────────────────────
exit_criteria:
  - "cortex/core/ reduced from 27 → 15 subdirs (8 stubs deleted) ✅"
  - "cortex/core/schemas/ deleted (pre-existing) ✅"
  - "cortex/core/examples/ absent (pre-existing) ✅"
  - "cortex/core/database/ absent (pre-existing) ✅"
  - "cortex/core/common/ canonical for: safety, errors, recovery, state, decorators, config, resilience ✅"
  - "cortex/core/bootstrap/ deleted — cortex/__init__.py uses cortex.bootstrap directly ✅"
  - "1420 smoke tests passing ✅"
  - "Zero import errors introduced ✅"
  - "19/19 Phase 68 governance tests GREEN ✅"

sweep_catalogue:
  - gap: "cortex/core/safety/ stub dir"
    status: CLOSED
  - gap: "cortex/core/errors/ stub dir"
    status: CLOSED
  - gap: "cortex/core/recovery/ stub dir"
    status: CLOSED
  - gap: "cortex/core/state/ stub dir"
    status: CLOSED
  - gap: "cortex/core/decorators/ stub dir"
    status: CLOSED
  - gap: "cortex/core/config/ stub dir"
    status: CLOSED
  - gap: "cortex/core/resilience/ stub dir"
    status: CLOSED
  - gap: "cortex/core/bootstrap/ broken __init__.py"
    status: CLOSED
  - gap: "Test imports using old cortex.core.{safety,errors,recovery,state,config,resilience} paths"
    status: CLOSED
</content>
</invoke>
# ==============================================================================
# REDIRECT STUB — CORE-035 compliance (GAP-69-04)
# ==============================================================================
# This file previously contained a near-duplicate of tier0-skull/skull-rules.yaml.
# Removed 2026-02-24 to eliminate the CORE-035 violation (single canonical implementation).
#
# CANONICAL SSOT: cortex-registry/core/tier0-skull/skull-rules.yaml
#
# Consumers that referenced this path have been updated:
#   - cortex/core/yaml_loaders.py          → uses tier0-skull/skull-rules.yaml
#   - cortex-registry/core/ccl-governance-crystal.yaml → updated reference
#   - workflows/templates/governance/golden-test-promotion.yaml → updated references
# ==============================================================================
redirect:
  canonical: "cortex-registry/core/tier0-skull/skull-rules.yaml"
  reason: "CORE-035 — single canonical implementation. Duplicate eliminated 2026-02-24."
  governance_fix: "GAP-69-04"

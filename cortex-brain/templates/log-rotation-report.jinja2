# 📋 Log Rotation Report

**Timestamp:** {{ timestamp }}  
**Rotated:** {{ rotated_count }} logs  
**Archived:** {{ archived_count|default(rotated_count) }} logs  
**Threshold:** {{ threshold_mb|default(10) }} MB

---

## Rotated Logs

{% if rotated_logs %}
{% for log in rotated_logs %}
### {{ loop.index }}. {{ log.path }}

- **Original Size:** {{ log.size_mb|round(2) }} MB
- **Archived To:** `{{ log.archived_to }}`
- **Status:** ✅ Rotated and compressed

{% endfor %}
{% else %}
_No logs rotated (all logs below threshold)_
{% endif %}

---

## Summary

{% if rotated_count > 0 %}
Successfully rotated {{ rotated_count }} log file{% if rotated_count != 1 %}s{% endif %} exceeding {{ threshold_mb }}MB threshold. All logs were compressed and archived, then truncated to free space.
{% else %}
No logs required rotation. All logs are below the {{ threshold_mb }}MB threshold.
{% endif %}

---

✅ **Log rotation complete!**

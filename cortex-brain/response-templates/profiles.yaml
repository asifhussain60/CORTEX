# Profile-Driven Variants (Phase 5)
# Schema: v0.1 (scaffold)
# Purpose: Interaction mode and experience-level adjustments
# Backcompat: Composer-only consumption

schema_version: '0.1'
last_updated: '2025-12-02'

profiles:
  interaction_modes:
    autonomous:
      response_overrides:
        # Keep terse, action-first
        response_content_prefix: ""
    guided:
      response_overrides:
        # Default behavior – no changes
        response_content_prefix: ""
    educational:
      response_overrides:
        # Add brief rationale/explanations
        response_content_prefix: ""
    pair:
      response_overrides:
        # Invite small checkpoints/decisions
        response_content_prefix: ""

  experience_levels:
    junior: {}
    mid: {}
    senior: {}
    expert: {}

# TODO (Phase 5):
# - Define concrete overrides (sections visibility, detail level)
# - Add tech stack awareness mapping if needed

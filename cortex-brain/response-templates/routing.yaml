# Routing Map (Phase 5)
# Schema: v0.1 (scaffold)
# Purpose: Triggers → template keys (modular composition)
# Backcompat: Composer-only consumption

schema_version: '0.1'
last_updated: '2025-12-02'

routing:
  # Examples (to be filled in migration)
  onboarding:
    triggers: ["onboard", "onboarding", "setup profile", "first time", "new user"]
  planning_overview:
    triggers: ["plan", "planning", "planning overview"]
  fallback:
    triggers: ["fallback"]

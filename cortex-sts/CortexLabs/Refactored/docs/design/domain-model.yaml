# ══════════════════════════════════════════════════════════════════════════════
# Domain Model — CortexLabs FinTrack
# ══════════════════════════════════════════════════════════════════════════════
# Generated by: CORTEX Design Orchestrator (Phase 22, Sub-Phase B)
# Date: 2026-02-22
# ══════════════════════════════════════════════════════════════════════════════

metadata:
  document_type: domain-model
  version: 1.0.0
  last_updated: 2026-02-22

bounded_contexts:
  - name: User Management
    aggregates: [User]
    services: [UserService, AuthService]

  - name: Financial Tracking
    aggregates: [Transaction, Account]
    services: [TransactionService, AccountService]

  - name: Reporting
    aggregates: [Report]
    services: [ReportService]

entities:
  - name: User
    namespace: CortexLabs.FinTrack.Domain.Entities
    properties:
      - { name: Id, type: int, key: true }
      - { name: UserName, type: string, required: true, max_length: 50 }
      - { name: Email, type: string, required: true, max_length: 100 }
      - { name: PasswordHash, type: string, required: true }
      - { name: Role, type: UserRole, required: true, default: User }
      - { name: IsActive, type: bool, default: true }
      - { name: CreatedAt, type: DateTime, audit: true }
      - { name: CreatedBy, type: string, audit: true }
      - { name: ModifiedAt, type: DateTime, audit: true }
      - { name: ModifiedBy, type: string, audit: true }
    relationships:
      - { target: Transaction, type: one-to-many, navigation: Transactions }
      - { target: Account, type: one-to-many, navigation: Accounts }
    smells_resolved: [SMELL-7, SMELL-19, SMELL-20]

  - name: Transaction
    namespace: CortexLabs.FinTrack.Domain.Entities
    properties:
      - { name: Id, type: int, key: true }
      - { name: Description, type: string, required: true, max_length: 200 }
      - { name: Amount, type: decimal, required: true, range: "0.01-999999999" }
      - { name: Category, type: TransactionCategory, required: true }
      - { name: Type, type: TransactionType, required: true }
      - { name: Date, type: DateTime, required: true }
      - { name: UserId, type: int, foreign_key: User.Id }
      - { name: CreatedAt, type: DateTime, audit: true }
      - { name: ModifiedAt, type: DateTime, audit: true }
    smells_resolved: [SMELL-7, SMELL-15, SMELL-19, SMELL-20]

  - name: Account
    namespace: CortexLabs.FinTrack.Domain.Entities
    properties:
      - { name: Id, type: int, key: true }
      - { name: Name, type: string, required: true, max_length: 100 }
      - { name: Balance, type: decimal, default: 0 }
      - { name: UserId, type: int, foreign_key: User.Id }
      - { name: AccountType, type: AccountType, required: true }
      - { name: CreatedAt, type: DateTime, audit: true }
      - { name: ModifiedAt, type: DateTime, audit: true }
      - { name: ConcurrencyToken, type: "byte[]", concurrency: true }
    smells_resolved: [SMELL-7, SMELL-19, SMELL-20]

  - name: Report
    namespace: CortexLabs.FinTrack.Domain.Entities
    properties:
      - { name: Id, type: int, key: true }
      - { name: Title, type: string, required: true }
      - { name: Content, type: string }
      - { name: GeneratedBy, type: int, foreign_key: User.Id }
      - { name: GeneratedAt, type: DateTime }
    smells_resolved: [SMELL-7]

enums:
  - name: TransactionType
    values: [Income, Expense]
    replaces: "Magic strings 'income', 'expense' (SMELL-15)"

  - name: TransactionCategory
    values: [Food, Transport, Entertainment, LargePurchase, MediumPurchase, Other]
    replaces: "Magic strings 'food', 'transport', 'entertainment' (SMELL-15)"

  - name: AccountType
    values: [Checking, Savings, Investment]
    replaces: "Untyped string field (SMELL-7)"

  - name: UserRole
    values: [Admin, User]
    replaces: "Magic strings 'admin', 'user' (SMELL-15)"

# Testing Comparison — Before vs After

> **Source:** `BadMonolith/` → `Refactored/` | **Generated by:** CORTEX Phase 22
> From 12 meaningless tests to 25 real assertions with behavioral coverage.

## Test Quality — Before vs After

```mermaid
graph TB
    subgraph "BEFORE — SomeTests.cs (122 LOC)"
        direction TB
        BT1["Test1()<br/>Assert.True(true)"]
        BT2["UserService_Should_Work()<br/>Assert.True(1 == 1)"]
        BT3["Transaction_Has_Properties()<br/>Assert.NotNull(new T())"]
        BT4["Account_Balance_Test()<br/>NO assertion at all"]
        BT5["Should_Validate_Email_Format()<br/>calls method, asserts true"]
        BT6["Legacy_Import_Test()<br/>Assert.True(true)"]
        BT7["Another_User_Test()<br/>duplicate of BT2"]
        BT8["Transaction_Categorization_Works()<br/>Assert.Equal(x, x) — tautology"]
        BT9["AccountTests class<br/>completely empty"]
        BT10["Report_Generation_Test()<br/>Skip — would fail"]
        BT11["Report_Export_Test()<br/>Skip — NotImplementedException"]
        BT12["Can_Connect_To_Database()<br/>try { True } catch { True }"]

        style BT1 fill:#e74c3c,color:#fff
        style BT2 fill:#e74c3c,color:#fff
        style BT3 fill:#ff9f43,color:#fff
        style BT4 fill:#e74c3c,color:#fff
        style BT5 fill:#e74c3c,color:#fff
        style BT6 fill:#e74c3c,color:#fff
        style BT7 fill:#e74c3c,color:#fff
        style BT8 fill:#e74c3c,color:#fff
        style BT9 fill:#95a5a6,color:#fff
        style BT10 fill:#95a5a6,color:#fff
        style BT11 fill:#95a5a6,color:#fff
        style BT12 fill:#e74c3c,color:#fff
    end
```

```mermaid
graph TB
    subgraph "AFTER — 4 Test Classes, 25 Assertions"
        direction TB

        subgraph "UserServiceTests (5 tests)"
            AT1["HashPassword_ShouldReturnSaltedHash<br/>✅ Verifies salt:hash format"]
            AT2["HashPassword_SameInput_DifferentHashes<br/>✅ Verifies unique salts"]
            AT3["VerifyPassword_CorrectPassword_True<br/>✅ Round-trip verification"]
            AT4["VerifyPassword_WrongPassword_False<br/>✅ Negative case"]
            AT5["VerifyPassword_MalformedHash_False<br/>✅ Edge case"]
        end

        subgraph "TransactionServiceTests (4 tests)"
            AT6["AutoCategorize_Theory × 7 cases<br/>✅ Boundary value analysis"]
            AT7["AutoCategorize_BoundaryAt1000<br/>✅ MediumPurchase"]
            AT8["AutoCategorize_BoundaryAt100<br/>✅ Other"]
        end

        subgraph "EmailValidatorTests (1 test, 11 cases)"
            AT9["IsValid_Theory × 11 cases<br/>✅ Valid + invalid + null + edge"]
        end

        subgraph "RepositoryIntegrationTests (5 tests)"
            AT10["UserRepo_CreateAndRetrieve<br/>✅ Full CRUD cycle"]
            AT11["UserRepo_GetByUsername<br/>✅ Lookup by field"]
            AT12["TransactionRepo_CreateAndCount<br/>✅ Aggregate verification"]
            AT13["AccountRepo_Pagination<br/>✅ Page 1 vs Page 2"]
            AT14["UserRepo_Delete<br/>✅ Deletion + confirmation"]
        end

        style AT1 fill:#27ae60,color:#fff
        style AT2 fill:#27ae60,color:#fff
        style AT3 fill:#27ae60,color:#fff
        style AT6 fill:#27ae60,color:#fff
        style AT9 fill:#27ae60,color:#fff
        style AT10 fill:#2ecc71,color:#fff
        style AT13 fill:#2ecc71,color:#fff
    end
```

## Test Pyramid

```mermaid
graph TB
    subgraph "BEFORE — Inverted/Missing Pyramid"
        BP_TOP["🔴 No E2E Tests"]
        BP_MID["🔴 1 'integration' test<br/>(catches exception, passes anyway)"]
        BP_BOT["🔴 11 'unit' tests<br/>(all Assert.True — test nothing)"]
        BP_TOP --- BP_MID --- BP_BOT
    end

    subgraph "AFTER — Proper Pyramid"
        AP_TOP["🟡 E2E Tests<br/>(planned — Phase 6)"]
        AP_MID["🟢 Integration Tests (5)<br/>Real SQLite DB, CRUD cycles,<br/>pagination, cleanup"]
        AP_BOT["🟢 Unit Tests (20)<br/>Hashing, validation, categorization<br/>boundary values, edge cases"]
        AP_TOP --- AP_MID --- AP_BOT
    end

    style BP_TOP fill:#e74c3c,color:#fff
    style BP_MID fill:#e74c3c,color:#fff
    style BP_BOT fill:#e74c3c,color:#fff
    style AP_TOP fill:#f39c12,color:#fff
    style AP_MID fill:#27ae60,color:#fff
    style AP_BOT fill:#27ae60,color:#fff
```

## Test Anti-Pattern Catalogue (Eliminated)

| Anti-Pattern | Before Example | After Fix |
|-------------|---------------|-----------|
| **Tautology** | `Assert.True(true)` | `Assert.Equal(expected, actual)` |
| **Duplicate** | 2 tests with `Assert.True(1==1)` | Unique test per behavior |
| **No Assertion** | `account.Balance = 100; // no assert` | Full round-trip verification |
| **Mock-the-World** | `Assert.Equal("food", "food")` | Call real method, check result |
| **Skipped Forever** | `[Fact(Skip = "Broken since Q1 2023")]` | Implemented or deleted |
| **Empty Class** | `public class AccountTests { }` | Real integration tests |
| **Catch-All Pass** | `catch { Assert.True(true); }` | Proper error assertions |
| **Misleading Name** | `Should_Validate_Email_Format` — doesn't | Name matches behavior tested |

## Coverage Heat Map

```mermaid
graph LR
    subgraph "BEFORE — Coverage"
        BC1["UserService<br/>0% real coverage"]
        BC2["TransactionService<br/>0% real coverage"]
        BC3["Models<br/>0% behavior tested"]
        BC4["Endpoints<br/>0% tested"]
        BC5["SQL Queries<br/>0% tested"]
    end

    subgraph "AFTER — Coverage"
        AC1["UserService<br/>password hash: ✅<br/>verification: ✅"]
        AC2["TransactionService<br/>categorization: ✅<br/>boundaries: ✅"]
        AC3["EmailValidator<br/>11 cases: ✅"]
        AC4["Repositories<br/>CRUD + pagination: ✅"]
        AC5["DatabaseInitializer<br/>schema creation: ✅"]
    end

    BC1 -->|"+5 tests"| AC1
    BC2 -->|"+4 tests"| AC2
    BC3 -->|"+11 cases"| AC3
    BC4 -->|"+5 tests"| AC4
    BC5 -->|"via repos"| AC5

    style BC1 fill:#e74c3c,color:#fff
    style BC2 fill:#e74c3c,color:#fff
    style BC3 fill:#e74c3c,color:#fff
    style AC1 fill:#27ae60,color:#fff
    style AC2 fill:#27ae60,color:#fff
    style AC3 fill:#27ae60,color:#fff
    style AC4 fill:#27ae60,color:#fff
```

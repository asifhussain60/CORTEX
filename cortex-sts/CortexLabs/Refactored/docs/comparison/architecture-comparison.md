# Architecture Comparison — Before vs After

> **Source:** `BadMonolith/` → `Refactored/` | **Generated by:** CORTEX Phase 22

## Before: BadMonolith Architecture (Flat Monolith)

```mermaid
graph TB
    subgraph "BadMonolith — 4 files, 1 project"
        PC["Program.cs<br/>591 LOC — God Class<br/>6 domains mixed"]
        MC["Models.cs<br/>60 LOC — 5 classes<br/>+ static AppCache"]
        SC["Services.cs<br/>184 LOC — 4 classes<br/>circular deps"]
        TC["SomeTests.cs<br/>122 LOC — 12 tests<br/>all Assert.True(true)"]

        PC -->|"direct new()"| SC
        SC -->|"circular dep"| SC
        PC -->|"inline SQL"| DB[(fintrack.db)]
        SC -->|"builds raw SQL"| DB
        MC -->|"static mutable"| PC
        TC -.->|"tests nothing"| PC
    end

    subgraph "Frontend — 2 files"
        APP["app.ts<br/>466 LOC — God Component<br/>all 'any' types"]
        MOD["models.ts<br/>22 LOC — all type = any"]
        APP -->|"direct fetch()"| PC
    end

    style PC fill:#ff6b6b,color:#fff
    style SC fill:#ff6b6b,color:#fff
    style APP fill:#ff6b6b,color:#fff
    style TC fill:#ff9f43,color:#fff
    style MC fill:#ff9f43,color:#fff
    style MOD fill:#ff9f43,color:#fff
```

## After: Refactored Architecture (Clean Architecture)

```mermaid
graph TB
    subgraph "API Layer"
        PRG["Program.cs<br/>~55 LOC — DI + pipeline"]
        UE["UserEndpoints"]
        TE["TransactionEndpoints"]
        AE["AccountEndpoints"]
        RE["ReportEndpoints"]
        HE["HealthEndpoints"]
        EHM["ErrorHandlingMiddleware"]
        RLM["RequestLoggingMiddleware"]
        PRG --> UE & TE & AE & RE & HE
        PRG --> EHM & RLM
    end

    subgraph "Application Layer"
        US["UserService"]
        TS["TransactionService"]
        AS["AccountService"]
        RS["ReportService"]
        EV["EmailValidator"]
        DTO["DTOs + PagedResponse"]
        UE --> US
        TE --> TS
        AE --> AS
        RE --> RS
    end

    subgraph "Domain Layer"
        EU["User Entity"]
        ET["Transaction Entity"]
        EA["Account Entity"]
        EN["Enums (4)"]
        IUR["IUserRepository"]
        ITR["ITransactionRepository"]
        IAR["IAccountRepository"]
        US --> IUR
        TS --> ITR
        AS --> IAR
        RS --> ITR & IAR
    end

    subgraph "Infrastructure Layer"
        UR["UserRepository<br/>parameterized SQL"]
        TR["TransactionRepository<br/>parameterized SQL"]
        AR["AccountRepository<br/>parameterized SQL"]
        DBI["DatabaseInitializer<br/>indexes + FK"]
        CFG["AppSettings"]
        IUR -.->|"implements"| UR
        ITR -.->|"implements"| TR
        IAR -.->|"implements"| AR
        UR & TR & AR --> DB[(fintrack.db)]
    end

    subgraph "Tests — Real Assertions"
        T1["UserServiceTests (5)"]
        T2["TransactionServiceTests (4)"]
        T3["EmailValidatorTests (11)"]
        T4["RepositoryIntegrationTests (5)"]
    end

    subgraph "Frontend — Typed Services"
        FAPP["app.ts<br/>~100 LOC — thin"]
        FAPI["ApiClient<br/>retry + errors"]
        FUS["UserService"]
        FTS["TransactionService"]
        FAS["AccountService"]
        FMOD["4 typed models"]
        FUTIL["validators + formatter"]
        FAPP --> FUS & FTS & FAS
        FUS & FTS & FAS --> FAPI
        FAPP --> FUTIL
    end

    style PRG fill:#2ecc71,color:#fff
    style US fill:#2ecc71,color:#fff
    style TS fill:#2ecc71,color:#fff
    style EU fill:#3498db,color:#fff
    style ET fill:#3498db,color:#fff
    style UR fill:#9b59b6,color:#fff
    style TR fill:#9b59b6,color:#fff
    style FAPP fill:#2ecc71,color:#fff
    style FAPI fill:#2ecc71,color:#fff
```

## Side-by-Side Metrics

| Metric | Before (BadMonolith) | After (Refactored) | Delta |
|--------|---------------------|--------------------:|------:|
| Backend files | 4 (.cs) | 35 (.cs/.csproj/.sln) | +31 |
| Frontend files | 3 (.ts/.html) | 13 (.ts/.html/.json) | +10 |
| Total LOC (backend) | ~957 | ~2,200 | +130% |
| Program.cs LOC | 591 (God Class) | ~55 (thin DI) | −91% |
| app.ts LOC | 466 (God Component) | ~100 (thin) | −79% |
| C# projects | 1 | 5 (Domain/App/Infra/Api/Tests) | +4 |
| Domains in one file | 6 | 1 per file | −83% |
| Test quality | 12 × Assert.True(true) | 25 real assertions | +25 |
| `any` types (TS) | 22+ | 0 | −100% |
| SQL injection points | 4 | 0 | −100% |
| Hardcoded secrets | 5 | 0 | −100% |
| CORS policy | AllowAnyOrigin() | WithOrigins(config) | ✅ Fixed |
| Pagination | None | All list endpoints | ✅ Added |
| Error handling | Empty catch / stack trace | ProblemDetails (RFC 7807) | ✅ Fixed |
| Dead code classes | 2 (NotificationService, ReportGenerator) | 0 | −100% |

# Code Quality Comparison — Before vs After

> **Source:** `BadMonolith/` → `Refactored/` | **Generated by:** CORTEX Phase 22
> 25 annotated code smells resolved across 5 categories.

## Smell Distribution — Before vs After

```mermaid
pie title "BadMonolith — 25 Code Smells by Category"
    "Security (P0)" : 5
    "SOLID Violations" : 9
    "Code Quality" : 7
    "Performance" : 2
    "Testing" : 2
```

```mermaid
pie title "Refactored — 0 Code Smells Remaining"
    "Resolved" : 25
    "Remaining" : 0
```

## SOLID Principles — Before vs After

```mermaid
graph TB
    subgraph "BEFORE — SOLID Violations"
        direction TB
        S1["❌ SRP: Program.cs = 6 domains<br/>591 LOC God Class"]
        O1["❌ OCP: Adding domain =<br/>modify Program.cs"]
        L1["❌ LSP: No interfaces,<br/>no substitution possible"]
        I1["❌ ISP: Services are<br/>monolithic classes"]
        D1["❌ DIP: new UserService()<br/>direct instantiation everywhere"]
    end

    subgraph "AFTER — SOLID Compliant"
        direction TB
        S2["✅ SRP: 1 class = 1 responsibility<br/>UserService, TransactionService, etc."]
        O2["✅ OCP: Add domain =<br/>new endpoint file + service"]
        L2["✅ LSP: IUserRepository<br/>→ UserRepository (swappable)"]
        I2["✅ ISP: Focused interfaces<br/>IUserRepo, ITransactionRepo"]
        D2["✅ DIP: Constructor injection<br/>via DI container"]
    end

    S1 -->|"REFACTOR"| S2
    O1 -->|"REFACTOR"| O2
    L1 -->|"REFACTOR"| L2
    I1 -->|"REFACTOR"| I2
    D1 -->|"REFACTOR"| D2

    style S1 fill:#e74c3c,color:#fff
    style O1 fill:#e74c3c,color:#fff
    style L1 fill:#e74c3c,color:#fff
    style I1 fill:#e74c3c,color:#fff
    style D1 fill:#e74c3c,color:#fff
    style S2 fill:#27ae60,color:#fff
    style O2 fill:#27ae60,color:#fff
    style L2 fill:#27ae60,color:#fff
    style I2 fill:#27ae60,color:#fff
    style D2 fill:#27ae60,color:#fff
```

## Naming Convention Violations (SMELL-7)

```mermaid
graph LR
    subgraph "BEFORE — Mixed Conventions in Models.cs"
        M1["user_name ← snake_case"]
        M2["Email ← PascalCase"]
        M3["password_hash ← snake_case"]
        M4["accountName ← camelCase"]
        M5["is_active ← snake_case"]
        M6["category_name ← snake_case"]
    end

    subgraph "AFTER — Consistent PascalCase"
        N1["Username"]
        N2["Email"]
        N3["PasswordHash"]
        N4["Name"]
        N5["Role (enum)"]
        N6["Category (enum)"]
    end

    M1 -->|"rename"| N1
    M2 -->|"keep"| N2
    M3 -->|"rename"| N3
    M4 -->|"rename"| N4
    M5 -->|"replace"| N5
    M6 -->|"replace"| N6

    style M1 fill:#e74c3c,color:#fff
    style M3 fill:#e74c3c,color:#fff
    style M4 fill:#e74c3c,color:#fff
    style M5 fill:#e74c3c,color:#fff
    style M6 fill:#e74c3c,color:#fff
    style N1 fill:#27ae60,color:#fff
    style N3 fill:#27ae60,color:#fff
    style N4 fill:#27ae60,color:#fff
    style N5 fill:#27ae60,color:#fff
    style N6 fill:#27ae60,color:#fff
```

## Dependency Graph — Before vs After

### Before: Circular Dependencies + Direct Instantiation

```mermaid
graph TD
    PCS["Program.cs"] -->|"new()"| US["UserService"]
    PCS -->|"new()"| TS["TransactionService"]
    US -->|"new()"| TS2["TransactionService<br/>(2nd instance!)"]
    TS -->|"SetUserService()"| US
    US -->|"new HttpClient()"| HC["HttpClient<br/>(unmanaged)"]
    NS["NotificationService<br/>(dead code)"] -.->|"never called"| PCS
    RG["ReportGenerator<br/>(dead code)"] -.->|"never called"| PCS

    style US fill:#e74c3c,color:#fff
    style TS fill:#e74c3c,color:#fff
    style TS2 fill:#ff6b6b,color:#fff
    style NS fill:#95a5a6,color:#fff
    style RG fill:#95a5a6,color:#fff
```

### After: Clean Dependency Injection (Acyclic)

```mermaid
graph TD
    DI["DI Container<br/>(Program.cs)"]
    DI -->|"singleton"| IUR["IUserRepository"]
    DI -->|"singleton"| ITR["ITransactionRepository"]
    DI -->|"singleton"| IAR["IAccountRepository"]
    DI -->|"scoped"| US["UserService"]
    DI -->|"scoped"| TS["TransactionService"]
    DI -->|"scoped"| AS["AccountService"]
    DI -->|"scoped"| RS["ReportService"]

    US -->|"injected"| IUR
    TS -->|"injected"| ITR
    AS -->|"injected"| IAR
    RS -->|"injected"| ITR
    RS -->|"injected"| IAR

    IUR -.->|"implements"| UR["UserRepository"]
    ITR -.->|"implements"| TR["TransactionRepository"]
    IAR -.->|"implements"| AR["AccountRepository"]

    style DI fill:#3498db,color:#fff
    style US fill:#27ae60,color:#fff
    style TS fill:#27ae60,color:#fff
    style AS fill:#27ae60,color:#fff
    style RS fill:#27ae60,color:#fff
    style IUR fill:#9b59b6,color:#fff
    style ITR fill:#9b59b6,color:#fff
    style IAR fill:#9b59b6,color:#fff
```

## Dead Code Elimination

| Dead Code Item | Location (Before) | Status (After) |
|---------------|-------------------|----------------|
| `NotificationService` class (62 LOC) | Services.cs | 🗑️ Deleted |
| `ReportGenerator` class (25 LOC) | Services.cs | 🗑️ Deleted |
| `SendWelcomeEmail()` method | UserService | 🗑️ Deleted |
| `ExportUserData()` method | UserService | 🗑️ Deleted |
| `GetUserCreditScore()` method | UserService | 🗑️ Deleted |
| `ArchiveOldTransactions()` method | TransactionService | 🗑️ Deleted |
| `calculateCompoundInterest()` function | app.ts | 🗑️ Deleted |
| `generatePdfReport()` function | app.ts | 🗑️ Deleted |
| `exportToExcel()` function | app.ts | 🗑️ Deleted |
| `AppCache` static class | Models.cs | 🗑️ Deleted |
| `Report` model class | Models.cs | 🗑️ Deleted |
| **Total dead code removed** | | **~200 LOC** |

## Smell-to-Fix Traceability Matrix

| Smell | Category | Before (File) | After (Fix) |
|-------|----------|---------------|-------------|
| SMELL-1 | 🔴 Security | SQL string concat in Program.cs + Services.cs | `@param` in all Repository classes |
| SMELL-2 | 🔴 Security | Password hash exposed in GET response | `UserDto` excludes `PasswordHash` |
| SMELL-3 | 🔴 SOLID | God Class — 591 LOC Program.cs | 5 endpoint files, 4 services |
| SMELL-4 | 🔴 SOLID | Infrastructure in startup | `DatabaseInitializer` class |
| SMELL-5 | 🔴 SOLID | Circular dep UserService ↔ TransactionService | Constructor injection, no cycles |
| SMELL-6 | 🟡 Perf | No pagination anywhere | `PagedResponse<T>` + LIMIT/OFFSET |
| SMELL-7 | 🟡 Quality | Mixed naming (snake/camel/Pascal) | PascalCase for C#, camelCase for TS |
| SMELL-8 | 🟡 Quality | 6 dead methods + 2 dead classes | All removed |
| SMELL-9 | 🟡 Quality | No API versioning | `/api/v1/` prefix |
| SMELL-10 | 🟡 Quality | Duplicate email validation (3 copies) | Single `EmailValidator` class |
| SMELL-11 | 🟡 Quality | `Console.WriteLine` everywhere | `ILogger<T>` structured logging |
| SMELL-12 | 🔴 Testing | 12 × `Assert.True(true)` | 25 real assertions (Theory + Fact) |
| SMELL-13 | 🔴 Security | CORS `AllowAnyOrigin()` | `WithOrigins(config[])` |
| SMELL-14 | 🟡 Perf | No retry on HTTP calls | `ApiClient` with retry + backoff |
| SMELL-15 | 🟡 Quality | Magic numbers/strings (15+ instances) | Enums + config constants |
| SMELL-16 | 🔴 SOLID | Global mutable `AppCache` | Stateless services, DI scoping |
| SMELL-17 | 🔴 SOLID | No DI — `new Service()` everywhere | Built-in DI container |
| SMELL-18 | 🔴 Security | `ex.ToString()` in response | `ErrorHandlingMiddleware` |
| SMELL-19 | 🟡 Quality | No validation attributes | `[Required]` `[Range]` `[Email]` |
| SMELL-20 | 🟡 Quality | No audit fields | `CreatedAt`, `UpdatedAt` on all entities |
| SMELL-21 | 🔴 SOLID | God Component — 466 LOC app.ts | ~100 LOC thin app.ts + services |
| SMELL-22 | 🟡 Quality | `any` everywhere in TypeScript | Typed interfaces, `strict: true` |
| SMELL-23 | 🔴 SOLID | Biz logic in UI layer | Service layer + utils |
| SMELL-24 | 🔴 SOLID | Direct `fetch()` calls | `ApiClient` service abstraction |
| SMELL-25 | 🟡 Quality | No error handling (silent failures) | try/catch + error banner + retry |

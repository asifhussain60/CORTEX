# Data Model Comparison — Before vs After

> **Source:** `BadMonolith/backend/Models/Models.cs` → `Refactored/backend/CortexLabs.FinTrack.Domain/`
> **Generated by:** CORTEX Phase 22

## Entity-Relationship — Before vs After

### Before: Flat Models (No Relationships, No Constraints)

```mermaid
erDiagram
    Users {
        INTEGER Id PK
        TEXT user_name "❌ snake_case"
        TEXT Email
        TEXT password_hash "❌ plaintext despite name"
        TEXT Role "❌ string, no enum"
        INTEGER is_active "❌ snake_case"
    }

    Transactions {
        INTEGER Id PK
        TEXT description "❌ camelCase"
        REAL Amount
        TEXT category_name "❌ snake_case"
        TEXT Type "❌ string, no enum"
        TEXT Date
        INTEGER UserId "❌ no FK constraint"
    }

    Accounts {
        INTEGER Id PK
        TEXT accountName "❌ camelCase"
        REAL Balance
        INTEGER user_id "❌ snake_case, no FK"
        TEXT account_type "❌ snake_case"
    }

    Reports {
        INTEGER Id PK
        TEXT title "❌ camelCase"
        TEXT Content
        INTEGER generated_by "❌ snake_case, no FK"
        TEXT generated_at "❌ snake_case"
    }

    Users ||--o{ Transactions : "no FK enforced"
    Users ||--o{ Accounts : "no FK enforced"
    Users ||--o{ Reports : "no FK enforced"
```

### After: Normalized with Constraints, FKs, Indexes

```mermaid
erDiagram
    Users {
        INTEGER Id PK
        TEXT Username UK "✅ PascalCase, UNIQUE"
        TEXT Email UK "✅ UNIQUE"
        TEXT PasswordHash "✅ SHA256+salt"
        TEXT Role "✅ enum: Admin | User"
        TEXT CreatedAt "✅ audit field"
        TEXT UpdatedAt "✅ audit field"
    }

    Transactions {
        INTEGER Id PK
        INTEGER UserId FK "✅ FK → Users(Id) CASCADE"
        REAL Amount "✅ NOT NULL"
        TEXT Type "✅ enum: Income | Expense"
        TEXT Category "✅ enum: 6 values"
        TEXT Description "✅ PascalCase"
        TEXT Date "✅ NOT NULL"
        TEXT CreatedAt "✅ audit"
        TEXT UpdatedAt "✅ audit"
    }

    Accounts {
        INTEGER Id PK
        INTEGER UserId FK "✅ FK → Users(Id) CASCADE"
        TEXT Name "✅ PascalCase"
        TEXT Type "✅ enum: Checking | Savings | Investment"
        REAL Balance "✅ NOT NULL DEFAULT 0"
        TEXT Currency "✅ 3-char ISO"
        TEXT CreatedAt "✅ audit"
        TEXT UpdatedAt "✅ audit"
    }

    Users ||--o{ Transactions : "FK with CASCADE"
    Users ||--o{ Accounts : "FK with CASCADE"
```

## Schema Changes Detail

```mermaid
graph TB
    subgraph "REMOVED"
        R1["Reports table<br/>(dead — never queried)"]
        R2["AppCache static class<br/>(global mutable state)"]
        style R1 fill:#95a5a6,color:#fff
        style R2 fill:#95a5a6,color:#fff
    end

    subgraph "ADDED"
        A1["FOREIGN KEY constraints<br/>ON DELETE CASCADE"]
        A2["UNIQUE constraints<br/>Username, Email"]
        A3["Indexes<br/>IX_Transactions_UserId<br/>IX_Accounts_UserId<br/>IX_Users_Username<br/>IX_Users_Email"]
        A4["Audit columns<br/>CreatedAt, UpdatedAt<br/>on all 3 tables"]
        A5["NOT NULL constraints<br/>on all required fields"]
        A6["DEFAULT values<br/>Role='User', Balance=0, Currency='USD'"]
        style A1 fill:#27ae60,color:#fff
        style A2 fill:#27ae60,color:#fff
        style A3 fill:#3498db,color:#fff
        style A4 fill:#27ae60,color:#fff
        style A5 fill:#27ae60,color:#fff
        style A6 fill:#27ae60,color:#fff
    end

    subgraph "RENAMED"
        RN1["user_name → Username"]
        RN2["password_hash → PasswordHash"]
        RN3["is_active → (removed — use Role)"]
        RN4["description → Description"]
        RN5["category_name → Category (enum)"]
        RN6["accountName → Name"]
        RN7["user_id → UserId"]
        RN8["account_type → Type (enum)"]
        style RN1 fill:#f39c12,color:#fff
        style RN2 fill:#f39c12,color:#fff
        style RN4 fill:#f39c12,color:#fff
        style RN5 fill:#f39c12,color:#fff
    end
```

## Type System — Before vs After

```mermaid
graph LR
    subgraph "BEFORE — Magic Strings"
        S1["Role = 'admin' | 'user'<br/>accepts any string"]
        S2["Type = 'income' | 'expense'<br/>accepts 'HACKED'"]
        S3["account_type = free text<br/>no validation"]
        S4["category_name = free text<br/>inconsistent values"]
    end

    subgraph "AFTER — Enums"
        E1["UserRole<br/>Admin, User"]
        E2["TransactionType<br/>Income, Expense"]
        E3["AccountType<br/>Checking, Savings, Investment"]
        E4["TransactionCategory<br/>Food, Transport, Entertainment,<br/>LargePurchase, MediumPurchase, Other"]
    end

    S1 -->|"typed"| E1
    S2 -->|"typed"| E2
    S3 -->|"typed"| E3
    S4 -->|"typed"| E4

    style S1 fill:#e74c3c,color:#fff
    style S2 fill:#e74c3c,color:#fff
    style S3 fill:#e74c3c,color:#fff
    style S4 fill:#e74c3c,color:#fff
    style E1 fill:#27ae60,color:#fff
    style E2 fill:#27ae60,color:#fff
    style E3 fill:#27ae60,color:#fff
    style E4 fill:#27ae60,color:#fff
```

## Column-Level Change Matrix

| Table | Column (Before) | Column (After) | Change |
|-------|----------------|----------------|--------|
| Users | `user_name TEXT` | `Username TEXT NOT NULL UNIQUE` | Renamed + constraint |
| Users | `Email TEXT` | `Email TEXT NOT NULL UNIQUE` | Added NOT NULL + UNIQUE |
| Users | `password_hash TEXT` (plaintext!) | `PasswordHash TEXT NOT NULL` (SHA256+salt) | Renamed + hashed |
| Users | `Role TEXT DEFAULT 'user'` | `Role TEXT NOT NULL DEFAULT 'User'` | Enum-backed |
| Users | `is_active INTEGER` | *(removed)* | Role-based instead |
| Users | *(missing)* | `CreatedAt TEXT NOT NULL` | Added audit |
| Users | *(missing)* | `UpdatedAt TEXT NOT NULL` | Added audit |
| Transactions | `description TEXT` | `Description TEXT NOT NULL DEFAULT ''` | Renamed + default |
| Transactions | `Amount REAL` | `Amount REAL NOT NULL` | Added NOT NULL |
| Transactions | `category_name TEXT` | `Category TEXT NOT NULL DEFAULT 'Other'` | Renamed + enum |
| Transactions | `Type TEXT` | `Type TEXT NOT NULL` | Added NOT NULL + enum |
| Transactions | `UserId INTEGER` | `UserId INTEGER NOT NULL FK` | Added FK + CASCADE |
| Transactions | *(missing)* | `CreatedAt, UpdatedAt` | Added audit |
| Accounts | `accountName TEXT` | `Name TEXT NOT NULL` | Renamed + NOT NULL |
| Accounts | `Balance REAL` | `Balance REAL NOT NULL DEFAULT 0` | Added default |
| Accounts | `user_id INTEGER` | `UserId INTEGER NOT NULL FK` | Renamed + FK |
| Accounts | `account_type TEXT` | `Type TEXT NOT NULL DEFAULT 'Checking'` | Renamed + enum |
| Accounts | *(missing)* | `Currency TEXT NOT NULL DEFAULT 'USD'` | Added |
| Accounts | *(missing)* | `CreatedAt, UpdatedAt` | Added audit |
| Reports | *(entire table)* | *(removed)* | Dead table deleted |

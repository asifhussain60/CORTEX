# CortexLabs FinTrack — Refactored

> ✅ **Production-grade refactoring** generated by CORTEX's onboarding pipeline.
> Source: `cortex-sts/CortexLabs/BadMonolith/` (25 intentional code smells → 0 remaining)

## Architecture

```
Refactored/
├── docs/                        # SDLC Documentation (generated by CORTEX)
│   ├── requirements/            # Business requirements extraction
│   ├── design/                  # Architecture design + diagrams
│   │   ├── adrs/                # Architecture Decision Records
│   │   └── diagrams/            # Mermaid diagrams (component, ER, sequence, deployment)
│   └── estimation/              # Effort estimation (PERT + Story Points)
├── cortex-registry/             # Local governance registry (mirrors CORTEX pattern)
│   └── planning/
│       └── phases/
├── backend/                     # C# Clean Architecture
│   ├── CortexLabs.FinTrack.Api/
│   ├── CortexLabs.FinTrack.Domain/
│   ├── CortexLabs.FinTrack.Application/
│   ├── CortexLabs.FinTrack.Infrastructure/
│   └── CortexLabs.FinTrack.Tests/
├── company/                     # Simulated company domain (mirrors cortex-registry/company/)
└── frontend/                    # TypeScript (strict, service layer)
    └── src/
        ├── models/
        ├── services/
        └── utils/
```

## Smells Resolved

| Category | Count | Examples |
|----------|-------|---------|
| Security | 5 | SQL injection → parameterized queries, secrets → env vars |
| SOLID | 9 | God class → Clean Architecture, DI container, service layer |
| Quality | 11 | Naming conventions, dead code removed, validation attributes |
| Performance | 2 | Pagination added, database indexes |
| Testing | 1 | Assert.True(true) → behavior-driven tests with real xUnit assertions |

## CORTEX Pipeline Used

```
cortex_onboard_repository_v3 → LENS scan
    → Requirements extraction (docs/requirements/)
    → Design documentation (docs/design/)
    → Estimation (docs/estimation/)
    → Phase plan (cortex-registry/planning/)
    → TDDOrchestrator → RED/GREEN/REFACTOR
    → SecurityOrchestrator → P0 security fixes
    → RefactoringOrchestrator → SOLID remediation
    → Output → Refactored/
```

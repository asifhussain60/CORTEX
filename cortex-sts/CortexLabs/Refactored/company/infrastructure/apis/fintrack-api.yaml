# ══════════════════════════════════════════════════════════════════════════════
# API Catalog: FinTrack API — CortexLabs
# ══════════════════════════════════════════════════════════════════════════════
# Generated by: CORTEX OnboardingOrchestrator (API detection)
# Mirrors: cortex-registry/company/infrastructure/apis/ pattern
# Date: 2026-02-22
# ══════════════════════════════════════════════════════════════════════════════

api:
  name: fintrack-api
  version: v1
  base_url: /api/v1
  technology: "ASP.NET Core 8.0 Minimal APIs"
  documentation: "Swagger/OpenAPI via Swashbuckle"
  authentication: JWT Bearer

  endpoints_summary:
    total: 13
    by_domain:
      users: 3       # GET list, GET search, POST create
      transactions: 3 # GET list, GET search, POST create
      accounts: 2     # GET list, POST transfer
      reports: 2      # GET list, POST generate
      auth: 1         # POST login
      analytics: 1    # GET summary
      health: 1       # GET health

  security_posture:
    sql_injection: protected  # All queries parameterized
    cors: configured          # Allowlist, not wildcard
    secrets: environment      # No hardcoded values
    error_handling: sanitized # ProblemDetails, no stack traces
    authentication: jwt       # Bearer token required

  dependencies:
    packages:
      - { name: "Microsoft.Data.Sqlite", version: "8.0.0" }
      - { name: "Swashbuckle.AspNetCore", version: "6.5.0" }
    external_apis: none  # No external API calls in refactored version

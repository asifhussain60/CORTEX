---
title: Multi‑Repo & MCP Overview
---

CORTEX supports cross-repo operations via:

- **MCP Server** (JSON‑RPC 2.0)
- **Multi‑Repo Manager**
- A repo registry (e.g., `repos.yaml`)

Each repo can provide its own `.cortex/` governance and best practices. Company brains are isolated by default.

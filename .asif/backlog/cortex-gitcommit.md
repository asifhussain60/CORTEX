Found an issue in cortex-gitcommit.md. I've noticed when I delete files in one machine and pull on another machines, the deleted filesare not being removed. I think this is because git is not tracking deletions properly across different environments.


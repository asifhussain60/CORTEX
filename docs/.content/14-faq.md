# FAQ

## How does V2 stay adaptive to new model capabilities?
By updating capability boundaries in `cortex-registry/core/llm-capabilities.yaml`.

## Where is execution tracked?
In `cortex-registry/cortex-master-v2.yaml` plus linked phase detail files.

## How is safety enforced?
Through mandatory governance and validation gates in workflows.
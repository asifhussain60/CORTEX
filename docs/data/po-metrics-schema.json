{
  "$schema": "https://cortex.ai/schemas/po-metrics-schema/v1",
  "description": "D3.js-compatible schema for CORTEX PO Intelligence Dashboard. Generated by POMetricsDashboard.",
  "version": "1.0.0",
  "velocity_trend": {
    "type": "array",
    "description": "Sprint velocity over time — used by D3 line/bar charts",
    "items": {
      "type": "object",
      "properties": {
        "sprint": { "type": "string", "description": "Sprint label e.g. 'Sprint 12'" },
        "points": { "type": "number", "description": "Story points completed" }
      },
      "required": ["sprint", "points"]
    }
  },
  "cycle_time_distribution": {
    "type": "array",
    "description": "Per-story cycle time in days — used by D3 histogram",
    "items": {
      "type": "object",
      "properties": {
        "story_id": { "type": "string" },
        "days": { "type": "number" }
      },
      "required": ["story_id", "days"]
    }
  },
  "predictability_score": {
    "type": "number",
    "minimum": 0,
    "maximum": 100,
    "description": "% of sprints meeting ≥90% of velocity baseline"
  },
  "blocked_themes": {
    "type": "array",
    "items": { "type": "string" },
    "description": "Recurring blocked themes extracted from work item tags and descriptions"
  }
}

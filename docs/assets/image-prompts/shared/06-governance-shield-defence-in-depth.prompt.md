# CORTEX Image Prompt: Governance Shield — Defence in Depth
# Role: Shared (all roles)
# Output: docs/assets/images/generated/shared/06-governance-shield-defence-in-depth.png
# Size: 1200x675 (16:9 landscape)
# Style: Dark glassmorphism, immutable architectural diagram
# Immutability: YES — the 3-layer enforcement model (commit/build/runtime) is foundational

## DALL-E Prompt

Create a professional shield-layered governance visualization for CORTEX's three enforcement layers. Dark navy background (#0a0e27) with glassmorphism aesthetic. This visualises how governance operates as concentric rings of protection — like a medieval castle with multiple walls.

CRITICAL LAYOUT RULE: All content must be inset within a safe zone with a minimum 60px margin on all four sides (left, right, top, bottom). No element, text, icon, border, glow, or card should touch or bleed into the outer 60px edge boundary. The outer margin area must remain the plain dark navy background (#0a0e27) with no content.

The image should show:
- THREE concentric shield rings (or fortress walls), each a glassmorphism arc:
  1. OUTER RING — "At Commit Time" (amber #f59e0b glow): "10 enforcement agents scan before code enters version control. Like the outer wall of a castle — most threats stopped here."
  2. MIDDLE RING — "During the Build" (purple #7b61ff glow): "Full governance suite runs in parallel with tests. Like the inner wall — catches anything that got past the gate."
  3. INNER RING — "During Execution" (cyan #00d4ff glow): "Governance gate runs before any files are touched. Like the keep — the last, strongest defence."
- At the center of all rings: a glowing "✓ Zero Violations" badge (emerald #10b981)
- Small icons along each ring showing specific checks: secret scanning (key icon), type checking (check icon), naming (tag icon), security (shield icon), duplicate detection (copy icon)
- An arrow from outside labeled "Code Change" penetrating inward — stopped at the outer ring with a "BLOCKED" label if a violation is found
- A second arrow showing clean code passing through all three rings to the green center
- Small counter badge: "60+ Active Rules" near the top
- Clean, authoritative, immediately communicates defence-in-depth
- No photographic elements — pure architectural/shield visualization
- No logo, wordmark, title text, or branding in any corner — all four corners must remain empty dark navy background

Style: Concentric shield diagram. Dark glassmorphism. Castle-defence metaphor. Enterprise reference quality. All glow effects must terminate well within the safe zone.

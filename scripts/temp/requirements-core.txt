# CORTEX Core Dependencies (Tier 1 - Essential Runtime)
# Installation time: ~2-3 minutes
# These are REQUIRED for basic CORTEX functionality

# Testing Framework
pytest>=8.4.0
pytest-cov>=6.0.0
pytest-asyncio>=1.3.0

# Configuration & Parsing
PyYAML>=6.0.2
python-dateutil>=2.8.2
pydantic>=2.0.0

# File System & Process Monitoring
watchdog>=6.0.0
psutil>=6.1.1
send2trash>=1.8.3

# Code Analysis (Lightweight)
parso>=0.8.5  # Python parser (no compilation needed)
sqlparse>=0.5.0  # SQL parsing
tomli>=2.0.0; python_version < "3.11"  # TOML parsing

# Document Parsing (Lightweight)
python-docx>=1.1.0  # Word documents
pypdf>=6.4.1  # PDF extraction
esprima>=4.0.1  # JavaScript AST

# HTTP Client (Lightweight)
requests>=2.31.0

# ============================================================================
# Total: ~20 packages, ~50 MB, ~2-3 minute install
# ============================================================================

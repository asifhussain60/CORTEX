```prompt
# CORTEX Image Prompt: AI Context Intelligence Pipeline
# Role: Software Engineer
# Output: cortex-docs/assets/images/generated/software-engineer/03-ai-context-intelligence-pipeline.png
# Size: 1200x675 (16:9 landscape)
# Style: Dark glassmorphism, technical, multi-stage pipeline
# Phase: 121 — AI Context Intelligence

## DALL-E Prompt

Create a professional diagram illustrating the CORTEX AI Context Intelligence pipeline. Dark navy background (#0a0e27) with glassmorphism aesthetic. This visualizes how CORTEX scans, classifies, guards, and disseminates AI artifacts from external repositories.

CRITICAL LAYOUT RULE: All content must be inset within a safe zone with a minimum 60px margin on all four sides (left, right, top, bottom). No element, text, icon, border, glow, or card should touch or bleed into the outer 60px edge boundary. The outer margin area must remain the plain dark navy background (#0a0e27) with no content. This ensures clean rendering with no cropped or edge-clipped elements.

The image should show:
- FOUR sequential stages flowing left-to-right as glassmorphism panels:
  1. "SCAN" (cyan glow): Icon representing file detection, 8 vendor logos (GitHub Copilot, Cursor, Claude, Windsurf, Cline, Continue, Tabnine, OpenAI). Shows multi-vendor AI artifact detection.
  2. "CLASSIFY" (purple glow): Icon representing structured extraction — tools, permissions, personas, rules being extracted from unstructured markdown.
  3. "GUARD" (amber/warning glow): Icon representing PII protection — shield symbol, redacting author names, emails, API keys. Security checkpoint.
  4. "DISSEMINATE" (emerald glow): Icon representing distribution to 5 registry destinations: knowledge/, governance/, config/, patterns/, metrics/. Knowledge spreading.
- Flowing arrows connecting each stage left-to-right
- Input arrow on left showing "Repository AI Artifacts" (copilot-instructions.md, .cursorrules, etc.)
- Output arrow on right showing "Enriched Registry YAML Hierarchy"
- Small annotation: "8 vendors → 5 destinations"
- Clean, technical, illustrating the intelligence pipeline concept
- No photographic elements — pure data flow / pipeline diagram
- No logo, wordmark, title text, or branding in any corner — all four corners must remain empty dark navy background

Style: Four-stage horizontal pipeline. Dark glassmorphism. Security-conscious design. Engineering documentation quality. All glow effects must terminate well within the safe zone — no glow or blur should bleed to the canvas edge.

```

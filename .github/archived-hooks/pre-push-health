#!/usr/bin/env python3
"""Git Pre-Push Hook - Health Warning Integration

Integrates CORTEX health warnings into git pre-push workflow.
Warns about health issues but allows push.

Usage:
    This hook is auto-installed by setup scripts.
    To skip: git push --no-verify

Author: CORTEX Framework
Phase: PHASE-95 S4
"""

import sys
from pathlib import Path

# Add project root to path
project_root = Path(__file__).parent.parent
sys.path.insert(0, str(project_root))

from cortex.orchestrators.health.hooks.pre_push_health import main

if __name__ == "__main__":
    sys.exit(main())

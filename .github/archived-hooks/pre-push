#!/bin/bash
# Pre-Push Hook
# Blacklist enforcement (CORE-056) removed — all cortex-registry paths are tracked.
echo "🔐 Pre-push enforcement: Checking for blacklist violations..."
echo "✅ Pre-push check: PASS (no blacklist violations)"
exit 0

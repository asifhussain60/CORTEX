# CORTEX Dependencies
# Phase 0: Tier 0 (Instinct Layer)

# Testing
pytest>=8.4.0
pytest-cov>=6.0.0
pytest-asyncio>=1.3.0

# YAML parsing
PyYAML>=6.0.2

# Date parsing (for YAML datetime fields)
python-dateutil>=2.8.2

# Phase 1 & 2: SQLite with FTS5 (coming soon)
# No additional dependencies needed - uses Python's built-in sqlite3

# Phase 2: Ambient Capture
watchdog>=6.0.0  # File system monitoring (plan_sync_manager, smart_cache_manager)
psutil>=6.1.1  # Process/memory monitoring (healthcheck, performance monitoring, crawlers)
send2trash>=1.8.3  # OS-native Recycle Bin support (sweeper_plugin)

# Phase 1.5: Token Optimization System
scikit-learn>=1.5.2  # ML context compression (TF-IDF vectorization) - compatible with Python 3.9
numpy>=1.26.4,<2.0.0  # Required by scikit-learn - Python 3.9 compatible

# Configuration & Data Validation
pydantic>=2.0.0  # Data validation for user path configuration (setup system)

# Dashboard Testing (P2 - Stub/Mock Remediation)
playwright>=1.48.0  # Browser automation for dashboard generation tests
# Installation: pip install playwright && playwright install

# NOTE: Development tools (black, flake8, mypy, radon, pylint, vulture) moved to optional-requirements.txt
# These are now lazy-loaded when first used by validation/formatting/analysis commands
# Install manually with: pip install -r optional-requirements.txt

# Enhanced Feedback System (CORTEX 3.0)
PyGithub>=2.5.0  # GitHub Gist integration for feedback sharing (enhanced_feedback_module)

# Multi-Language AST Parsing (CORTEX 3.10) - RESTORED ✅
tree-sitter>=0.22.0  # Modern tree-sitter with pre-built binaries (NO compilation required)
tree-sitter-python>=0.25.0  # Python AST parser
tree-sitter-javascript>=0.25.0  # JavaScript/JSX parser
tree-sitter-typescript>=0.23.2  # TypeScript/TSX parser
tree-sitter-c-sharp>=0.23.1  # C# parser (14,459 files across projects)
tree-sitter-sql>=0.3.11  # SQL parser (7,284 files across projects)
tree-sitter-html>=0.23.2  # HTML parser
tree-sitter-css>=0.25.0  # CSS/SCSS parser
tree-sitter-json>=0.24.8  # JSON parser
tree-sitter-yaml>=0.7.2  # YAML parser
tree-sitter-markdown>=0.5.1  # Markdown parser
tree-sitter-java>=0.23.5  # Java parser (future Android support)
tree-sitter-kotlin>=1.1.0  # Kotlin parser (future Android support)

# CORTEX Lens Multi-Language Analysis - Fallback Parsers 🆕
parso>=0.8.5  # Error-recovery Python parser (587k users, powers Jedi autocomplete)
sqlparse>=0.5.0  # SQL parsing with multi-dialect support (15+ years, production-grade)
tomli>=2.0.0; python_version < "3.11"  # TOML parsing backport (3.11+ uses stdlib tomllib)

# Universal Document Conversion (Word/PDF to Markdown)
python-docx>=1.1.0  # Word document parsing (document_converter, policy_analyzer)
pypdf>=6.4.1  # PDF text extraction - MODERN replacement for deprecated PyPDF2 (drop-in API)
# Note: pandoc (external tool) provides best quality but requires separate installation
# pdftotext (poppler-utils) is optional external tool for faster PDF extraction

# Application Onboarding System (CORTEX 3.2 - Phase 1)
networkx>=3.1  # Graph analysis for architecture visualization
tomli>=2.0.0; python_version < '3.11'  # TOML parsing (backport for Python <3.11, stdlib in 3.11+)

# Dashboard UI Testing (CORTEX 3.8+)
selenium>=4.15.0  # Browser automation for dashboard testing
pytest-selenium>=4.0.0  # pytest integration for Selenium

# Dashboard System - Technology Risk Scorer (CORTEX 3.8)
requests>=2.31.0  # HTTP client for endoflife.date API integration
# Note: astroid and requirements-parser omitted (not strictly needed for simplified onboarding)

# Orchestration Analytics Dashboard (CORTEX 3.8.1 - Feature 15)
matplotlib>=3.5.0  # Chart generation for performance trends and success rate visualizations
Flask>=2.3.0  # Web framework for live dashboard server (port 5000)
